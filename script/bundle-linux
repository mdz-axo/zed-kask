#!/usr/bin/env bash

set -euo pipefail

echo "error: script/bundle-linux is disabled in zed-kask" >&2
echo "Zed's Linux bundler creates zed*.app archives that can overwrite the user's real Zed installation." >&2
echo "Build and publish zed-kask only through .github/workflows/kask-release.yml and kask/scripts/build/." >&2
exit 1
