# Rust coding guidelines

* Prioritize correctness and clarity over speed and efficiency.
* Comments explain "why", not "what". No organizational comments.
* Prefer existing files over creating new ones. Avoid many small files.
* No `unwrap()` — use `?` to propagate errors. Be careful with indexing.
* Never silently discard errors with `let _ =` on fallible operations. Propagate with `?`, use `.log_err()` for visibility, or handle explicitly.
* Async errors must propagate to the UI layer for meaningful user feedback.
* No `mod.rs` files — use `src/module.rs` instead.
* New crates: specify `[lib] path = "..."` in `Cargo.toml` (e.g., `gpui.rs`, not `lib.rs`).
* No creative additions unless explicitly requested.
* Full words for variable names (no abbreviations).
* Use variable shadowing to scope clones in async contexts.
* Build: use `./script/clippy` instead of `cargo clippy`.

# Timers in tests

Use `cx.background_executor().timer(duration).await` in GPUI tests, not `smol::Timer::after(...)` — smol timers aren't tracked by GPUI's scheduler and break `run_until_parked()`.

# GPUI traps

* No `block_on` on the foreground thread — it deadlocks if the future needs the GPUI or tokio executor. Make the method async instead.
* Don't use `Arc::get_mut` on `Thread.messages` — it returns `None` when shared. Clone the inner value, modify, replace the `Arc`.
* Don't busy-spin or use timers to wait for deferred tool results in `end_turn` — let the turn end; results drain on the next iteration.
* `AsyncApp` is not `Send` — background tokio tasks must use `tokio::sync::mpsc` channels with a foreground drainer, not capture `AsyncApp`.
* `cx.background_spawn` panics on tokio-dependent futures (reqwest, tokio::time, etc.) — use `gpui_tokio::Tokio::spawn(&*cx, ...)` instead.
* Center-pane `Item` Toggle must use `Toggle` action (not `ToggleFocus`). After `add_item_to_active_pane`, explicitly call `page.focus_handle(cx).focus(window, cx)` if focus delegates to a child entity.

# Pull request hygiene

- Imperative PR title, no conventional commit prefixes, no trailing punctuation.
- Optional crate prefix (e.g., `git_ui: Add history view`).
- `Release Notes:` section as final section, blank line after heading, one bullet (`- Added/Fixed/Improved/N/A`).

# Sankey diagrams

* Conservation is domain-dependent (engineering Sankey conserves, funnels/attribution don't). Carry a per-domain conservation mode.
* Delegate external source extraction to specialized skills — don't ask users to transcribe machine-readable data.
* Never fabricate weights. Use `value=1` placeholder if user declines to provide a weight.

# Rules hygiene

* `.rules` are traps to avoid, not maps to follow. No architectural descriptions.
* New rules must be: non-obvious, repeatedly encountered, and specific enough to act on.
* Single-crate rules go in that crate's `.rules`, not the repo root.
* Don't edit `.rules` inline during feature work — propose additions in PR descriptions.

# Agent loop traps

* **Tool retry death spiral (hard-enforced).** The `ToolRetryTracker` in `crates/agent/src/tool_retry_tracker.rs` tracks two dimensions: `(tool_name, input_hash) → failure_count` (identical retries) and `tool_name → consecutive_failure_count` (trivially different inputs, same tool). After 3 failures, the tool result carries a warning with the Bayesian probability of success (`1/(N+2)`) and a directive to switch tools. After 5 failures, the tool hard-refuses — `handle_tool_use_event` returns an error result without calling `tool.run()`. A successful call resets both trackers. This covers all tools uniformly (built-in + MCP) because they all dispatch through `handle_tool_use_event` → `run_tool`. The tracker lives on `Thread` (per-thread, not per-session).

# zed-kask integration traps

## Divergence surface

`DIVERGENCE.md` defines what's ours (`kask/`) vs upstream Zed. Don't "fix" upstream files speculatively — push fixes into `kask/` behind a D-seam. If an upstream edit is unavoidable, add a D-seam entry + test in the same PR. Every `// zed-kask:` comment disabling upstream behavior needs a test pinning the disabled behavior.

## Failure signals

* Every `set_*` hook wired conditionally must `log::warn!` on failure — operators can't distinguish "not configured" from "configured but broken" without it. `OnceLock` hooks must warn on the `Err` branch of `set`; `Mutex` hooks are re-settable and don't need it.
* Numeric env vars that fail to parse must `log::warn!` naming the malformed value, not silently fall back.
* Opt-in `HKASK_USE_*` features that fail must log the failure classification (HTTP status, error variant), not collapse to `None` via `.ok()?`.
* `unwrap_or(0)` on regulation-loop sense inputs is a broken feedback loop — a DB outage returns 0, which the loop reads as "no deviation." Propagate the error or emit `tracing::warn!`.
* When a deferred task wires multiple hooks in one `if` block, the `else` warn must name ALL unwired hooks, not just one.

## Skill system

* Skills execute via **upstream Zed body injection**: `SkillTool::run` reads the `SKILL.md` body from disk and injects it via `render_skill_envelope`. The model reads the body and follows the instructions. No manifest executor, no step machine.
* `lisp_eval` tool — sandboxed Lisp interpreter (`hkask_lisp::eval_sandboxed_with_budget`). No I/O, no `eval`, no network. Bounded by `max_steps` (default 100000) and `max_depth` (default 64). The model calls it when a SKILL.md instructs deterministic computation (convergence signals, invariant checks, scoring).
* `render_template` tool — renders Jinja2 templates from `kask/registry/templates/` using `minijinja`. Strips YAML frontmatter (`---`-delimited header). Path traversal protection via `canonicalize` + `starts_with` check. Template base path wired via `agent::set_template_base_path()` (OnceLock) in `main.rs` at startup.
* PDCA loops are **model-coordinated**, not machine-enforced. The SKILL.md body describes convergence criteria; the model self-iterates using `lisp_eval` for deterministic checks and `render_template` for structured prompt scaffolding.
* `LazyToolRouter` filters MCP tools only — built-in Zed tools must bypass the router.
* Convention priors from `.rules` must be verified against the codebase before use — rules can be stale.

## Settings and constants

* Kask settings defaults live in `Default` impls only — not in `#[serde(default)]`, `From<Content>` literals, or `mcp_env()` comparison literals.
* Model-name constants must reference `hkask_inference::model_constants::DEFAULT_*_MODEL`, not re-declare literals across crates.
* MCP server credentials/config are scoped per-server via allowlists. New servers use `Some(&[])` for both, never `None`. Allowlists must align with actual env-var reads.
* After `INFERENCE_SOCKET_PATH` is set, call `sync_kask_mcp_servers` again so servers restart with updated env.
* `kask_bridge::identity::mirror_provisioned_db_passphrase` must `.await` to completion in the deferred post-login task **before** governed MCP server launch. Reordering the deferred task breaks first-run provisioning — the primary `ctx.credentials` tier reads the mirrored `kask://credentials/hkask_db_passphrase` entry, and a missing mirror falls back to the env/keychain tier silently (broken feedback loop).

## MCP server patterns

* Tool responses are `{"content": <value>}` envelopes — use `unwrap_tool_envelope`, don't re-implement.
* Error classification must be per-variant, not blanket `McpToolError::internal`.
* Tool inputs accepting arbitrary JSON must use `AnyJsonValue`, not `serde_json::Value` (schemars renders `Value` as bare `true`, which breaks strict-schema providers).
* Live-mutation probe suites must run with `--test-threads=1` and keep probes self-contained.
* Missing credentials must surface as `McpToolError::permission_denied` with the env var named in the message, not as `unavailable`, `invalid_argument`, `failed_precondition`, or a silent fallback. Canonical pattern: read credential from `ctx.credentials.get("ENV_VAR")` → if `None`, return a typed error → map to `permission_denied` at the tool boundary. See `hkask-mcp-swarm/src/abw_client.rs:require_auth` for the reference implementation. A missing credential is an authorization failure, not a transient unavailability or a bad argument. Silent fallbacks (empty results, in-memory DB, skipped env injection) are broken feedback loops — the operator cannot distinguish "not configured" from "no results" or "provider down."
* The canonical `HKASK_DB_PASSPHRASE` resolution helper is `hkask_mcp_server::server::resolve_db_passphrase(&ctx.credentials)` — a 2-tier chain (ctx.credentials → resolve_credential which does env → keychain). All MCP servers that consume `HKASK_DB_PASSPHRASE` must use this helper, not inline re-implementations. See `hkask-mcp-kata-kanban` for the reference pattern.
* `write_credential` / `delete_credential` for `kask://credentials/...` URLs must call `nudge_mcp_servers(cx)` after the keychain write — without it the running server keeps the old key until next launch. The nudge re-writes `kask.mcp.load_default` to itself to fire the `SettingsStore` observer → `sync_kask_mcp_runtime_servers` → env diff → restart. Only `kask://credentials/...` URLs need it; inference-provider `api_url` writes go through zed's provider registry reload.


## Dead code patterns

* Trait-with-one-impl is speculative generality — grep for `self.<field>` before merging; if only constructor matches, it's unwired. This generalizes to: convention helpers with only test callers are dead code, and folded-service modules with no production callers outside the subtree are dead surface.
* Advertised invariants (doc comments claiming security gates, audit surfaces, migrations) must point to the enforcement line. If no enforcement point exists, the doc must say "not yet enforced." Validation gates must return `Undetermined`/`Skipped` when the check wasn't performed, not `Ready` with empty findings.

* Cargo.toml deps outlive their consumers — when deleting a source file, grep for `use <dep>` in remaining `src/` and `tests/`; zero hits = remove the dep.

## Other traps

* `LanguageModelInferencePort` honors `model_override` via registry resolution — silently dropping it caused inference calls to use the wrong model.
* `LanguageModelProvider` registry subscriptions must filter self-events to avoid unbounded warn storms.
* Ontology tag field-drop: a widget that doesn't parse an `ontology` field silently drops it — add an S4 sensor test asserting the widget parses the field.
* `.desktop` files must never declare `text/plain`, `application/x-zerosize`, `x-scheme-handler/zed`, or `Keywords=zed` — run `check-desktop-no-collision.sh` before committing.
* Stale diagnostics after bulk edits: the crate's lib root is authoritative, not individual-file diagnostics. Don't retry stale diagnostics or fix phantom errors.
* LLM-improves-against-LLM-scored-target loops can game the scorer. Three mitigations required: ground in real sources, preserve load-bearing properties, validate against external ground truth.

# Agent ecology verification (paper: "Verification for Agent Ecologies")

* A grounding check that has never been falsified is inert. Every verification contract clause must have a test that breaks it and shows the check going red. Generalizes the existing "every `// zed-kask:` comment needs a test" rule to verification contracts.
* Absence in a delegation result is `Unknown`, not `0.0` or `false`. A `task_success: None` means "not checked," not "failed." A `bind_matched: None` means "no accepts declared or non-text label," not "mismatch." A `grounding: None` means "no contract," not "compliant." Generalizes the existing `unwrap_or(0)` rule to delegation verdicts. Systems that collapse absence into success accumulate unexamined things that look examined.
* After the `PortRegistry` lands (`hkask-mcp-swarm/src/port_registry.rs`), port labels (`accepts`/`produces` on `LocalAgentCard`) are type references, not free strings. Every label must resolve to a registered type. A new label requires a registry entry in the same PR.
* The `classify_request` heuristic in `local_runtime.rs` was deleted — it had no correct setting (widen it and it swallows structured ports, narrow it and it misses real declarations). `check_bind` now returns `Some(true)` only for `accepts: ["text"]` (universal accept) and `None` for everything else. The typing layer at admission (`validate_typing` + `PortRegistry`) is the gate; runtime classification of free-text requests is the typing layer's unfinished transition. Do not re-introduce a runtime classification heuristic — a half-tightened heuristic is worse than a coarse one because it over-reaches on honest input (paper Rule 5.2).
* The grounding contract in `hkask-verification/src/grounding.rs` is hand-declared and therefore incomplete (paper §6). Built-in contracts cover `task`, `research`, `narrator`, `skill`, `creative`, `meta`, and administrative tools; card-declared contracts extend coverage to third-party agents via `card_contract::register_if_valid`. Coverage is itself a metric — do not pretend a grounding check covers all agents when it covers only the ones someone has written it for. Extending to other agent types requires a new contract registered via `VerificationStore::register_contract()` in the same PR.
* The six-valued grounding vocabulary (Sourced / Inferred / Derived / UncommissionedInference / Narrative / Unsourced) in `hkask-verification/src/grounding.rs` distinguishes commissioned judgment from uncommissioned inference and platform derivations. Do not collapse `UncommissionedInference` into `Unsourced` — nulling an agent's legitimate reasoning because no tool "returned" it removes the agent's entire product. The distinction is: a genome size is a fact sitting in a source the agent did not consult (Unsourced); a threat level is a judgment the agent was commissioned to make (Inferred); a file purpose the agent inferred without being asked is UncommissionedInference; a superorder computed from taxonomy by a closed table is Derived.
* The card-declared grounding contract (`hkask-verification/src/card_contract.rs`) validates a card's `output_contract.grounding` at admission time. Every `sourced` entry must name a tool the agent declares in `mcp_tools`. `why` is mandatory (≥40 chars). Closed status set. This is what makes grounding scale to third-party agents.
* The minimal JSON Schema validator (`hkask-verification/src/schema_validate.rs`) supports 7 keywords (type, properties, required, items, enum, const, oneOf). Unsupported keywords are NOT a pass — `unverified_unsupported_schema`. Runs AFTER grounding, BEFORE the payload is consumed.
* The delegation envelope (`hkask-verification/src/envelope.rs`) carries the enforced payload, provenance stamps, and violations across agent-to-agent composition. Grounding survives the hop — the composition path is the protected one.
* The rollup trust contract (`hkask-verification/src/rollup_trust.rs`) is Rung 2 (Truth): documents denormalised fields and their sources of truth. `cost` is capped at `credits_authorized`; `cost_uncapped` is the source of truth. `balance: None` is not 0 — a failed read is not a measured zero.
* The `VerificationStore` (`hkask-verification/src/ledger.rs`) is the single source of truth for grounding records. Every MCP server that delegates to agents must call `VerificationStore::enforce_for_agent()` (or `enforce_and_stamp()`) on each delegation — including delegations inside aggregate tools (fanout, pipeline, execute_plan), not just the server's primary delegation tool. Do not write grounding records to per-tool stores (stigmergy trails, kanban DB) — the central ledger is append-only, cross-tool, and cross-server.
* `VerificationStore::enforce_for_agent()` is the single enforcement entry point. It handles contract lookup, grounding enforcement, record writing, and returns `(Option<GroundingResult>, cleaned_json)`. `enforce_and_stamp()` is the higher-level wrapper that also parses the response and returns an `EnforcementOutcome`. Do not re-implement the parse → enforce → record pattern inline in delegating tools — call `enforce_and_stamp`.
* `GroundingSensor` metrics (`hkask-regulation/src/sensor_provider.rs`) must lead with deletion-resistant indicators. `delegations_with_zero_nulled` is a raw count that cannot be gamed by recording fewer delegations or retiring cards with violations (paper Rule 5.4). The derived `clean_rate` ratio IS gameable. The `curator_grounding_trend` tool surfaces `delegations_with_zero_nulled` as the top-level field before the rates. A DB outage must NOT collapse to "no signal" — the sensor logs a `warn!` and returns `None` (the broken-feedback-loop trap).
* Value-matching (Truth rung) in `grounding.rs` requires the field value to appear in the tool's `result` field, not merely that the tool's name matches. A `Sourced` tag without a `result` to match against is the Antaxius-beieri bug: "sourced" because a source existed, not because the value came from it. The `tool_calls` summary entries must carry `"result": <value>` on success — tools that omit it (old-style `{ "tool": ..., "ok": true }`) cannot be value-matched and are treated as Unsourced.
* `value_contains` in `grounding.rs` has a 10-char minimum guard for substring matching — short strings ("pass", "ok", "0") require exact `Value` equality to avoid false positives. The `transform` hint on `FieldSpec` loosens this to substring matching for all lengths, but only when the contract explicitly declares the agent may transform the value.
* Pass 2 of `enforce_grounding` must process non-derived fields before derived fields (pass 2a → pass 2b). A derived field looks up its source's provenance in `result.provenance` — if the source hasn't been processed yet (HashMap iteration order is non-deterministic), the derived field is incorrectly tagged Unsourced.
* `provenance_strength` rates `Derived` at 4 (a ceiling, not an absolute strength). The actual strength is `min(4, source_strength)`, computed by the weakest-link rule. A dashboard that shows "average provenance strength" will over-rate Derived fields if it reads the number in isolation.
* The `DelegationCounter` trait (`hkask-verification/src/ledger.rs`) is the liveness gap's second data source. Without it wired, `GroundingSensorMetric::LivenessGap` returns 0.0 — honest ("no gap detected") but not informative. The true gap = `counter.delegation_count() - verification_store.total_delegations`.
* The envelope's `GroundingStatus` is three-valued (`Enforced`/`Unenforceable`/`NoContract`). A consumer that reads `violations: []` without checking `grounding_status` will treat an ungrounded delegation as clean. The old `grounding_enforced: bool` / `has_grounding_contract: bool` pair was ripped because both were set to the same value — a bool cannot distinguish "no contract" from "contract existed but output wasn't JSON."
* `FieldSpec.response_path` defaults to `""` (search the entire result) via `#[serde(default)]`. This is the loosest matching mode. Card-declared contracts without `response_path` get a warning finding (`grounding_sourced_response_path`) — built-in contracts use `""` deliberately but should be tightened with specific paths where the tool's return structure is known.

# kask registry guidelines

* Convergence-check `primary_result` must incorporate ALL substantive step results, not just the first. Exception: single-step skills can use `step_1_result` directly.
* Config sub-structs must use `#[serde(default, deny_unknown_fields)]` — without it, extra fields are silently dropped.