.agents/skills/constraint-forces-recast/SKILL.md:68:- All flow templates are `KnowAct` type with `Public` visibility. Reference documents are `RenderAct`.
.agents/skills/company-research-deep/SKILL.md:8:Equity research deep pipeline converted from EFRA-AI (Replicant-Partners). Sequential 16-step flowdef producing a deep company analysis and investment thesis. MCP tool calls (company_transcript, dcf_valuation, comparable_analysis, web_search, web_extract, scenario_build) are called directly; templates do LLM synthesis over their outputs.
.agents/skills/company-research-deep/SKILL.md:81:4. Do NOT self-evaluate — the investment_grade / needs_work / incomplete verdict comes from the cross-skill goal-analysis/judge step in the flowdef.
.agents/skills/idiomatic-rust/SKILL.md:118:- Compiler grounding is preferred but not required — when LSP tools are unavailable (pure FlowDef execution), the skill falls back to intrinsic reasoning with reduced confidence.
.agents/skills/improv/SKILL.md:8:Composable interaction grammar for hKask agents. Five improv modes — Plussing, Yes And, Yes But, Freestyling, and Riffing — provide constructive-by-default communication protocols for dual-presence chat, ensemble sessions, and kata coaching loops. A selector KnowAct evaluates conversation context and routes to the appropriate WordAct; convergence is evaluated by the model after each iteration.
.agents/skills/improv/SKILL.md:27:3. Do NOT apply the mode — routing to individual WordActs is handled by the manifest flow.
.agents/skills/deep-module/SKILL.md:70:- All templates are `KnowAct` type with `Public` visibility.
.agents/skills/deep-module/SKILL.md:71:- Templates use KnowAct with Public visibility.
.agents/skills/sankey-flow/SKILL.md:139:4. **Surface the diagram.** A final `render` step (`present-sankey.j2`, RenderAct — deterministic, no LLM call) wraps the adapt step's mermaid source in a titled, annotated markdown document with a title, description, the mermaid diagram, the conservation check, a data table annotating each flow with its weight and provenance, and references. This becomes the cascade's final output — the fenced ```mermaid block reaches the chat stream directly, not buried inside a JSON object field.
.agents/skills/sankey-flow/SKILL.md:183:- **Visual artifact surfacing** — the `present-sankey.j2` render step (RenderAct) must be the cascade's final output step. It surfaces the fenced ```mermaid block as a raw markdown string so acp_thread's mermaid renderer picks it up. Removing it causes the diagram to stay buried in the adapt step's JSON output — the model must then discover and extract the `markdown` field, which is fragile.
.agents/skills/superforecasting/SKILL.md:52:The former single inside-view step is split into three FlowDef steps. Generation and counterfactual analysis are delegated to the `falsifiability` skill; probability estimation stays in superforecasting.
.agents/skills/metacognition/SKILL.md:80:- All flow templates are KnowAct type with Public visibility. Reference documents are RenderAct.
.agents/skills/wardley-mapper/SKILL.md:65:1. RenderAct (step 7, `render` action, `present-map.j2`): surfaces the quadrant chart (from map-value-chain's `map_diagram`) and strategic recommendations (from synthesize-recommendations) as a single markdown string containing a fenced ```mermaid block.
.agents/skills/wardley-mapper/SKILL.md:89:- `present-map.j2`: Public. RenderAct (no inference) — surfaces the diagram as the cascade's final output.
.agents/skills/gradient-hunter/SKILL.md:111:- All flow templates are `KnowAct` type with `Public` visibility. Reference documents are `RenderAct`.
.agents/skills/capabilities-reasoner/SKILL.md:225:- All flow templates are `KnowAct` type with `Public` visibility. Reference documents are `RenderAct`.
.agents/skills/refactor-architecture/SKILL.md:113:- All templates are `KnowAct` type with `Public` visibility.
.agents/skills/company-research-flash/SKILL.md:8:Equity research flash pipeline converted from EFRA-AI (Replicant-Partners). Sequential 23-step flowdef producing a flash note / initiation report. MCP tool calls (forecast_list, research_search, web_search, company_transcript, scenario_build, dcf_valuation, comparable_analysis, expectations_gap, scenario_impact_valuation, market_check_resolutions, market_calibration, market_match, evaluate_evidence, forecast_persist) are called directly; templates do LLM synthesis over their outputs.
.agents/skills/company-research-flash/SKILL.md:93:- Step 6 reuses the full `listening` skill as a sub-flowdef (MAIA v3 earnings-call listening, no-fabrication invariant).
.agents/skills/prompt-enhance/SKILL.md:93:- All templates are `KnowAct` type with `Public` visibility.
.agents/skills/lean-prover/SKILL.md:112:- All templates are `KnowAct` type with `Public` visibility.
.agents/skills/sequential-inquiry/SKILL.md:74:- All flow templates are KnowAct type with Public visibility.
.agents/skills/coding-guidelines/SKILL.md:42:- Visibility is Public across all KnowAct templates; the anti-patterns fragment has no standalone contract
.agents/skills/ui-layout-discipline/SKILL.md:61:- All templates are `KnowAct` with `Public` visibility; they emit `reg.ui_layout.*` spans.
.agents/skills/code-review/SKILL.md:107:- All templates are `KnowAct`, `Public`
.agents/skills/swarm-intelligence/SKILL.md:166:a new FlowDef execution surface (the Curator's normal tool-call turn IS the
.agents/skills/swarm-intelligence/SKILL.md:232:- Reference: `kask/registry/templates/swarm-intelligence/swarm-patterns.yaml` (RenderAct — PSO/ACO/Reynolds/Onto4MAT tuning palette; not sent to the LLM)
.agents/skills/swarm-steering/SKILL.md:28:array — closing the feedback loop without a new FlowDef execution surface (the
.agents/skills/swarm-steering/SKILL.md:127:- Templates: `kask/registry/templates/swarm-steering/swarm-steering-direct.j2` (KnowAct — produce the local-swarm steering directive: delegation sequence + delegate_results collection shape + re-invoke instruction)
.agents/skills/hypothesis-framer/SKILL.md:46:- All templates are `KnowAct` type with `Public` visibility
.agents/skills/diataxis-diagram/SKILL.md:33:7. **Surface the diagram.** The write step produces `{file_path, file_content, description_paragraph}` as JSON. A final `render` step (`present-diagram.j2`, RenderAct — deterministic, no LLM call) flattens the `file_content` field into a raw string, which becomes the cascade's final output. This ensures the fenced ```mermaid block reaches the chat stream — without it, the diagram stays buried inside a JSON object field that the model must discover and extract.
.agents/skills/diataxis-diagram/SKILL.md:60:- **Visual artifact surfacing** — the `present-diagram.j2` render step (RenderAct) must be the cascade's final output step. It surfaces the fenced ```mermaid block as a raw markdown string so acp_thread's mermaid renderer picks it up. Removing it causes the diagram to stay buried in the write step's JSON `{file_path, file_content}` object.
.agents/skills/diagnose/SKILL.md:52:- All templates are `KnowAct` type with `Public` visibility
.agents/skills/gradient-seeded-recombination/SKILL.md:69:- All flow templates are `KnowAct` type with `Public` visibility. Reference documents are `RenderAct`.
