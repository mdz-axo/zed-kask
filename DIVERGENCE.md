# DIVERGENCE.md — zed-kask fork divergence from upstream Zed

> **Purpose:** the headline doc for upstream-sync conflict resolution. Every
> `git fetch upstream && git merge upstream/main` consults this file to know
> which crates/files are divergent and why. Referenced from
> `kask/docs/architecture/zed-host-architecture-plan.md` (§13.4).
>
> **Rule:** everything under `kask/` is ours (additive; upstream never touches
> here → near-zero merge conflict). Everything else tracks upstream; the only
> divergences are the D-seams listed below + the `[workspace.members]` /
> `[workspace.dependencies]` arrays in the root `Cargo.toml`.

## Release identity — 0.40.0 (operator decision, 2026-09-10)

D7: the `zed-kask` application (`crates/zed/Cargo.toml`, package `zed`) now
inherits `[workspace.package].version` alongside the Kask crates. The release
is **0.40.0**, based on upstream Zed **1.21.0** at `595d62863e`; upstream
library package versions remain unchanged. `main.rs` already supplies
`CARGO_PKG_VERSION` to `AppVersion::load`, so default application version
reporting follows the workspace release (with existing build metadata).
The explicit `ZED_APP_VERSION` override remains available.

Pinned by `kask/scripts/check-version-sync.sh` and its self-test: the app
manifest must inherit the workspace version, rather than use an independent
literal. Observed RED on the upstream `1.21.0` declaration before the change.
The source installer's no-manifest fallback and current-version README
labels are updated to 0.40.0; historical document revisions are not releases
and are not rewritten. This change does not create a release tag or install
binaries.

## Upstream merge recovery — 2026-09-10

Recovered the interrupted merge from fork `f0e2d97e1f` into upstream
`595d62863e8a6f2d8353835abe94502ee2a08139` (Zed 1.21.0). Existing staged
resolutions were preserved. Shared stream mapping and schema normalization
required mapped reapplication (D20/D25/D36/D47); other remaining conflicts
were surgical merges retaining both upstream changes and Kask wiring.

Recovered fork policy, not new product decisions: `5269cf00ca` keeps
`reasoning_effort` naming; `5f939570bb` and `a2134949e2` make explicit reasoning
disable unconditional at the provider boundary; `fe2bfc050c` keeps skill URL
import and marketplace code removed. The Responses API preserves its
seven-argument converter and explicit `effort: none`, pinned by
`into_open_ai_response_sends_none_when_thinking_is_disabled` and the existing
request-payload tests. RunPod (D29) now shares the core streaming mapper;
Kask tool schemas (D44/D47) use the new normalized-JSON interface.

The merge retains D7/D16 isolation, Kask workspace members, and the calloop
pin. `Cargo.lock` was regenerated against the combined workspace. Validation
ran on the uncommitted merge tree: full Zed and bridge/foundation checks,
scoped `script/clippy` with all targets/features, isolation and dependency
boundary checks, plus focused stream/schema, skill, persistence, provider,
and composition-root tests. This is build/test evidence, not an installed
application smoke test. The merge commit is the completion record.

## Core-review continuation — 2026-09-04

**D8 embedding startup:** `crates/zed/src/main.rs` now passes
`ResolvedEmbeddingCredentials` directly to `LanguageModelEmbeddingPort::new`.
The bundle retains the provider descriptor with its key; bare endpoint/key
arguments and the bridge's permissive prefix-stripping helper are removed.
Provider-qualified mismatches fail before HTTP, including through IPC, per the
operator-approved D2 core-review decision. Pins:
`embedding_provider_mismatch_sends_no_http` and
`ipc_embedding_provider_mismatch_is_invalid_request`; the changed composition
root is also checked by `cargo check -p zed`.

**D3 runtime (Kask-owned implementation):** desired launch generations are
cancelled on stop/replacement. Startup publication shares a lifecycle lock with
stop; a drop guard retains failed-start cleanup ownership. Reconnect is polled
on a runtime worker rather than blocking its caller. Handshake/discovery each
have a configurable deadline (`HKASK_MCP_STARTUP_TIMEOUT_SECS`, default 60s).
Pins in `reconnect_integration.rs`: `unload_during_discovery_stays_unloaded`,
`discovery_failure_reaps_child`, `replacement_during_discovery_keeps_new_configuration`,
`foreground_progresses_during_reconnect`, and `stalled_discovery_has_a_phase_deadline`.
These supersede older runtime timing/ownership descriptions below, not the
outcome-unknown no-replay rule.

**D8/D9 authority and lifetime continuation — 2026-09-05:** the operator's
D1 ruling is implemented by `delegation_grants` and the `tool_invoke` gate.
`KaskMcpSettingsContent::delegated_tools` flows into `KaskMcpSettings`, then
`build_mcp_server_env` injects an opaque per-server `HKASK_TOOL_GRANT`. Empty
configuration denies tools; request allowlists can only narrow the parent grant.
Grant replacement/unload revokes old tokens. `main.rs` revokes before settings
unload; disabled-server env diffs cannot remint grants, and registry changes
precede credential awaits. Pins: `ipc_child_cannot_expand_parent_grant`,
`delegated_grants_follow_settings_and_launch_lifecycle`,
`grants_are_stable_until_changed_and_revoked`, `main_unload_revokes_before_stop`.
This is not PID binding or same-UID OS isolation. Provisioning is documented in
`kask/docs/reference/kask-settings.md`; existing card allowlists alone do not
provision parent authority.

The operator's D3 ruling bounds total admitted chat work at twice configured
concurrency, with `Overloaded` before dispatch and `Timeout` over queue/model
resolution/establishment/drain. A GPUI-clock expiry check also covers an unpolled
receiver; caller closure releases request-owned capacity. `handle_connection`
monitors actual Unix EOF and refuses pipelining; no unknown-effect replay is
introduced. Pins: `cancelled_queued_request_never_starts_model`,
`queued_and_established_requests_share_deadline`,
`streaming_deadline_covers_unpolled_queue_and_drain`,
`streaming_cancellation_releases_permits_with_disabled_deadline`,
`ipc_disconnect_drops_pending_dispatch`, and the inference client's
`ipc_client_receives_server_timeout_before_closing`. AIMD is unchanged.

## The divergence surface (D1–D55)

Every hKask integration maps to a named, isolated change in zed-kask. These
are the _only_ edits to zed-kask's tree outside `kask/`. Any hKask behavior
that would require touching other Zed crates is a smell — push the logic into
an hKask crate behind one of these seams instead.

| D   | Surface                                                                                 | zed-kask crate / file                                                                                                                                                                                                                                                                                                                                | What's wired                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --- | --------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | Skill execution (`lisp_eval` + `render_template` tools)                             | `crates/agent/src/tools/skill_tool.rs` + `crates/agent/src/tools/lisp_eval_tool.rs` + `crates/agent/src/tools/render_template_tool.rs` + `crates/agent/src/tools/record_skill_feedback_tool.rs` + `crates/agent/src/agent.rs` + `crates/agent_skills/agent_skills.rs` + `crates/zed/src/main.rs`                                                                                                                           | `SkillTool` follows **upstream Zed**: reads the `SKILL.md` body from disk via `agent_skills::read_skill_body` and injects it via `render_skill_envelope`. The model reads the body and follows the instructions. **2026-09-10 merge (uncommitted):** `agent.rs` adopts upstream's `SkillTool::with_body_resolver` registration and promotes `skill_body_resolver_for_project` to production; slash activation uses the same resolver. Project-local bodies come through project buffers (remote workspaces and unsaved edits), global bodies through the filesystem. The Kask outcome/feedback hooks, dependency checks, core-skill overrides and authorization policy remain requirements. `skill_tool.rs` now dispatches through `with_body_resolver`, retaining all three `record_skill_outcome` sites and the dependency/authorization flow. The 81-test agent skill slice passed, including project-buffer resolution, authorization, dependency failure and outcome recording. Kask's disk-seeded core skills remain distinct from upstream's `BuiltIn` skill catalogue. Resolver pin: `test_project_skill_body_resolves_through_buffer`; hook pins below remain. **`lisp_eval` tool:** built-in (`crates/agent/src/tools/lisp_eval_tool.rs`), wraps `hkask_lisp::eval_sandboxed_with_budget`. Registered in `add_default_tools`. `hkask-lisp` workspace dependency in `crates/agent/Cargo.toml`. **`render_template` tool:** built-in (`crates/agent/src/tools/render_template_tool.rs`), renders prompt templates from `kask/registry/templates/` with `minijinja`. Path traversal protection via `canonicalize` + `starts_with`, applied to both the top-level ref and `{% include %}` resolution (`bind_registry_loader`: includes resolve within the registry root and receive the same frontmatter stripping as the top-level render; pinned generically — corpus-derived, no hardcoded file names — by `corpus_includes_resolve_and_strip` and `test_include_traversal_is_blocked`). Template base path wired via `agent::set_template_base_path()` (OnceLock) in `main.rs` at startup. `minijinja` workspace dependency in `crates/agent/Cargo.toml`. Registered in `add_default_tools` (alongside `lisp_eval`). **Production template seeding:** `agent_skills::seed_templates` (build.rs embeds `kask/registry/templates/*.j2` via `include_str!`) writes the compiled-in templates to `{kask_data_dir}/skills/registry/templates/` at startup, always (dev and production), so MCP servers (corpus, training) that read `HKASK_TEMPLATE_ROOT` find them regardless of CWD. `kask_bridge::mcp_env()` always emits `HKASK_TEMPLATE_ROOT` — when the operator hasn't overridden `corpus.template_root`, it resolves to `{data_dir}/skills/registry/`; when overridden, it uses the operator's value. Pinned by `test_seed_templates_writes_all_shipped_templates_to_disk`, `test_seed_templates_overwrites_existing_files` (agent_skills), `mcp_env_always_emits_template_root`, `mcp_env_template_root_follows_data_dir_override`, `mcp_env_template_root_override_emits_user_value` (kask_bridge). **PDCA:** model-coordinated — the SKILL.md body describes convergence criteria; the model uses `lisp_eval` for deterministic checks and `render_template` for structured prompt scaffolding. **Core-skill frontmatter enforcement** (retained): `parse_skill_file_content_for_loading` refuses `core: true` on non-`CORE_SKILL_NAMES` skills. Marketplace-installed skills (`SkillSource::Public`) and `load_marketplace_skills` removed (skill marketplace retired). D28 storage remains. **Skill outcome recording (operator-ratified 2026-09-08):** `SkillTool::run` fires `agent::record_skill_outcome(skill_id, success, error)` at its outcome points — success on a rendered envelope, failure on missing dependencies or an unreadable body; not-found and authorization-denial are request errors, not skill outcomes, and are deliberately not recorded. The hook is a re-settable `ProcessGlobal` in `agent.rs` (`set_skill_outcome_recorder` / `record_skill_outcome` — the same pattern as `set_mcp_tool_outcome_recorder`, T-V1); `main.rs` wires it to `RegulationLedger::record_skill_span(skill_id, "outcome", payload)` on the GPUI-global tokio runtime, feeding the metacognition loop's `sense_feedback_drift` (per-skill success-rate decline). This is the write side the read side shipped without — until this wiring the `SkillSpanStore` was permanently empty and drift sensing could never fire. Scope: measures skill *activation* reliability, not work quality (which happens across subsequent turns). The vacuous manifest gate `kask/scripts/check-skill-span-namespace.sh` + its selftest are deleted with this wiring (they validated a `ledger.span_namespace` field no manifest ever declared, in a manifests directory that does not exist — a flagged-but-unfixed "0 = 0" pass); the enforcement is now the runtime pins below. The `refuse_skill_catalog_read` no-op stub and its pin tests are deleted (D1-revert residue: the function returned `Ok(())` unconditionally — a gate that gated nothing, with two tests pinning that it does nothing). **Skill operator-feedback recording (T15, 2026-09-09; operator ruling: BOTH channels are requirements — "it is not a or b -- it is a and b"):** the `operator_feedback` ledger reader (the metacognition loop's "declining operator acceptance" drift trend) shipped with no writer — that half of drift sensing read a permanently-empty phase. Both specified producers now fire one process-global hook, mirroring the outcome recorder above: `OperatorFeedbackRecorder` + `set_operator_feedback_recorder` / `record_operator_feedback` in `agent.rs` (re-settable `ProcessGlobal`; unwired drops with a debug log — the absent-input state stays explicitly unobserved, never a fabricated disposition). Channel (b) — the operator's direct rating: the `record_skill_feedback` built-in tool (`tools/record_skill_feedback_tool.rs`; registered per-session in `register_session` next to `SkillTool`; input: `skill_id`, `accepted`, optional `note`) is task-breakdown's recording surface for "overridden tasks, rejection reasons, corrected_fields". Channel (a) — the advice-apply bridge: a successful `curator_advice_mark_applied` whose response names a skill is the operator accepting that skill's recommendation (lora-training: "the operator reacts to a recommendation"); the curator server persists `skill_id` into the escalation context as the durable record (`AdviceAppliedRequest.skill_id`, serde-defaulted; echoed in the response) and the editor-process run wrapper fires the hook (see the `context_server_registry.rs` entry under Other zed-kask-modified files). `main.rs` wires the hook to `RegulationLedger::record_skill_span(skill_id, "operator_feedback", {accepted, note?})` on the GPUI-global tokio runtime — the write side the acceptance-drift reader shipped without. Pinned by `operator_feedback_recorder_records_and_is_replaceable` (agent.rs — records + re-settable), `records_the_operators_disposition` (record_skill_feedback_tool.rs — the direct rating reaches the hook), `test_advice_apply_bridge_fires_only_on_skill_naming_success` (context_server_registry.rs — the bridge decision), and `advice_apply_persists_and_echoes_skill_feedback` (hkask-mcp-curator tests/tool_behavior.rs — persistence + echo, and no fabricated skill when absent). **Three-gate visibility completion (2026-09-09, post-rebuild review):** the tool initially shipped registered but invisible — `Thread::enabled_tools` drops any built-in absent from the shipped `write`/`ask` profile allowlists (`assets/settings/default.json`; `AgentProfileSettings::is_tool_enabled` treats unlisted as disabled), and the tool was missing from all three documented silent-drop gates (the `tools!` macro registry, the profile lists, the permission-UI `EXCLUDED_TOOLS`). Fixed in one pass: `RecordSkillFeedbackTool` added to the `tools!` macro (the SkillTool precedent — macro-listed, per-session-constructed), `"record_skill_feedback": true` added to the write + ask profiles, and an `EXCLUDED_TOOLS` entry (telemetry hook, no permission-relevant side effects). Gate 1 was testless — the silent-drop class had no pin — so `every_built_in_tool_is_listed_in_the_default_write_profile` (tools.rs; `serde_json_lenient` dev-dep parsing the shipped JSONC) now requires every `ALL_TOOL_NAMES` entry to be present in the write profile (true, or false for a deliberate disable — the `ask_user` pattern); observed RED naming the missing tool, GREEN after the profile fix. Pinned by `test_skill_tool_returns_content`, `test_system_prompt_skills_section_describes_body_retrieval`, `test_skill_tool_missing_dependency_fails_fast` (dependency pre-check), `skill_outcome_recorder_records_and_is_replaceable` (agent.rs — hook records + is re-settable), and `test_skill_tool_records_outcome` (skill_tool.rs — success/missing-deps recorded, not-found not recorded). |
| D2  | Curator agent                                                                           | `crates/agent/src/agent.rs` + `crates/agent_ui/src/agent_ui.rs` + `crates/agent/src/kask_thread_state.rs` + `crates/hkask-steer/`                                                                                                                                                      | `Agent::Curator` variant; `CURATOR_AGENT_ID`; selectable in Agent Panel. The Curator overlay's `CURATOR_STATIC_CONTEXT` (`crates/agent/src/curator_agent_server.rs`) is appended via `Thread::set_static_context` (delegates to `KaskThreadState::set_static_context`) and rendered by the `## Session Context` block in `system_prompt.hbs`; that block is a **sibling** of the `{{#if (or user_agents_md has_rules)}}` custom-instructions guard (not nested inside it), so it renders even for projects with no `.rules` and no personal `AGENTS.md` (regression: previously nested → silently dropped). Pinned by `test_system_prompt_renders_session_context_without_rules_or_agents_md` in `templates.rs` and `test_system_prompt_override_bypasses_template` in `thread.rs`. **`with_extra_static_context` is zed-kask's mode mechanism.** Upstream Zed has no modes; Cline/Roo deliver phase constraint via a runtime mode token. zed-kask instead composes a per-context overlay onto the base prompt through this one channel: `CURATOR_STATIC_CONTEXT` (~1.1 KB) plus, in Steer, `swarm_panel::steer_system_prompt` (~9.7 KB) or `kanban_panel::steer_system_prompt` (~2.2 KB), each also `with_mcp_server_scope`-d to one MCP server so the tool surface matches what the overlay advertises. Two consequences follow, and both have bitten: (1) an overlay is captured **once** at conversation construction, so any panel state it bakes in must invalidate the conversation on change — the shared `hkask_steer::SteerSurface` ( `crates/hkask-steer/` ) owns this lifecycle (`ensure` / `invalidate`); the compose flow and the swarm-mode observer call `steer.invalidate()` for this reason; (2) an overlay that advertises tool names must derive them from the server's actual tool list, or a rename degrades to "tool not found" at dispatch — since 2026-09-04 the four Steer overlays (swarm, kanban, portfolio, media) RENDER their tool lists from the server's build.rs-generated `TOOL_NAMES` (`hkask_steer::render_grouped_tool_advertisement` / `render_tool_names`; the swarm panel splits ABW vs local via `hkask_mcp_swarm::CLOUD_SWARM_TOOL_NAMES` plus its complement in `TOOL_NAMES`), so the lists cannot drift (the 2026-09-03 media consolidation shipped stale names because the lists were hand-written); `hkask_steer::verify_tool_advertisement` remains the check on prose tool mentions, and the panels' prompt-token tests (`steer_prompt_advertises_only_known_tools` + `server_tools_are_all_advertised`, in all four panels) are the CI enforcement. **Steer split (2026-09-07, operator ruling that only `kask_bridge` may depend on zed crates):** the portable half of `hkask-steer` — `advertised_tool_names`, `render_tool_names`, `render_grouped_tool_advertisement`, `verify_tool_advertisement` + their 8 unit tests — moved to `kask/crates/hkask-steer-core` (std + `log` only, no editor closure); `crates/hkask-steer` keeps the `ConversationView` lifecycle (`SteerSurface`/`SteerContext`/`ensure_steer`/`ThreadPicker`) and re-exports the core fns, so all four panels' `hkask_steer::` call sites and the prompt-token tests are unchanged. Treat these as the two standing obligations for any new overlay. **Refactored 2026-08-27:** the Steer conversation lifecycle (previously hand-rolled per panel as `ensure_steer_conversation` + `steer_conversation`/`steer_connection_store` fields) is shared in `crates/hkask-steer/`; the hand-mirrored kanban tool lists (`ADVERTISED_KANBAN_TOOLS`, `parse::KANBAN_TOOLS`) are deleted in favor of the kanban server's generated `TOOL_NAMES`. **One-call helper added 2026-08-28:** `hkask_steer::ensure_steer` combines `verify_tool_advertisement` + `SteerSurface::ensure`; panels pass a `SteerContext` + prefixes and the helper bakes the prompt, lands the view, and caches it. It is the panel-facing API; the raw `ensure` remains exposed for tests with a stub factory. **Portfolio Steer-only panel added 2026-08-28:** `crates/portfolio_panel` registers a Steer-only variant (no browse forms — portfolio chat-driven CRUD through the scoped curator is the whole surface), proving the helper's adoption path is a single call. **Refactored 2026-08-26:** `agent_static_context`, `system_prompt_override`, and `mcp_server_scope` moved from `Thread` fields to `KaskThreadState` (`crates/agent/src/kask_thread_state.rs`). The turn-loop call sites use `self.kask.static_context()` / `self.kask.system_prompt_override()` / `self.kask.mcp_server_in_scope()` etc. (The refactor plan doc `kask/docs/plans/thread-hooks-refactor-2026-08-26.md` has since been removed; this entry is the record.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D3  | hKask tools in-process                                                                  | `crates/zed/src/main.rs`                                                                                                                                                                                                                                                                                                                             | `McpRuntime` implements `ToolPort` directly (call metering + `reg.mcp.cap` spans and a `SpanKind::ToolCompleted` RegulationRecord at target `reg.mcp`; the `reg.tool` ontology tag was removed in e0ca5d5f7f) and is passed wherever a `ToolPort` is needed — no bridge adapter. MCP servers run as child processes (stdio). Daemon transport deleted; identity from `ServerContext.webid` (`HKASK_WEBID`). **`McpRuntime::invoke` performs NO per-call authorization.** The former capability-match gate was deleted 2026-08-12 (RR-0056): all three production mint sites derived the `DelegationToken`'s `resource_id` from the same tool name they passed to `invoke`, so `is_valid_for` compared a caller-supplied value against itself and could not deny — it ran on every tool call and denied nothing while being advertised here as "the enforced gate." `DelegationToken`, `panel_default_token`, `capabilities_match`, `capability_from_server_id`, and `ToolPortError::CapabilityDenied` are gone; `invoke` now takes `agent: WebID` (accounting identity, not a credential). Authority lives at the boundaries whose list the caller does not choose: the inference IPC parent-held grant intersected with `tool_allowlist` (fail-closed, D8), the swarm card `mcp_tools` allowlist, and the per-server MCP env/credential allowlists (RR-0038). Previously pinned by `tests/invoke_gate.rs::{unregistered_agent_is_auto_registered_not_denied, exhausted_ceiling_trips_the_runaway_breaker, no_governance_dispatches_unmetered, unreachable_server_reports_unavailable_not_failed, only_unavailable_is_retryable, interrupted_is_never_auto_retried, interrupted_and_unavailable_are_distinguishable, unknown_tool_is_not_found_not_unavailable}` (public-API metering + retry-classification tests); that file and the inline `reconnect_path_tests` module were both deleted with the condenser teardown and **not restored** — `runtime.rs` has no `#[cfg(test)]` section and the `kask/crates/hkask-mcp/tests/` directory no longer exists. **Restored 2026-08-24:** the metering + retry-classification behavior is now re-pinned by `runtime.rs::metering_tests::{unregistered_agent_is_auto_registered_not_denied, exhausted_ceiling_trips_the_runaway_breaker, no_governance_dispatches_unmetered, only_unavailable_is_retryable, interrupted_is_never_auto_retried, interrupted_and_unavailable_are_distinguishable, unknown_tool_is_not_found_not_unavailable}` (7 inline tests). Settings-change restart: governed McpRuntime instances are started once at login; `sync_kask_mcp_runtime_servers` (a `SettingsStore` observer) diffs each server's computed env (`kask_server_env`) against the launch baseline and restarts changed servers via `McpRuntime::stop_server` + `start_server_with_env`, so e.g. a `kask.swarm.mode` toggle re-routes the governed path; the restart logs the *changed env keys* (never values — several are credentials) because a restart tears down live connections and fails every in-flight panel call, so "env changed" alone could not distinguish a deliberate toggle from a startup-ordering artifact. **Connections self-heal.** A dead connection used to be sticky and unrecoverable: the keeper task's `running.waiting()` arm exited without removing the entry, `start_server_with_env` early-returned on a presence-based `connections.contains_key` check, and `get_peer` handed out the corpse forever — so every call returned `Transport closed` until an operator changed a setting. Four mechanisms now close that loop (`kask/crates/hkask-mcp/src/runtime.rs`): (1) the keeper task reaps its own connection when the service loop exits on its own, guarded by a generation stamp so a late-exiting keeper cannot remove a healthy replacement installed by a reconnect; (2) `get_peer` filters peers whose `is_transport_closed()` is already true, covering the window before the keeper is scheduled; (3) `start_server_with_env` records a `LaunchSpec` so `call_tool_inner` reconnects on demand and retries **once**, bounded by `RECONNECT_COOLDOWN` (5s) so a crash-looping binary cannot become a process-spawn storm; **(4) a per-server health supervisor** checks transport liveness every 60s (`HEALTH_CHECK_INTERVAL`) and proactively removes dead connections before the next tool call discovers them, with a circuit breaker that stops auto-healing after `MAX_CONSECUTIVE_HEALTH_FAILURES` (3) consecutive failures and logs an operator-actionable error. **The breaker is real as of 2026-08-29** — before that, the supervisor only degraded its check interval and restarted forever (the live crash-loop defect: a keyless instance respawned a new pid every interval with no stop condition, contradicting this advertised invariant). The stop is not a permanent outage: any explicit start (settings-change restart, operator action, `call_tool_inner`'s on-demand reconnect) spawns a fresh supervisor whose first healthy check resets the counter. The degraded-interval mechanism (`HKASK_MCP_DEGRADED_HEALTH_CHECK_INTERVAL_SECS`) was removed with it. Breaker + idempotence pinned by `reconnect_integration.rs::{health_breaker_trips_after_consecutive_restart_failures, second_start_with_same_env_is_a_noop}` (invariants I4/I5 of the MCP lifecycle review). The supervisor does not call `try_reconnect` directly (the future is not `Send`); it removes the dead connection and relies on mechanism (3) for healing. `start_server_with_env` also retries spawn+handshake up to `STARTUP_MAX_RETRIES` (3) with exponential backoff (500ms→10s) before reporting failure, and pipes child stderr to `tracing` with `target: "hkask.mcp.child"` and the `server_id` field for attributed diagnostics. A TOCTOU race between concurrent `start_server_with_env` calls (e.g. startup + on-demand reconnect) is handled by checking at insert time: the loser's keeper is cancelled and its child process killed, the winner's connection is kept. `stop_server`/`shutdown_all` drop the launch spec and clear `health_failures` so a deliberate stop is not resurrected. The idempotency check is now liveness-based, not presence-based. **Retry safety is classified by provable delivery, not by "was it a transport error".** `ToolPortError` gained two variants: `Unavailable` (no live peer accepted the request — the tool provably did not run, so a retry cannot double a side effect) and `Interrupted` (a live peer accepted it and the connection then failed — **the outcome is unknown**). The distinction is forced by rmcp, which reports both a failed send (`service.rs:921`) and a dropped response channel (`service.rs:555,566`) as the same `ServiceError::TransportClosed`: once a request reaches a live peer, a transport loss is not proof of non-delivery. `is_retryable()` is true for `Unavailable` only; `Interrupted` is never auto-retried, at any layer, because doing so would duplicate state-changing effects (two `kanban_task_create`s, a `swarm_hire` charging credits twice). Internally `DispatchError::{NotDelivered, Interrupted, Failed}` carries this, and only `NotDelivered` drives the one-shot reconnect-and-retry. Previously pinned by `runtime.rs::reconnect_path_tests::{failed_start_still_records_a_launch_spec_for_later_reconnect, stop_server_clears_the_reconnect_path, shutdown_all_clears_every_reconnect_path, reconnect_is_rate_limited_by_the_cooldown, metadata_only_server_cannot_be_reconnected}` (inline module asserting on the private `launch_specs` map and `try_reconnect`); that module was deleted with the condenser teardown and **not restored** — `runtime.rs` has no `#[cfg(test)]` section. **Restored 2026-08-24:** the reconnect-path recording and cooldown behavior is now re-pinned by `runtime.rs::reconnect_path_tests::{metadata_only_server_cannot_be_reconnected, stop_server_clears_the_reconnect_path, shutdown_all_clears_every_reconnect_path, reconnect_is_rate_limited_by_the_cooldown, failed_start_still_records_a_launch_spec_for_later_reconnect}` (5 inline tests). Healing was previously verified against real child processes by `tests/reconnect_integration.rs` (5 tests, `--features test-fixture`), which SIGKILLs a controllable fixture MCP server (`src/bin/mcp_test_fixture.rs`) and asserts the next call reconnects and is served by a *new* pid; that file was deleted with the condenser teardown and **not restored** — the `kask/crates/hkask-mcp/tests/` directory no longer exists. The mutation tests (`killed_server_is_reconnected_on_the_next_call`, `dead_connection_is_reaped_from_the_runtime`) are gone with it, so the four self-heal mechanisms are currently unpinned. **Restored 2026-08-24:** `tests/reconnect_integration.rs` is restored with 7 integration tests (`--features test-fixture`, `--test-threads=1`): `killed_server_is_reconnected_on_the_next_call` (mechanism 3 — on-demand reconnect), `dead_connection_is_reaped_from_the_runtime` (mechanism 1 — keeper reaps), `stop_server_then_call_returns_unavailable` (deliberate-stop clearing), `startup_retry_with_backoff` (startup retry), `health_supervisor_removes_dead_connection_without_tool_call` (mechanism 4 — health supervisor, using `HKASK_MCP_HEALTH_CHECK_INTERVAL_SECS=1` to reduce the 60s wait), plus `second_start_with_same_env_is_a_noop` and `health_breaker_trips_after_consecutive_restart_failures` (the I4/I5 invariants cited above). The four self-heal mechanisms are now pinned. The fixture's kill path uses `libc::kill` and **asserts** success, because an earlier version that swallowed the result with `let _ =` made the whole suite pass vacuously with all healing disabled. **Server-side replay protection resolves the `Interrupted` ambiguity for the three tools that cannot absorb a duplicate.** `Interrupted` is unavoidable at the client, so it can only be *resolved* at the server: `hkask-mcp-kata-kanban/src/idempotency.rs` stores `(tool, key) → first response` in the kanban database, and `kanban_task_create` / `kanban_board_create` / `kanban_task_spawn` accept an optional `idempotency_key` and return the original response (plus `replayed: true`) instead of re-running. The claim is a single `INSERT OR IGNORE` whose zero-rowcount *is* the already-seen signal, so two concurrent replays cannot both win — no read-then-write race, and no transaction (`dyn DatabaseDriver` exposes none). Reservation is two-phase (`reserve` → work → `record`) because the work happens between: a key claimed but never recorded is `Pending`, reported as unknown rather than silently re-run, since whether it landed is precisely what is unknown; a cleanly-failed call calls `release` so a retry starts fresh instead of inheriting that verdict. Only these three needed it — `task_update`/`task_assign`/`task_delete`/`board_delete` already converge, and `swarm_clone_to_local`/`swarm_push_to_cloud`/`swarm_create_local_agent` are keyed by `agent_name`. `kanban_goal_create` also accepts an `idempotency_key`, but its protection is **process-local by design** (a separate `goal_idempotency: IdempotencyStore::default()` field on `KanbanServer`, never the durable store): goals are ephemeral (in-memory store, operator ruling 2026-08-29), so a durable replay cache would return a stale success — the dead goal's id, `replayed: true` — after a restart, handing the agent a ghost pointer whose next `kanban_goal_judge` fails NotFound. Pinned by `goal_replay_protection_does_not_survive_a_restart` (fresh goal minted across a shared durable DB) + `goal_replay_is_absorbed_within_the_process` (in-process retry absorbed, labelled `idempotency_durable: false`). **`swarm_hire` was already protected** by `spend_gate`'s consume→spend→refund over a single-use `ConsentStore` token (`spend_gate.rs` — `authorize_hire`/`complete_hire`; atomic across processes via `DELETE`-rowcount), so a replayed hire fails closed rather than double-charging; the client-side warning remains because the *outcome* is still unknown, which is a query problem, not a safety one. `kanban_panel` generates one key per operator **gesture** (before the retry loop, not per attempt — per-attempt keys would defeat the mechanism entirely) and widens its retry to cover `Interrupted` for exactly `IDEMPOTENT_TOOLS`. Because the panel injects the key into a raw JSON object, nothing ties the two sides together at compile time and a rename would silently disable protection via `#[serde(default)]` — pinned by `wire_field_name_matches_what_clients_send`. Non-durable degradation is labelled (`idempotency_durable: false`) when the kanban DB is in-memory, per the advertised-invariant rule. Previously pinned by `tests/idempotent_creates.rs` (15, incl. cross-process replay, row-count oracles, and `wire_field_name_matches_what_clients_send`) + `kanban_panel::tests` key-policy tests; the file was deleted with the condenser teardown and **restored** — `tests/idempotent_creates.rs` is live (17 tests as of 2026-08-29, including the two goal-replay pins above).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D5  | Keychain access                                                                         | `kask/crates/hkask-keystore`                                                                                                                                                                                                                                                                                                                         | **Single keychain namespace architecture.** Data-service API keys (Exa, Tavily, Brave, SerpAPI, Firecrawl, FMP, EODHD, FRED, Nebius, HF Token, ABW, etc.) are stored in zed's `CredentialsProvider` keychain namespace under `kask://credentials/<key>` (label `zed-github-account`, attribute `url=kask://credentials/<key>`). **Inference-provider keys (OpenRouter, DeepInfra, RunPod) are the one exception (2026-08-31 consolidation): each lives at its provider's `api_url` keychain slot (`https://openrouter.ai/api/v1`, `https://api.deepinfra.com/v1/openai`, `https://api.runpod.io`) — the same slot zed's `ApiKeyState` reads — resolved by `kask_bridge::credential_url_for_key`; nothing reads or writes `kask://credentials/{openrouter,deepinfra,runpod}` anymore (the former two-slot state was the split-brain behind the 2026-08-31 DeepInfra 401: a stale kask-slot copy fed MCP servers a dead key while the user's fresh key sat unread at `api_url`). `build_mcp_server_env` reads from both slot kinds and injects as env vars into MCP server child processes. The server's `resolve_credential` reads API keys from env vars only — there is NO `service=hkask` keychain fallback for API keys. The legacy `service=hkask` namespace is fully removed and purged at startup (`purge_legacy_hkask_entries`, `hkask-keystore/src/keychain.rs`; implemented via `oo7`, not `keyring`) — do not write anything to it; entries silently vanish. The single internal key is `hkask_db_passphrase` (underscore), living in the same `kask://credentials/` namespace; there is ONE passphrase (`HKASK_DB_PASSPHRASE`) for every SQLCipher DB — no separate swarm-memory passphrase exists. There are NO `*_enabled` settings toggles — the key's presence in the keychain IS the toggle. The `.env` file loading has been fully removed — the keychain is the single source of truth for API keys. The a2a secret threading was removed (self-referential token verification — security theater); the in-process `DelegationToken` that replaced it was itself removed 2026-08-12 (see D3, RR-0056).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D6  | Thread → memory                                                                         | `crates/agent/src/thread.rs` + `crates/agent/src/agent.rs` + `crates/agent/src/kask_thread_state.rs` + `kask/crates/hkask-types` + `kask/crates/kask_bridge/src/memory.rs`                                                                                                                                                     | `MemoryPort` trait in `hkask-types` (carries `agent_id` so the port routes by owning agent). `BridgeMemoryPort` + `RealMemoryPort` in `kask_bridge`. Global hook `agent::set_memory_port()` (Mutex — re-settable), wired in the deferred task at startup (no longer gated on Zed user — uses fallback identity "kask" if user hasn't resolved). Thread turn completion ingests via `cx.background_spawn()`. `Thread` carries `agent_id: Option<AgentId>` (now on `KaskThreadState`, accessed via `thread.kask.agent_id()`) set by `NativeAgent::new_session` when the agent is the Curator. **All turns are ingested** — both curator and zed agent turns are written to the curator's DB. `RealMemoryPort::ingest_turn` branches on `agent_id`: Curator turns go to the curator's sovereign `agents/curator/curator.db` as a curator-perspective h_mem (Private, `curator_webid`) + a shared copy (Shared) for curator recall. Non-curator turns get a shared copy only (no curator-perspective h_mem). `recall_context` and `recall_thread` (the `MemoryPort` trait impls) return empty vecs (the MemoryPort trait impls are no-ops; actual recall is via `recall_context_curator` / `recall_thread_curator`). The curator recalls its own memory via `recall_context_curator` / `recall_thread_curator` (inherent methods on `RealMemoryPort`, not on the `MemoryPort` trait). **The episodic/semantic distinction has been completely removed** — all h_mems are unified; the ontology blob carries dual-axis anchoring (PKO process + DC state) but there is no type distinction. Consolidation is confidence-based cleanup only (delete low-confidence, prune to budget) — no promotion, no re-tagging. **Swarm memory lives in the swarm MCP server, not the bridge.** Local swarm agents execute via `LocalSwarmRuntime::delegate` inside the `hkask-mcp-swarm` process, so their turns never reach the bridge's `Thread`/`ingest_turn` — the bridge cannot observe them. The shared `swarm_memory.db` (one DB for all swarms and all agents — a single knowledgebase swarms share) is written and read entirely in the swarm server: `local_knowledge::ingest_turn` stores each delegation's full turn (task + response + model) as an h_mem under a per-turn entity (`agent:<agent_id>:turn:<uuid>`) plus an embedding of the task, wired into `swarm_delegate_local` + `swarm_execute_plan_local`. `local_knowledge::recall_turns` + the `swarm_recall_local` MCP tool recall prior turns by semantic similarity via the real `InferencePort::embed` (IPC bridge → `LanguageModelEmbeddingPort`); `search_similar` has no agent filter, so a turn any agent produced is retrievable by any other (the shared-knowledgebase property). The bridge-side `SwarmStore`, `recall_context_swarm`, the `swarm_store` field, and `memory_health_json`'s `swarm_store` entry were **deleted** as dead surface. Pinned by `local_knowledge::tests::{ingest_turn_stores_full_turn_retrievable_by_recall, recall_spans_all_agents_shared_knowledgebase, ingest_turn_skips_and_recall_errors_when_store_unavailable, multiple_turns_accumulate_in_shared_knowledgebase}` + the updated `memory_health_json_reports_degraded_without_healing` (curator-only `degraded` now). **Refactored 2026-08-26:** the `agent_id` field and all kask-specific per-thread state moved from `Thread` to `KaskThreadState` (`crates/agent/src/kask_thread_state.rs`). The turn-loop call sites use `self.kask.agent_id()` / `self.kask.check_tool_retry()` etc. (The refactor plan doc `kask/docs/plans/thread-hooks-refactor-2026-08-26.md` has since been removed; this entry is the record.) **Curator memory-edit tool gating removed 2026-09-01:** the `is_curator_memory_edit_tool`/`is_curator_thread` gate that restricted `memory_insert`/`memory_update`/`memory_resolve_contradiction` to curator threads was removed by operator decision — live evidence showed models cannot reliably emit calls to functions absent from their visible list (4/4 announce-then-stop on GLM 5.3), so the gate produced silent no-effect stops and blocked operator-approved memory writes. `is_curator_memory_edit_tool`, `is_curator_thread`, and the Pin B6 classifier test were deleted; the D44 marker text, `LoopFailureDistinctions` #4/#7, the curator server tool descriptions, and `memory-system-specification.md` §9/§10 were updated in the same pass. Pinned by `test_curator_memory_edit_tools_available_to_non_curator_threads` (`crates/agent/src/tests/mod.rs`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D7  | App-identity                                                                            | `crates/paths/src/paths.rs`, `crates/cli/src/main.rs`, `crates/release_channel/src/lib.rs`, `crates/zed/src/zed/mac_only_instance.rs`, `crates/title_bar/src/title_bar.rs` + `crates/title_bar/src/update_version.rs` (the auto_update removal sites — static version display, updates via the terminal script), `crates/zed/Cargo.toml`, `kask/scripts/build/install.sh`, `kask/scripts/build/install-binary.sh`, `kask/scripts/build/update-zed-kask.sh`, `script/bundle-linux`, `script/bundle-mac`, `script/bundle-windows.ps1`, `script/snap-build`, `nix/build.nix`, `nix/modules/packages.nix`, `flake.nix` | `APP_NAME`→`Zed-Kask`, `app_identifier`→`Zed-Kask-Editor`, `app_id`→`dev.zed-kask.Zed-Kask`, `display_name`→`Zed-Kask`, port offset +500, binary `zed-kask`, remote dirs `.zed-kask_server` / `.zed-kask_wsl_server`, bundle IDs `dev.zed-kask.*`, URL scheme `zed-kask://`. **Flatpak runtime code removed (2026-08-27):** upstream's CLI `mod flatpak` (sandbox escape via `flatpak-spawn --host`, hard-coding upstream Zed's `FLATPAK_ID=dev.zed.Zed` and binary layout) and the `FLATPAK_XDG_{CONFIG,DATA,STATE,CACHE}_HOME` overrides in `paths.rs` are deleted — zed-kask is not distributed as a flatpak, and honoring upstream Zed's sandbox env vars would launch upstream Zed's editor / point zed-kask at upstream Zed's data dirs when the CLI runs inside upstream Zed's sandbox. Pinned by `check-zed-isolation.sh` (`mod flatpak|flatpak-spawn|FLATPAK_ID` in `crates/cli/src/main.rs`, `FLATPAK_XDG_` in `crates/paths/src/paths.rs`). **Hard Zed-isolation invariant:** zed-kask is installed and updated only through `kask/scripts/build/`; it may write only `zed-kask`, `hkask-mcp-*`, zed-kask config/data, and zed-kask icons. Upstream Zed's updater is not initialized or imported by the zed binary. The `Update Zed-Kask` menu action launches only the installed `share/zed-kask/install/update-zed-kask.sh` in a visible terminal; that wrapper locks updates, derives its prefix from its own location, and invokes the checksum-verified flat-binary installer. Legacy Zed bundlers for Linux, macOS, Windows, and Snap are fail-closed; upstream release workflows, the Zed desktop template, and Flatpak/Snap resources are deleted; macOS `[package.metadata.bundle-*]` sections (including `osx_url_schemes = ["zed"]`) are removed — zed-kask is Linux-only and does not bundle for macOS. The nix derivation (`nix/build.nix`) is build-only (consumed by the devshell for dev tooling); the installPhase produces `zed-kask` (not `zed`), installs no icons or `.desktop` files, and `nix/modules/packages.nix` exports no installable packages. `flake.nix` carries zed-kask identity (description, no `zed.cachix.org`). Installer destinations are canonicalized and rejected if they resolve inside Zed's app, data, config, command, or launcher paths. Enforced dynamically at the installer mutation boundary and by `kask/scripts/build/check-zed-isolation.sh` (CI: the `Kask invariants` workflow, `.github/workflows/kask-invariants.yml`). Residual: the script's pattern checks are grep-based — they pin the exact upstream identifiers (`FLATPAK_XDG_`, `mod flatpak`, workflow and crate names), so a reintroduction under a renamed identifier would slip past them; the crate-absence and fail-closed-bundler checks are structural and rename-proof. **Workstation isolation (added 2026-09-01, from the live incident):** an upstream Flatpak Zed window opened on this workspace ran its rust-analyzer at 5.7 GB RSS / 263% CPU for 5 hours (`cargo check --workspace` over ~450 crates) — the project-level `lsp` disable alone did not stop it. The repo's `.zed/settings.json` now disables rust-analyzer in BOTH forms (the `lsp` section AND the Rust `language_servers: ["!rust-analyzer"]` exclusion — older Zed builds honor only one), pinned by `check-zed-isolation.sh`. **Operator rules:** develop zed-kask in zed-kask (RA is intentionally disabled in-project — the workspace is too large for live indexing); do not open this repo in upstream Zed; never grant upstream Zed's agent write access to zed-kask's data/config dirs — a folder is a folder, and no install-side isolation can stop an editor you point at the repo from indexing it. |
| D8  | Bridge + adapters                                                                       | `kask/crates/kask_bridge/` (incl. `src/inference_socket.rs`, `src/condenser_bridge.rs`) + `crates/agent/src/agent.rs` (`ThreadCondenser`) + `crates/agent/src/thread.rs` (manual compaction precompression) + `crates/zed/src/main.rs` (condenser wiring)                                                                                                                                                                                                                                                                                       | **Manual compaction precompression (`46f9c476a2`):** `ThreadCondenser::precompress_history` runs on a background copy of the manual summarizer's history before the existing LLM call; it excludes the final summarization instruction. `BridgeThreadCondenser` routes eligible older tool output through the existing algorithms, preserving prose, the latest user-led exchange, protected tools, failed results, JSON, images and reasoning metadata. Only nonempty, smaller excerpts replace text. The hook is installed even when ingestion compression is off; that setting still controls only incoming tool-result compression. Automatic compaction, output allowances, stored originals, Summary persistence and native cancellation/error handling remain unchanged. No token-fit guarantee: protected content can still exceed the provider limit. Pins: `test_manual_compact_forces_summary` (observed red before wiring) and `history_precompression_preserves_protected_content_and_routes_algorithms`. Additional regression coverage (uncommitted): automatic compaction bypasses the hook; `test_manual_compact_precompression_failure_leaves_history_unchanged` asserts surfaced failure, no model dispatch and unchanged history. Live synthetic bridge output shrank from 1,440,720 to 160,157 bytes; OpenRouter accepted the latter at 80,119 input tokens. The uncompressed probe timed out at 60 seconds, so this is not a paired rejection/success proof. **THE bidirectional seam** — implements all ports over zed-kask facilities. `InferencePort` over `LanguageModel`, `LanguageModelEmbeddingPort` (resolves credentials from `INFERENCE_PROVIDERS` + env var, no `LanguageModelRegistry` lookup), `resolve_model_names` (generic provider-prefixed model-name resolution), `BridgeMemoryPort`, `BridgeContextInjector`, `BridgeCuratorContextInjector` (curator-scoped recall via `RealMemoryPort::recall_context_curator`), `BridgeThreadCondenser`, `BridgeMetacognitionProvider`, `InferenceIpcServer`, `KaskSettings`. Channel pattern solves GPUI/tokio `Send`+`Sync` boundary. `LanguageModelInferencePort` holds **two** mpsc channels: `tx` (non-streaming `InferenceRequest` → `InferenceResult`) and `stream_tx` (streaming `StreamInferenceRequest` → `InferenceStreamChunk` stream). The GPUI-side receiver task polls both via `tokio::select!`. The shared `collect_completion` collects non-streaming events into a single `InferenceResult` via `StreamAccumulator`; its streaming mode forwards `Text`/`Thinking` events immediately as `InferenceStreamChunk`s (live thinking-trace for skill execution) and accumulates only metadata (`ToolUse`, `Stop`, `UsageUpdate`) into a final chunk. `StreamAccumulator` is the shared event-processing struct used by both paths. The `generate_stream` trait override uses `futures_util::stream::unfold` to convert the tokio mpsc receiver into a futures stream (no `tokio-stream` dependency). `generate_stream` does **not** support `model_override` (hardcoded to `None`) — the since-removed skill-cascade executor's `call_inference_stream` called `generate_stream` without an override; `generate_stream_with_model` (added 2026-08) threads `model_override` through `StreamInferenceRequest.model_override` to `collect_completion`, which calls `resolve_model` with the override — so streaming + override preserves the live trace (the prior claim that it fell back to non-streaming `generate_with_model` was stale). The inference IPC protocol carries one MCP-server-to-zed dispatch method: `tool_invoke` (governed `McpRuntime` via the injected `ToolPort`). The parent-held grant intersected with per-request `tool_allowlist` is the authority boundary for delegated dispatch (fail-closed before dispatch). A child cannot authorize itself by supplying its own list — pinned by `ipc_child_cannot_expand_parent_grant` and the D8/D9 launch tests above. The call meter is **fail-open on an unregistered agent** (RR-0057): it auto-registers at `DEFAULT_RUNAWAY_CALL_CEILING` and logs the wiring gap rather than refusing, because a missing seed is a wiring omission, not an authorization decision. The prior fail-closed cap silently broke delegated paths — `main.rs` seeded only the `swarm-panel` persona while this dispatch mints `kask-panel`, which derive different WebIDs, so every IPC tool call was refused with `EnergyBudgetExceeded` while this row claimed the seed made them "succeed while remaining bounded." The one pre-dispatch refusal is an exhausted per-tick ceiling (runaway-loop breaker; resets each regulation tick). **Inference IPC socket path management:** `kask_bridge::inference_socket` (a re-settable `Mutex<String>`) owns the socket path state, replacing a local `OnceLock<String>` in `main.rs` that could not be updated when the IPC server restarted (e.g. when the real model resolved after the no-op fallback). `main.rs` calls `kask_bridge::set_inference_socket_path` after `InferenceIpcServer::start` succeeds, and `kask_bridge::get_inference_socket_path` when building MCP server env maps via `build_mcp_server_env`. The `OnceLock`'s `set()` was write-once — a second IPC server start (model resolved after no-op fallback) silently dropped the new socket path, leaving MCP servers with a stale or empty `HKASK_INFERENCE_SOCKET` env var and all inference calls failing with "IPC bridge not configured." **Both MCP server paths re-synced on socket set:** after `set_inference_socket_path`, `main.rs` calls both `sync_kask_mcp_servers` (ContextServerStore path) AND `sync_kask_mcp_runtime_servers` (governed McpRuntime path). Previously only the ContextServerStore path was re-synced, so governed McpRuntime servers kept their stale env (no socket) and all skill execution / panel inference calls failed even though the socket was live. **Model-resolved re-wire (2026-08-30):** the deferred task's inference-stack wiring is extracted into `wire_kask_inference_stack` (`main.rs`) and, when the registry has no default model at deferred-task time, retried by a `LanguageModelRegistry` event subscription that latches on success — previously the wiring was one-shot, so a model resolving after startup left the no-op IPC server in place for the whole session (sensed as `InferenceModelAvailable=0.0`, escalated as `model_unavailable`). `try_wire_edit_prediction_port`'s AtomicBool latch was fixed the same way (latch on success, not on attempt — a failed attempt previously left edit predictions unwired for the session). |
| D9  | Settings + credentials                                                                  | `kask/crates/kask_bridge/src/settings.rs` + `crates/settings_content/src/settings_content.rs` + `crates/settings_ui/src/pages/kask_page.rs` + `crates/settings_ui/src/page_data.rs`                                                                                                                                                                  | `KaskSettings` struct + `"kask"` section in settings.json. **Keychain locations (one key, one location; see D5):** data-service API keys (Exa, Tavily, Brave, SerpAPI, Firecrawl, FMP, EODHD, FRED, Nebius, HF Token, ABW, etc.) are stored in zed's `CredentialsProvider` keychain under `kask://credentials/<key>` (label `zed-github-account`, attribute `url=kask://credentials/<key>`); inference-provider keys (OpenRouter, DeepInfra, RunPod) live at their provider `api_url` slots. There are NO `*_enabled` settings toggles — the key's presence in the keychain IS the toggle. The `KaskDataServiceSettings` struct and its `*_enabled` bool fields have been deleted (2026-08-24); `credential_urls_for_mcp()` emits all credential URLs unconditionally. The legacy `service=hkask` namespace is fully removed and purged at startup (`hkask-keystore/src/keychain.rs`); the single internal key `hkask_db_passphrase` lives in the same `kask://credentials/` namespace. `resolve_credential` reads API keys from env vars only (no keychain fallback); `HKASK_DB_PASSPHRASE` resolves via the canonical 2-tier helper (ctx.credentials → env → `kask://credentials/hkask_db_passphrase`). There is no `HKASK_SWARM_MEMORY_PASSPHRASE` — one passphrase covers every SQLCipher DB. **`nudge_mcp_servers` restart trigger:** after `write_credential` / `delete_credential` writes a keychain URL to the keychain, it calls `nudge_mcp_servers(cx)` which calls `SettingsStore::notify_observers(cx)` — a direct `Effect::NotifyGlobalObservers` push via `global_mut` that bypasses the file-write path entirely. This fires the `SettingsStore` observer → `sync_kask_mcp_runtime_servers` → `build_mcp_server_env` (re-reads keychain) → restart. The trigger predicate is `credential_url_feeds_mcp_servers`: it fires for every URL in `credential_urls_for_mcp()`'s set — both `kask://credentials/<key>` data-service slots and the inference providers' `api_url` slots (a key rotated via Settings → Kask → Data Services or written by a provider's own authenticate flow reaches the running MCP servers without a restart). The startup mirror (`mirror_kask_credentials_to_providers` / `mirror_credential_to_provider`) is deleted (2026-08-31) — see D5 for the one-key-one-location consolidation. **`permission_denied` classification standard:** missing credentials must surface as `McpToolError::permission_denied` with the env var named. **Canonical `HKASK_DB_PASSPHRASE` resolution helper:** `hkask_mcp_server::server::resolve_db_passphrase(&ctx.credentials)` — 2-tier chain (ctx.credentials → `resolve_credential` which does env → `hkask-keystore` keychain `hkask-db-passphrase`). **First-run provisioning (one chain, 2026-08-30):** `hkask_keystore::keychain::provision_db_passphrase_string` owns the whole chain (env → keychain → first-run default `"allostery"`, with an empty/whitespace-only env value treated as unset — the guard the bridge's old inline chain carried, moved into the chain and pinned by `empty_env_passphrase_falls_through_to_keychain`); `kask_bridge::identity::{provision_agent, provision_db_passphrase}` are thin adapters over it (no inline passphrase logic remains in the bridge); called at governed MCP server launch (`kask/crates/kask_bridge/src/mcp_servers.rs`, inside `provision_default_passphrase`); `build_mcp_server_env` then mirrors the provisioned passphrase into zed's keychain via `write_credentials` so the `ctx.credentials` tier of the 2-tier chain resolves it. Pinned by `provision_stores_default_when_absent_and_is_stable_after` (live-keychain, snapshot/restore).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| D11 | `time::format_description::parse` deprecation allow                                     | `crates/git_ui/src/git_graph.rs`                                                                                                                                                                                                                                                                                                                     | Upstream `git_graph.rs` calls `time::format_description::parse`, which is `#[deprecated]` in `time 0.3.54+` (the version the workspace resolves to; lower versions break `plist`/`project`). Two call sites (`timestamp_format`, the commit-date formatter) carry `#[allow(deprecated)]` with a `// zed-kask:` pointer to this seam. Remove this seam when upstream migrates to `parse_borrowed`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D12 | OpenAI/Anthropic-compatible env var name                                                | `crates/language_models/src/provider/api_compatible.rs`                                                                                                                                                                                                                                                                                              | Upstream computes the API-key env var name as `format!("{}_API_KEY", id).to_case(Case::UpperSnake)`, which splits multi-word provider ids on case boundaries (e.g. `SomeProvider` → `SOME_PROVIDER_API_KEY`) and leaves `fal.ai` as an invalid env var name (`FAL.AI_API_KEY`). The entire kask ecosystem (MCP servers, keystore, UI text, docs) uses the concatenated alphanumeric form (`OPENROUTER_API_KEY`, `FALAI_API_KEY`), so the upstream computation never matches the env vars kask users set. The `// zed-kask:` block in `ApiCompatibleProviderState::new` strips non-alphanumerics and uppercases instead of using `convert_case`. `convert_case` was removed from `crates/language_models/Cargo.toml` (still used by other crates, so the workspace dep stays). Pinned by `test_api_key_env_var_name_kask_contract` in `api_compatible.rs`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D13 | OpenRouter output budget                                                                | `crates/open_router/src/open_router.rs` + `crates/language_models/src/provider/open_router.rs` + `crates/open_router/Cargo.toml` + `crates/language_model_core/src/request.rs` + `kask/crates/kask_bridge/src/inference_chat.rs`                                                                                                                                                                                                                     | Upstream omits `max_tokens` from completion requests entirely (`Model::max_output_tokens()` hardcodes `None`). OpenRouter then reserves the model's full default output size (e.g. 64k for claude-haiku-4.5, 128k for glm-5.2) against the key's credit limit before dispatching, and rejects with 402 on keys whose remaining limit can't cover the reservation — even for a one-line prompt. Zed-kask parses `top_provider.max_completion_tokens` from the `/models` and `/models/user` responses into a new `Model::max_output_tokens` field, which flows into the request as an explicit `max_tokens` budget. Settings-defined `available_models[].max_output_tokens` (already in `OpenRouterAvailableModel` upstream, previously unwired) overrides the API-derived value. The API-derived value is capped at half the model's `context_length`: some providers (e.g. GLM 5.2) advertise `max_completion_tokens == context_length` (1048576), which is mathematically impossible (input + output can never exceed the context window), so sending it raw made OpenRouter reject every non-empty request with 400 "maximum context length exceeded" and broke the compaction math (`max_input_tokens = context - max_output_tokens` collapsed to 0, triggering auto-compact on every turn). The half-context cap only affects broken models; every sane model advertises a cap already below 50% of its context. Zed-kask also adds a per-request `max_tokens: Option<u64>` field to `LanguageModelRequest` (`language_model_core/src/request.rs`); `into_open_router` prefers `request.max_tokens` over the model default when set. (Historical note: `kask_bridge::LanguageModelInferencePort::build_request` previously set this from a since-removed `LLMParameters.max_tokens` field for the skill-cascade executor; the cascade teardown removed that wiring, and `build_request` now leaves `max_tokens: None` — the model-default budget from `Model::max_output_tokens` covers every current kask inference call. No kask caller sets `request.max_tokens` today; the per-request override mechanism remains wired and pinned for future use.) Pinned by `test_max_completion_tokens_from_api_becomes_request_budget`, `test_max_output_tokens_capped_at_half_context_length`, and `test_per_request_max_tokens_overrides_model_default`. **2026-09-10 merge:** retains the output cap and per-request precedence while carrying upstream reasoning metadata and the ten-argument model constructor; the fork retains `reasoning_effort` per `5269cf00ca`. Output reservation limits remain distinct from D42 reasoning budgets. |
| D14 | Streaming text reveal timer interval                                                    | `crates/acp_thread/src/acp_thread.rs`                                                                                                                                                                                                                                                                                                                | Upstream hardcodes `StreamingTextBuffer::TASK_UPDATE_MS = 16` (60fps). During active streaming, this fires a `Markdown::append` + `cx.notify()` on the foreground thread 60 times per second, saturating the render loop and starving the `BlinkManager`'s 500ms timer — the cursor flashes irregularly/rapidly. Zed-kask increases it to 50ms (20fps): still smooth for text reveal (the human eye can't perceive text appearing at >20fps) but 3x less render pressure. Pinned by `test_streaming_reveal_timer_interval_kask_contract`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| D15 | Bounded cursor-blink timers                                                             | `crates/editor/src/blink_manager.rs`                                                                                                                                                                                                                                                                                                                 | Two upstream timer-accumulation vectors make the cursor strobe at (N+1)× the normal rate. (1) `pause_blinking` detaches a new 500ms resume task on every selection change — rapid typing accumulates uncancellable foreground wake-ups, and blur leaves pending blink/resume callbacks valid across a later refocus. Zed-kask stores one restartable resume task, keeps `blinking_paused` consistent with the actual state, and invalidates pending callbacks on disable. (2) The `SettingsStore` observer unconditionally calls `blink_cursors` on every settings update, and `blink_cursors` spawns a fresh 500ms timer on top of any already running — each unrelated settings change (profile switch, model selection, settings.json save) stacks another blink cycle. Zed-kask only (re)starts blinking on the disabled→enabled transition. Pinned by `test_pause_blinking_restarts_single_resume_deadline`, `test_disable_cancels_pending_resume`, and `test_settings_updates_do_not_accumulate_blink_timers`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D16 | App menu rename + safe zed-kask updater                                                 | `crates/zed/src/zed.rs` + `crates/zed/src/zed/app_menus.rs`                                                                                                                                                                                                                                                                                          | Leftmost app menu `name` changed from `"Zed"` to `"z-k"`. On macOS the platform overrides this title with the bundle name ("Zed-Kask" via D7); the rename only takes effect on the cross-platform title-bar `ApplicationMenu` (Linux/Windows, `crates/title_bar/src/application_menu.rs`). The upstream update actions remain removed because upstream Zed's installer writes `zed*.app` into `~/.local` and can replace the user's real Zed. `Update Zed-Kask` is a distinct `RunZedKaskUpdate` action: it resolves only the installed zed-kask updater relative to the running executable and launches it in a visible terminal task. It has no `auto_update` dependency or call path. Pinned by `test_leftmost_menu_name_is_zk`, `test_leftmost_menu_has_zed_kask_update_item`, and `kask/scripts/build/check-zed-isolation.sh`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| D18 | Graph + kanban + portfolio + scenarios block renderer (viz widgets in markdown) | `crates/markdown/src/markdown.rs` + `crates/agent_ui/src/conversation_view.rs` + `crates/hkask-viz-core/` + `crates/hkask-graph-widget/` + `crates/hkask-kanban-widget/` + `crates/hkask-portfolio-widget/` + `crates/hkask-scenarios-widget/`                                                                        | New `media_block_renderer: Option<MediaBlockRendererFn>` field on `MarkdownElement` (an optional callback that intercepts fenced code blocks before the default code block renderer). Called for every fenced code block — returns `Some(div)` to render a viz widget (the event-tree DAG for `graph`, kanban board columns for `kanban`, portfolio dashboard for `portfolio`, scenario pipeline/matrix/timeline for `scenarios`), or `None` to fall through to default code block rendering. The callback type `MediaBlockRendererFn` and the `.media_block_renderer()` builder method are the only upstream additions. The widget implementations live in `crates/hkask-graph-widget/`, `crates/hkask-kanban-widget/`, `crates/hkask-portfolio-widget/`, and `crates/hkask-scenarios-widget/` (zed-kask-side — they render GPUI elements, so they depend on `gpui`/`theme` and live outside `kask/` to preserve the §13.1 invariant that hKask crates never depend on zed-kask crates). Wired in `render_agent_markdown` (`conversation_view.rs`) via `.media_block_renderer(hkask_viz_core::block_renderer())` — the `hkask-viz-core` registry composes all widget renderers into one callback so the upstream D18 field/builder/dispatch stay unchanged. The kanban/portfolio/scenarios widgets are inline chat-stream renderers for their respective fenced blocks. **rJoule/spend-log removal (2026-09-08, operator budget-deprecation ruling):** the kanban widget's `TaskBody.spend_log` field and `SpendEntryBody` type are deleted, along with the spend-log rendering in `view.rs`'s detail popover and the `spend_log: Vec::new()` construction site in `crates/kanban_panel/src/kanban_panel.rs` (the panel builds `TaskBody` from fetched `TaskInfo`; the server no longer emits the field — kata-kanban tool/teardown in `kask/mcp-servers/hkask-mcp-kata-kanban`, decision record in its `tests/rjoule_removal_pin.rs`). The field was serde-optional, so older persisted blocks parse unchanged. Pinned by `selects_event_tree_body` / `falls_through_non_graph_bodies` in `hkask-graph-widget`, the layout tests in `hkask-graph-widget/src/layout.rs`, the widget's updated `parses_full_task_detail_fields` / `full_detail_fields_default_empty_when_absent` (no spend_log), and `rjoule_removal_pin.rs` (source-tree sweep). |
| D20 | Observed per-call USD cost in `TokenUsage`                                              | `crates/language_model_core/src/language_model_core.rs` + `crates/language_model_core/src/chat_completion.rs` + `crates/open_ai/src/open_ai.rs` + `crates/open_ai/src/completion.rs` + `crates/copilot_chat/src/model.rs` + `kask/crates/kask_bridge/src/inference_chat.rs`                                                                                                                                     | Upstream's `TokenUsage` carries only token counts; the provider's reported USD cost (`usage.cost` / `estimated_cost` / `market_cost`) is discarded, so the IPC-bridge path (the primary inference route — the bridge sees only `LanguageModelCompletionEvent::UsageUpdate(TokenUsage)`) cannot observe the per-call USD cost. Zed-kask adds a `cost: Option<f64>` field to `TokenUsage` (serde-optional, backward-compatible). The shared Chat Completions `Usage::token_usage` parses `usage.cost`/`estimated_cost`/`market_cost` into it (precedence: `market_cost`, then `cost`, then `estimated_cost`; missing cost stays `None`), and `kask_bridge::LanguageModelInferencePort` reads `TokenUsage.cost` from the `UsageUpdate` event into `InferenceResult.cost_usd`. `Add`/`Sub` for `TokenUsage` sum/subtract `cost` for cumulative accounting. After the 2026-09-10 shared-mapper migration, all Chat Completions consumers, including native OpenRouter, llama.cpp and LM Studio, preserve cost when reported and emit `None` when absent. The OpenAI Responses API still sets `cost: None`; this migration does not add cost reporting to other wire protocols. **`crates/copilot_chat/src/model.rs`** was added by upstream's `6943d7362e` (Copilot Chat cleanup — moved `CopilotChatLanguageModel` out of `language_models/src/provider/copilot_chat.rs` into the `copilot_chat` crate); the three `TokenUsage` construction sites in `map_events` carry `// zed-kask: D20` markers and set `cost: None` (Copilot doesn't report USD cost). Pinned by `test_token_usage_cost_round_trips` + `test_token_usage_cost_adds_and_subs` in `language_model_core`, `test_map_event_populates_cost_from_usage` in `language_model_core::chat_completion` (wire decoding, cache splitting, cost precedence, zero and absent cost), and `responses_stream_usage_carries_no_cost`, `chat_completion_usage_carries_no_cost`, and `incomplete_stream_usage_carries_no_cost` in `copilot_chat::model` (one per D20 construction site).                                                                                                               |
| D21 | Widget→agent compose-back seam (the "I disagree" gesture)                               | `crates/agent_ui/src/conversation_view.rs` + `crates/hkask-conversation-injector/`                                                                                                                                                                                                                                                                   | Kask viz widgets (D18) are leaves with no access to the active agent conversation thread, so they cannot compose a structured message back into it (a provenance-scoped revision request / "I disagree"). The `hkask-conversation-injector` leaf crate hosts the `ConversationInjector` trait + a process-global accessor (`set_active_injector` / `shared_injector`), mirroring the `hkask-tool-invoker` (widget→MCP) seam but for widget→agent. The production impl `ThreadConversationInjector` (in `agent_ui`, the D-seam) holds the active `Entity<ThreadView>` and pre-fills its message editor with the composed body via `MessageEditor::clear` + `insert_text` (both production-available; `set_text` is test-gated); the user reviews and submits via the existing Send button so the turn-loop's checkpoints/telemetry are preserved (auto-send would bypass them, and `ThreadView::send`/`send_content` are deliberately untouched). `ConversationView::publish_injector` reads `active_thread()` and publishes/clears the global; it is called from the active-thread-change points `navigate_to_thread` and `set_server_state` (covers connect + disconnect). The global is re-settable (Mutex, not OnceLock) so each activation replaces the prior. No cross-thread channel is needed — `inject` is called from a foreground widget `on_click` (`window` + `cx` passed in). The trait + accessor live in the leaf crate so kask widgets can compose back without depending on the heavy `agent_ui` crate (§13.1 invariant preserved). The `render_agent_markdown` block renderer is untouched. Pinned by `tests::publish_injector_wires_global_on_activation_and_clears_on_disconnect` in `conversation_view.rs`, and the `hkask-conversation-injector` leaf tests (`compose_back_via_injector_returns_some_on_no_injector`, `compose_back_via_injector_returns_none_on_successful_inject`, `compose_back_via_injector_surfaces_draft_on_inject_error`).                                                                           |
| D22 | Block-reachability pins in `main.rs`                                                    | `crates/zed/src/main.rs`                                                                                                                                                                                                                                                                                                                             | Two block-reachability pins in `main.rs` used constructs that do not compile under `--deny warnings`. (1) The F4 pin used `std::any::TypeId::of::<hkask_regulation::DEFAULT_VARIETY_MAX_DEFICIT>()`, which treats an `f64` const as a type — replaced with a plain const read (`let _ = hkask_regulation::DEFAULT_VARIETY_MAX_DEFICIT;`). (2) The F23 pin used `std::ptr::addr_of!(kask_server_env)`, which takes the address of a temporary (a function name is not an lvalue) — replaced with a plain name reference (`let _ = kask_server_env;`), which evaluates to a function pointer and pins the symbol. Both fixes are isolated to the pin lines; the other block-reachability pins are upstream `TypeId::of::<Type>()` calls and are untouched. Note: `kask_wiring_symbols_exist` (which carries both pins) only runs under `--features test-support` — a plain `cargo test -p zed` executes none of it. No behavioral change — the pins are compile-time reachability guards, not runtime paths.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D23 | `AgentPanelSiblingHost` visibility + worktree spawn wiring                              | `crates/agent_ui/src/agent_panel.rs` + `crates/agent_ui/src/agent_ui.rs` + `crates/zed/src/main.rs`                                                                                                                                                                                                                                                  | `AgentPanelSiblingHost` (was `pub(crate)`) is now `pub` and re-exported from `agent_ui` so `kask_bridge` can use it via the `WorktreeSpawner` trait. `main.rs` adds `AgentPanelWorktreeSpawner` (impl `WorktreeSpawner`) + a `cx.observe_new` on `AgentPanel` that sets the process-global `WORKTREE_SPAWNER` when a panel is created and clears it when dropped. The `InferenceIpcServer` GPUI-side task reads the global to dispatch `CreateWorktreeThread` IPC requests → `AgentPanelSiblingHost::create_sibling_thread` with `use_new_worktree: true`. The `kanban_task_spawn` MCP tool tries worktree spawn first, falls back to in-memory `LazyLocalSwarmRuntime`. No upstream behavioral change — the visibility widening is additive (the struct was already used cross-crate within `agent_ui`; making it `pub` extends that to `kask_bridge`). Pinned by the `kask_bridge` IPC dispatch tests (`dispatch_generate_returns_canned_result`, etc.) which pass `None` for the worktree spawn channel.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D24 | Edit predictions via `LanguageModelRegistry` (glm-5.2)                                 | `crates/edit_prediction/src/open_ai_compatible.rs` + `crates/edit_prediction/src/edit_prediction_tests.rs` + `crates/language_models/src/provider/open_router.rs` + `crates/zed/src/main.rs` + `kask/crates/kask_bridge/src/inference_edit_prediction.rs` + `assets/settings/default.json`                                                                                                                               | zed-kask routes edit predictions through the `LanguageModelRegistry` using `DEFAULT_FALLBACK_MODEL` (`OpenRouter/z-ai/glm-5.2`), collapsing the edit-prediction model onto the same model + credentials the agent uses, instead of Zed's cloud Zeta endpoint (the 429-rate-limited default). Three edits: (1) `OpenRouterLanguageModel` gains `api_url()`/`api_key()` trait overrides (returning the OpenRouter base URL + API key from provider state) — the trait default returned `None`, forcing callers to resolve credentials from env vars instead of the registry; the kask embedding port already uses this pattern. (2) `open_ai_compatible.rs` gains a process-global `Mutex<Option<Arc<dyn KaskCompletionPort>>>` hook (`set_kask_completion_port`); when wired, `send_custom_server_request` delegates to the port instead of making the HTTP call. (3) `kask_bridge::BridgeEditPredictionPort` implements `KaskCompletionPort` — at construction it resolves `DEFAULT_FALLBACK_MODEL` from the registry via `resolve_model_names`, extracts `api_url()`/`api_key()`, strips the provider prefix, and makes raw `/completions` POSTs via `HttpClient` (same pattern as `LanguageModelEmbeddingPort` but `/completions` instead of `/embeddings`). Wired in `main.rs` from the model-dependent task (fires when the registry resolves a model). `default.json` changes `edit_predictions.provider` from `"zed"` to `"open_ai_compatible_api"` with `prompt_format: "glm"` and `model: "z-ai/glm-5.2"` — the `Glm` FIM format (`fim.rs:189`) formats as bare `{prefix}`, and the `edit_prediction_provider_config_for_settings` router sends `OpenAiCompatibleApi` + non-Zeta format to the `Fim` model path, which calls `send_custom_server_request` (intercepted by the kask port). The `api_url`/`model` in settings are fallbacks for the pre-wiring window. When the port is `None` (before the model resolves), `send_custom_server_request` falls through to the existing HTTP path (upstream behavior). Pinned by `test_kask_completion_port_intercepts_send_custom_server_request` in `open_ai_compatible.rs`. **Test provider override:** `default.json`'s `provider: "open_ai_compatible_api"` changes what `test_settings()` resolves, breaking 21 `edit_prediction_tests` — the test `FakeHttpClient` handlers only serve the Zeta cloud endpoints (`/predict_edits/v3`, `/predict_edits/v4`), but with `OpenAiCompatibleApi` the request routes through `send_custom_server_request` to an unhandled URL, causing deadlocks or 404s. `override_edit_prediction_provider_to_zed` in `edit_prediction_tests.rs` resets the provider to `Zed` in `init_test` + `init_test_with_fake_client_and_legacy_data_collection`, restoring the upstream test path. Tests that need `OpenAiCompatibleApi` (`make_sweep_prompt_test_ep_store`, `test_unauthenticated_without_custom_url_blocks_prediction_impl`) already override the provider explicitly. |
| D25 | Chat Completions `finish_reason: "length"` → `MaxTokens` | `crates/language_model_core/src/chat_completion.rs` + `crates/agent/src/kask_thread_state.rs` | Originally implemented in the OpenAI and OpenRouter event mappers because their catch-all mapped output truncation to `EndTurn`. The 2026-09-10 merge adopts upstream's shared `ChatCompletionEventMapper`, whose explicit length arm already preserves `StopReason::MaxTokens`; the old provider mappers are removed, not kept as unused compatibility code. D36 must not drain length-truncated tool input as complete. Pinned by `stream_maps_length_finish_reason_to_max_tokens_stop` and `length_with_pending_tool_calls_remains_truncated` in shared core. The downstream `last_completion_truncated` flag on `KaskThreadState` remains set on MaxTokens, read when flushing pending tool input, and reset per completion; its existing pin is `test_last_completion_truncated_distinguishes_max_tokens_from_cancel`. |
| D26 | Tool-use warnings in system prompt template | `crates/agent/src/templates/system_prompt.hbs` + `kask/crates/kask_bridge/src/context_injector.rs` | The upstream Zed agent harness prompt carries general tool-use guidance ("Do not waste tokens by re-reading files...", "send a brief preamble...", a 3-strikes loop rule), and the built-in tools (`read_file`, `edit_file`, `terminal`) are upstream code with known failure modes (`read_file` returning "tool input was not fully received", `edit_file` `old_text` mismatch loops, opaque `terminal` commands). The kask-specific failure-mode warnings (`read_file` re-read-after-success and "tool input was not fully received" fallback to `terminal`; `edit_file` surgical edits and one-retry-on-mismatch; `terminal` preamble requirement and `timeout_ms`) are rendered as an unconditional `## Tool failure-mode warnings (kask)` section in `system_prompt.hbs`. **Refactored 2026-08-25:** previously delivered via `BridgeContextInjector::inject_static_context` (a runtime injection returning `Some(TOOL_WARNING_PROMPT)`); now baked into the template directly. The `inject_static_context` method, `Thread.static_context` field, and `static_context_loaded` flag were deleted. Thread-scoped memory recall was folded into the per-turn `inject_context` path so memory is fresh at decision time. Pinned by `test_system_prompt_contains_tool_failure_mode_warnings` in `templates.rs`. |
| D27 | Sandboxed terminal non-interactive shell                                                 | `crates/acp_thread/src/acp_thread.rs`                                                                                                                                                                                                                                                                                                                | Upstream's `AcpThread::create_terminal` only calls `ShellBuilder::non_interactive()` when `HeadlessTerminal::is_enabled(cx)` is true (eval CLI only). In the GUI, `HeadlessTerminal` is not set, so the sandboxed shell is launched with `-i` (interactive mode). But the sandboxed command has stdin redirected to `/dev/null` (`redirect_stdin_to_dev_null`) and runs in a new session (`setsid()` in the `pre_exec` hook) with no controlling terminal — bwrap's user namespace severs the PTY's controlling-terminal assignment. When an interactive shell (`/bin/sh -i`) starts without a controlling terminal, it tries `tcsetpgrp()`, fails, prints `"Cannot set tty process group (No such process)"` to stderr, and exits with code 2 — even though the actual command ran fine. This made **every** sandboxed terminal command appear to fail (346 instances in a single agent thread), with agents seeing exit code 2 and the tty warning appended to every command output. Zed-kask adds `|| sandbox_wrap.is_some()` to the `non_interactive()` condition on the non-Windows path, matching the Windows sandboxed path (which already always uses `non_interactive()`). The unsandboxed GUI path (when `sandbox_wrap` is `None` and `headless` is false) still uses interactive mode, preserving existing behavior for non-sandboxed terminals. Pinned by `test_sandboxed_terminal_runs_non_interactively` in `acp_thread.rs`. |
| D28 | Standardized Artifact Storage                                                              | `crates/agent/src/db.rs` + `crates/agent/src/agent.rs` + `crates/agent/src/tools/tool_permissions.rs` + `crates/agent_skills/agent_skills.rs` + `crates/zed/src/main.rs` + `kask/crates/hkask-types/src/agent_paths.rs` + `kask/crates/kask_bridge/src/settings.rs` + `kask/crates/kask_bridge/src/mcp_servers.rs` + `kask/crates/kask_bridge/src/memory/curator_stores.rs` + `kask/mcp-servers/hkask-mcp-kata-kanban/` + `kask/mcp-servers/hkask-mcp-research/` + `kask/mcp-servers/hkask-mcp-portfolio/` + `kask/mcp-servers/hkask-mcp-companies/` + `kask/mcp-servers/hkask-mcp-prediction-markets/` + `kask/mcp-servers/hkask-mcp-scenarios/` + `kask/mcp-servers/hkask-mcp-swarm/` + `kask/mcp-servers/hkask-mcp-training/` + `kask/mcp-servers/hkask-mcp-curator/` | Establishes a single rooted data tree (`{kask_data_dir}/`) with four class subdirs (`agents/`, `mcp/`, `skills/`, `threads/`) for all persistent kask artifacts. **Ownership principle:** an artifact lives under the class subdir of the entity that owns it — agent-owned under `agents/{name}/`, server-owned under `mcp/{server_id}/`, user-owned under flat class dirs. **Archived threads** relocated from `paths::data_dir()/threads/` to `{kask_data_dir}/threads/threads.db` via `THREADS_DB_PATH_OVERRIDE` hook (`agent.rs`), pinned by `test_threads_db_override_hook_round_trips`. **2026-09-10 merge:** retain this storage hook and the error-to-UI `ThreadSaveFailed` event while adopting upstream release-driven session ownership and final-save draining; see the supporting thread-persistence note below. **Skills** relocated from `paths::data_dir()/agents/skills/` to `{kask_data_dir}/skills/` via `GLOBAL_SKILLS_DIR_OVERRIDE` hook (`agent_skills.rs`), pinned by `test_global_skills_dir_override_hook_round_trips` (agent_skills). **MCP server DBs** migrated to `mcp/{server_id}/{purpose}.db` (databases are infrastructure — the ONE artifact class that stays in the hidden internal data dir): kata-kanban (`mcp/kata-kanban/kanban.db`), research (`mcp/research/rss.db`), portfolio (`mcp/portfolio/{owner}/master.db`), companies (shares portfolio path + `mcp/companies/fibo-cache/{owner}/master.db`), prediction-markets (`mcp/prediction-markets/`), scenarios (`mcp/scenarios/`), swarm (`mcp/swarm/ledger.db` + `mcp/swarm/consent.db`), training (`mcp/training/training.db`; adapter weights are hosted on HuggingFace, only metadata is local). **Artifact files and outputs** of the content servers live in the VISIBLE artifacts dir (`~/Documents/zk-data/`) at `{server}-mcp/{artifact-type}/` via `mcp_artifacts_subdir` + `resolve_under_artifacts_dir`: companies reports/screens (`companies-mcp/reports/`, `companies-mcp/screens/`), portfolio transaction files (`portfolio-mcp/transactions/`), corpus cache files (`corpus-mcp/cache/`), media generated assets (`media-mcp/generated/`). **Curator DB** renamed from `pod.db` to `curator.db` (the "pod" concept was deprecated); `agent_pod_db()` renamed to `agent_db()` producing `{agent_name}.db`. Stays under `agents/curator/` (agent artifact, not server artifact). **`HKASK_TRANSACTIONS_DIR`** wired: emitted in `mcp_env()` (default `portfolio-mcp/transactions/` under the artifacts dir), added to portfolio server's `config_env` allowlist. **`HKASK_ARTIFACTS_DIR`** always emitted by `mcp_env()` and allowlisted for the servers that resolve artifact routes (portfolio, companies, corpus, media). **`dirs` crate** removed from portfolio, companies, and swarm crates (replaced by `resolve_under_data_dir`). **Self-healing posture** (decision 2026-08-28): every artifact/DB write path `create_dir_all`s its parent, so user-deleted/moved directories self-heal on next write or server start; no read-only protection (0444-on-write and OS-snapshot options considered and deferred — see the §0 decision record in the storage doc). Pinned by the in-crate layout tests in `kask/crates/hkask-types/src/agent_paths.rs:236+` (`agent_db_follows_agents_class_layout`, `mcp_server_db_follows_mcp_class_layout`, `mcp_server_subdir_handles_empty_and_nested`, `all_layout_helpers_resolve_under_one_root`). See `kask/docs/architecture/standardized-artifact-storage.md` for the full layout spec. |
| D29 | RunPod serverless endpoint provider                                                      | `crates/language_models/src/provider/runpod.rs` + `crates/language_models/src/settings.rs` + `crates/language_models/src/language_models.rs` + `crates/language_models/src/provider.rs` + `crates/settings_content/src/language_model.rs` + `crates/settings_content/src/settings_content.rs` + `kask/crates/hkask-inference/src/model_constants.rs` + `kask/crates/hkask-inference/src/config.rs` + `kask/crates/kask_bridge/src/inference_providers.rs` + `kask/crates/kask_bridge/src/identity.rs` + `kask/crates/kask_bridge/src/kask_bridge.rs` + `crates/zed/src/main.rs` + `kask/mcp-servers/hkask-mcp-corpus/src/services/convert.rs` + `kask/mcp-servers/hkask-mcp-corpus/src/ocr/pipeline.rs` + `kask/mcp-servers/hkask-mcp-corpus/src/ocr/config.rs` | Registers RunPod as a dedicated `LanguageModelProvider` (like DeepSeek, not via `openai_compatible`) so the IPC bridge can resolve `RunPod/kask-ocr` for the corpus OCR pipeline. Each endpoint carries its own API URL (`https://api.runpod.io/v2/{endpoint_id}/openai/v1`); discovery via the RunPod GraphQL API (`{api_url}/graphql`), not `/v1/models`. `DEFAULT_OCR_MODEL` changed from `RunPod/allenai/olmocr-2-7b-1025` to `RunPod/kask-ocr` (the endpoint name is the model id). `LanguageModel::name()` returns the endpoint name (the resolution key), NOT the display_name — the IPC server builds `ModelListEntry.name` as `{provider_id}/{model.name()}`, and a display_name suffix like `(OLMOCR-2)` would break `resolve_ocr_model`'s match. `supports_tools`/`supports_streaming_tools` are `false` (OLMOCR-2 is a specialized OCR model, not tool-capable). **Credential bridge (one key, one location, 2026-08-31):** RunPod is in `INFERENCE_PROVIDERS` with `credential_key: "runpod"`; its API key lives at exactly ONE keychain location — the provider `api_url` (`https://api.runpod.io`), the same slot the provider's `ApiKeyState` reads. Every consumer resolves it through `credential_url_for_key("runpod")`: MCP env injection (`credential_urls_for_mcp` emits the pair exactly once — the `DATA_SERVICES` loop carries it, so the `INFERENCE_PROVIDERS` loop skips the env var it already covered; the key's presence is the toggle, no `runpod_enabled` settings field), the settings UI Data Services row (which also drives the key into the RunPod endpoint provider live via its `set_api_key` path, so `RunPod/*` models refresh without a restart), and the embedding/IPC paths. The startup mirror (`mirror_kask_credentials_to_providers`) is **deleted** — it was one-directional and anti-clobber (kask→api_url only when api_url was empty), so no update path ever refreshed the kask slot, and a key rotated via Settings → AI → LLM Providers left MCP servers authenticating with the dead copy (the 2026-08-31 split-brain, which surfaced as the DeepInfra 401 on the same seam). The former RunPod-only mirror (`mirror_runpod_api_key` in `identity.rs`) and the former env-var mirror (`mirror_env_keys_to_keychain`) are also gone; there is no mirror at all anymore — inference-provider keys never live at `kask://credentials/<key>`. The `.env` file loading has been fully removed — the keychain is the single source of truth for API keys. **`resolve_ocr_model` case-insensitivity fix:** the corpus `resolve_ocr_model` gate used case-sensitive `==` against `ModelEntry.prefixed_name`/`model`, but the IPC server produces lowercase `runpod/...` while `DEFAULT_OCR_MODEL` is `RunPod/...`. Changed to `eq_ignore_ascii_case` to align with `resolve_model_names`'s case-insensitive contract. Pinned by `endpoint_url_derives_from_api_url_and_endpoint_id`, `telemetry_id_format_is_runpod_prefixed_endpoint_name`, `runpod_provider_id_matches_runpod_case_insensitively`, `runpod_telemetry_id_matches_configured_name_case_insensitively`, `graphql_response_parses_into_available_models` (in `runpod.rs`), and `inference_provider_keys_resolve_to_the_provider_slot` + `credential_urls_for_mcp_injects_provider_keys_from_their_api_url_slot` (in `inference_providers.rs` — the one-key-one-location side, incl. RunPod resolving to `https://api.runpod.io` and each env var injected exactly once), plus `runpod_is_a_dedicated_provider_not_openai_compatible` (in `language_models.rs` — the provider-registration side: the dedicated provider surfaces with an empty settings file, which no `openai_compatible` entry can). |
| D31 | wasmtime `ResourceTable` leak in `WasmExtension` call sites | `crates/extension_host/src/wasm_host.rs` | Upstream's `WasmExtension` impl of `extension::Extension` pushes a resource (worktree, project, kv_store, delegate) into `store.data_mut().table` at 12 call sites (`language_server_command`, `language_server_initialization_options`, `language_server_workspace_configuration`, `language_server_initialization_options_schema`, `language_server_workspace_configuration_schema`, `language_server_additional_initialization_options`, `language_server_additional_workspace_configuration`, `run_slash_command`, `context_server_command`, `context_server_configuration`, `index_docs`, `get_dap_binary`) and **never calls `table.delete`** to reclaim the entry. The wasmtime `ResourceTable` (`wasmtime-36.0.13/src/runtime/component/resource_table.rs`) has a default `max_capacity` of 1,000,000 entries; once exhausted, every subsequent `push` returns `ResourceTableError::Full` ("resource table has no free keys"), which `lsp_store.rs:4111` logs at ERROR via `.log_err()`. The leak manifested as a flood of `getting additional workspace configuration for X from Y: resource table has no free keys` errors because `workspace_configuration_for_adapter` (`lsp_store.rs:4096-4116`) loops over every registered LSP adapter and calls `additional_workspace_configuration` on each — with 6 adapters (rust-analyzer, yaml-language-server, bash-language-server [built-in, no leak] + buf, protobuf-language-server, protols [wasm-backed `proto` extension, leaking]) the cross-product leaked 30 entries per cycle, hitting the cap in ~33,000 cycles and breaking all wasm-backed LSP adapters. The downstream symptoms were rust-analyzer `textDocument/diagnostic` and `textDocument/formatting` timeouts (120s cancellation) and a foreground hang on `app_will_quit`. **Fix:** each call site now captures `resource.rep()` (the `u32` handle) before passing `resource` into the extension call (since `Resource<T>` is not `Copy` — it contains `AtomicU32` state), then calls `store.data_mut().table.delete(Resource::<T>::new_own(rep))` after the call resolves, on both success and error paths. The `delete` runs before the first `?` that propagates the wasmtime error, so it runs regardless of whether the extension call succeeded. **Why not the bridge?** The leak is in the wasm host's `Extension` trait impl, which is upstream code that kask crates don't touch; the bridge is for kask-specific behavior, not for patching upstream trait impls. This is a genuine D-seam: an upstream bug fix that benefits all Zed users, not just kask. Should be reported upstream to `zed-industries/zed`. Pinned by `test_resource_table_push_delete_round_trip_reclaims_entries` (verifies the push-then-delete pattern reclaims entries on a locally constructed `ResourceTable`) and `test_every_resource_push_site_has_a_paired_delete` (source-structure pin, mermaid-precedent style: reads the file's own source, excludes the test module, and asserts every `table.push` site has a paired `delete` — 12 of each; fails if any single `delete` is removed) and `test_resource_table_full_error_message_matches_log_symptom` (documents the failure mode so future maintainers recognize the log symptom). |
| D32 | No-op `update_settings_file` write skips file write + observer notification | `crates/settings/src/settings_store.rs` + `crates/language_models/src/provider/runpod.rs` | Upstream's `SettingsStore::update_settings_file_inner` unconditionally calls `fs.atomic_write` (triggering the settings file watcher) and `cx.update_global::<SettingsStore>` (whose `end_global_lease` pushes `Effect::NotifyGlobalObservers` even when `set_user_settings` returns `Unchanged`) on every call, even when the `new_text` is byte-for-byte identical to `old_text`. This created a self-sustaining feedback loop (historical trigger, since removed): `ensure_openai_compatible_entries` (then wired to a `SettingsStore` observer in `main.rs`) called `update_settings_file` on every settings notification → the file write triggered the file watcher → `cx.update_global` pushed `NotifyGlobalObservers` → the observer re-fired → `ensure_openai_compatible_entries` re-wrote the same file → loop. The RunPod "auto_discover is enabled but no API key is set" warning fired on every iteration because the RunPod `State`'s `SettingsStore` observer called `cx.notify()` unconditionally (unlike its siblings `ollama`, `llama_cpp`, `open_router`, `api_compatible`, which all guard with `if settings_changed`). **Fix 1 (root cause):** `update_settings_file_inner` now compares `new_text` to `old_text` and skips both the `fs.atomic_write` and the `cx.update_global` when they are equal, breaking the loop for all callers. `set_user_settings` already early-returns `Unchanged` on identical content, so skipping the `update_global` entirely is consistent with the in-memory store's own idempotency. **Fix 2 (amplifier):** the RunPod `State` observer now tracks `last_settings` and only calls `cx.notify()` when the settings actually changed, matching the `ollama`/`llama_cpp`/`open_router`/`api_compatible` pattern. This stops the warning spam even if some unrelated settings write fires the observer. **Why not the bridge?** The `update_settings_file_inner` fix is in upstream's `SettingsStore` impl — the bridge is for kask-specific behavior, not for patching upstream settings-store internals. This is a genuine D-seam: an upstream bug fix that benefits all Zed users (any caller that writes the same settings content twice now skips the file write and observer storm. Note: this skip initially defeated `nudge_mcp_servers` (D9) — its file-write path stopped firing observers — so the nudge was re-fixed to call `SettingsStore::notify_observers` directly (`kask_page.rs`), pinned by `test_noop_update_settings_file_does_not_fire_settings_store_observer`). Should be reported upstream to `zed-industries/zed`. Pinned by `test_update_settings_file_skips_noop_write` (verifies that a no-op write does not touch the filesystem or fire observers) and `test_update_settings_file_writes_when_content_differs` (verifies the write still happens when content actually changes). |
| D33 | Swarm panel full CRUD + cloud↔local movement: composition editing, confirmation flow, cloud delete, metadata edit, swarm copy, port display, push/pull | `crates/swarm_panel/src/{swarm_panel.rs,swarm_ops.rs,card.rs,parse.rs,fetch.rs,hire.rs,compose.rs,agent_edit.rs,author.rs}` + `kask/mcp-servers/hkask-mcp-swarm/src/{local_swarms.rs,local_tools.rs,request_types.rs}` | Extends the swarm agent panel from read+add/remove to full management: **Edit swarm metadata** (local only — `swarm_update_local_swarm` tool; ABW has no PATCH endpoint, 405 verified), **Copy/Duplicate swarm** (`swarm_clone_local_swarm` for local; Compose pre-fill for ABW), **Delete swarm (both backends)** (`swarm_delete_local_swarm` for local, `swarm_delete_swarm` for ABW — `DELETE /api/teams/{id}`), **Destructive-op confirmation** (two-step: stage → confirm; shows active-run warning for ABW deletes), **Roster composition view** (each agent row shows `accepts`/`produces` port labels enriched from the local agent catalogue; local rosters cross-reference `swarm_list_local_agents`), **Cloud↔local swarm movement** (`swarm_push_local_swarm` creates ABW workspace from local swarm with consent-gated member hires; `swarm_pull_swarm_to_local` creates local swarm copy from ABW workspace, no cost), **`cloud_workspace_id` link tracking** (new `LocalSwarm` field; set by push/pull; shown as "synced with ABW" badge in detail view; drives `AgentSource::Synced` badge for swarms in browse list), and **UI terminology rename** ("Clone" → "Copy" everywhere). New MCP tools: `swarm_update_local_swarm`, `swarm_clone_local_swarm`, `swarm_push_local_swarm`, `swarm_pull_swarm_to_local`. New `LocalSwarmRegistry` methods: `update_metadata`, `clone_swarm`, `set_cloud_workspace_id`. New panel state: `DestructiveAction` enum (DeleteSwarm/RemoveAgent), `pending_destructive` confirmation flow, the metadata-edit flow (name/mission editing saved via `swarm_update_local_swarm`; the former `detail.rs` editor fields were reworked into the compose/author forms), `cloud_workspace_id` on the detail view. The `SwarmRosterAgent` struct gained `accepts`/`produces` fields; `parse_swarm_roster` extracts them from ABW roster objects; local rosters are enriched by cross-referencing `swarm_list_local_agents`. `SwarmCard` gained `cloud_workspace_id`; `LocalSwarmInfo` gained `cloud_workspace_id`; `filter_entries` includes `Synced` swarms in both Cloud and Local source filters. Swarm card "More actions" menu: Copy (both backends), Push to Cloud (local), Copy to Local (cloud). Pinned by `local_swarms::tests::{update_metadata_*, clone_swarm_*, cloud_workspace_id_*, set_cloud_workspace_id_*}` (backend, 9 tests) + swarm_panel tests for confirmation flow, tool-name pinning, and port parsing. |
| D34 | Batch inference API (OpenRouter + DeepInfra) via the IPC bridge | `kask/crates/hkask-types/src/{inference_ipc.rs, ports/inference_port.rs}` + `kask/crates/hkask-inference/src/{batch.rs, hkask_inference.rs, inference_ipc_client.rs}` + `kask/crates/kask_bridge/src/{inference_ipc_server.rs, inference_socket.rs, mcp_servers.rs, Cargo.toml}` + `kask/crates/hkask-mcp-server/src/server/validation.rs` + `kask/mcp-servers/hkask-mcp-corpus/src/{services/qa_pipeline.rs, tools/semantic/batch_api.rs, tools/semantic.rs, tools/storage.rs, services.rs, Cargo.toml}` | **Batch inference routing through the IPC bridge** — MCP servers submit batch LLM calls as a single `InferenceMethod::GenerateBatch` IPC request; the zed side holds the API keys (read from the keychain via a GPUI-side credential channel) and calls the provider's Batch API (OpenRouter `/api/beta/batches` at 50% cost discount, DeepInfra `/v1/openai/batches` at 20%). The MCP server never sees the credentials. **Provider detection** (`hkask_inference::batch::detect_batch_provider`): model names ending in `:batch` route to OpenRouter (suffix stripped); model names starting with `DeepInfra/` route to DeepInfra (prefix stripped); `HKASK_BATCH_PROVIDER` env var forces a provider. **IPC protocol additions**: `InferenceMethod::GenerateBatch` variant, `BatchPromptEntry`/`BatchResultEntry` structs, `batch_prompts`/`batch_max_tokens` fields on `InferenceParams`, `InferenceOutcome::BatchResults` variant. **`InferencePort::generate_batch` trait method** (default: error) implemented by `InferenceIpcClient` and `LazyInferencePort` — no direct fallback (batch requires the IPC bridge). **Batch IPC read timeout**: `IPC_BATCH_READ_TIMEOUT` (6h+60s) in `inference_ipc_client.rs` — batch calls use `read_response_line_with_timeout` instead of the default `ipc_read_timeout` (~330s) because batch processing can take hours. **Inference socket fallback file**: `set_inference_socket_path` writes the socket path to `$XDG_RUNTIME_DIR/kask/inference-socket-path`; `InferenceIpcClient::from_env` falls back to reading this file when `HKASK_INFERENCE_SOCKET` is absent (covers self-healing reconnects with stale `LaunchSpec` env). **Multi-root path containment** (`validation.rs`): `contain` now allows paths under CWD + kask data dir + kask artifacts dir (D28 standardized artifact storage) instead of CWD only. **Corpus server integration**: `corpus_generate_qa_batch` routes to `tools/semantic/batch_api.rs` when the model is batch-eligible; `services/qa_pipeline.rs` deduplicates the QA prompt formatting + result envelope construction shared by all three QA generation paths (single-chunk, concurrent batch, provider batch API); `tools/storage.rs` `corpus_query` accepts a Lisp S-expression query (`(list (list "query" "...") (list "top-k" 5) (list "include-text" t) (list "min-score" 0.6))`) parsed via `hkask_lisp::eval_sandboxed_with_budget`, plus `search_passages` extraction deduplicating the hydrated/in-memory search paths. **Dependencies**: `hkask-lisp` added to `hkask-mcp-corpus`; `zed_credentials_provider` added to `kask_bridge`. Pinned by `batch.rs::tests::{detect_openrouter_batch_suffix, detect_deepinfra_prefix, detect_non_batch_model_returns_none, detect_strips_batch_suffix_from_deepinfra_prefix, parse_successful_result, parse_error_result_includes_error_message, parse_result_with_no_choices_is_failed, parse_empty_lines_skipped, parse_mixed_success_and_failure, parse_malformed_line_returns_error, format_batch_jsonl_produces_valid_jsonl}` (11 tests) + `storage.rs::tests::{search_returns_results_sorted_by_score, search_top_k_truncates, search_min_score_filters, search_include_text_adds_text_field, search_empty_passages_returns_empty, parse_lisp_query_basic, parse_lisp_query_with_min_score, parse_lisp_query_missing_query_returns_error, parse_lisp_query_unknown_key_returns_error, parse_lisp_query_generate_answer}` (10 tests) + `validation.rs::tests::{contain_accepts_cwd_relative_path, contain_accepts_absolute_cwd_path, contain_accepts_data_dir_path, contain_accepts_artifacts_dir_path, contain_rejects_etc_passwd, contain_rejects_traversal_escape, contain_rejects_nonexistent_read_path}` (7 tests) + `qa_pipeline.rs::tests` (5 tests). |
| D35 | Media MCP server + MediaRouter + MovieLabs OMC ontology | `kask/mcp-servers/hkask-mcp-media/` (28 source files) + `kask/crates/hkask-inference/src/{media_router.rs, provider.rs, media_providers.rs}` + `kask/crates/hkask-bridge-ontology/src/omc.rs` + `kask/crates/hkask-storage/src/gallery.rs` + `kask/crates/hkask-types/src/{voice.rs, ports/inference_port.rs (MediaGenerateParams, MediaFuture, media_generate trait method)}` + `kask/crates/hkask-inference/src/{hkask_inference.rs (LazyInferencePort::media_generate — child-local dispatch), model_constants.rs (DEFAULT_TTS/STT/VISION/IMAGE_GEN_MODEL)}` + `kask/crates/kask_bridge/src/{mcp_servers.rs (media server registration)}` + `kask/registry/templates/media/` (7 template files) + `.agents/skills/create-skill/SKILL.md` (OMC ontology row) + `Cargo.toml` (workspace member + nom-exif dep) | **Recovered media stack** — the media MCP server (80 tools: gallery management, image/video/audio generation, transcription, face recognition), the bespoke `MediaRouter` (pluggable `MediaProvider` trait + `ProviderRegistry` for non-chat media APIs that zed's `LanguageModel` cannot represent), and the MovieLabs OMC ontology bridge (9 seed concepts: CreativeWork, Scene, Shot, Sequence, Participant, Capture, Asset, Task, VERSION_INFO + `tool_to_omc` mapping for all 80 tools (in `hkask-mcp-media/src/omc.rs`; the concepts + `explain_tool_for` dispatch live in `hkask-bridge-ontology/src/omc.rs`)). **Media generation is child-local (2026-08-31)**: `LazyInferencePort::media_generate` dispatches to a process-local `MediaRouter` (`LOCAL_MEDIA_ROUTER` OnceLock in `hkask_inference.rs`) built from the MCP server process's env-injected keys (`DEEPINFRA_API_KEY` / `OPENROUTER_API_KEY` via the server's credential allowlist) — no IPC round-trip. The former IPC path (`InferenceMethod::MediaGenerate` variant, `InferenceOutcome::Media` outcome, `InferenceParams::media_*` fields, `InferenceIpcClient::call_media_generate`, the zed-side `media_router` field on `LanguageModelInferencePort`, and the `MediaGenerate` dispatch arm) is deleted: the zed-side router was built from `InferenceConfig::from_env()` — the zed process env, which never contains the keys (they are injected only into child processes) — so every IPC-routed media call failed with "no provider configured" even with keys installed, and the IPC branch shadowed the working child-local fallback. Media APIs are not LanguageModel calls, so the bridge bought nothing; vision/chat/embed/list_models still route over the IPC bridge to zed's `LanguageModelRegistry`. **Operator-ratified routing policy (2026-09-06):** resolve `params.model` first, otherwise the per-operation env model; require full `OpenRouter/...` or `DeepInfra/...` qualification (ASCII case-insensitive provider names, nonempty local model, no whitespace). No bare/short aliases, score/order selection, or automatic cross-provider retry on any failure. Dispatch once to the named registered provider with only the local model; preserve typed errors (Model → invalid_argument, NotConfigured/Auth → permission_denied). Fixed DeepInfra background removal/upscale require no model env and reject overrides. Supersedes `d660f3b754` scoring/fallback and `8cc79c797e` order routing; preserves `f86cf19a70` child-local and foreground media behavior. Scorer and the unused MediaRouter chat facade are removed. Pins: `media_routing_tests` (including real loopback adapters and LazyInferencePort with zero IPC calls) and media `error::tests::real_router_http_auth_reaches_media_tool_mapper`. **GalleryStore**: SQLite-backed gallery with image indexing, tag persistence, face registry, generation lineage, and EXIF timeline grouping. Restored to `hkask-storage` with `SqliteDriver::file_pool` re-added for unencrypted file-backed pools. **Deprecated rJoule/budget/gas system stripped**: `budget.rs` deleted, all `charge_budget` calls removed from tool handlers, `VerificationStore` and `hkask-templates` dependencies removed. **Server registration**: `BuiltinMcpServer` entry with `HKASK_INFERENCE_SOCKET`, `HKASK_DATA_DIR`, `HKASK_MEDIA_DB`, and media model env vars in `config_env`; `OPENROUTER_API_KEY` + `DEEPINFRA_API_KEY` in `credentials` (both load-bearing for the child-local `MediaRouter`). Pinned by `media_router::tests::{media_generate_unknown_op_errors, media_generate_no_provider_errors_clearly}` + `media_providers::tests::{detect_audio_format_from_extension, detect_audio_format_defaults_to_mp3}` + `omc::tests` (19 tests in `omc.rs` plus the `tool_surface_is_exactly_80_registered_tools` and `omc_mapping_covers_all_registered_tools` pins in `hkask_mcp_media.rs`) + `gallery.rs::tests` (20 tests including durability across restart) + `levenshtein_tests` (5 tests). |

| D36 | Chat Completions `finish_reason: "stop"` with accumulated tool calls → drain before stop | `crates/language_model_core/src/chat_completion.rs` | Some providers (e.g. GLM via OpenRouter) emit tool deltas but finish with `stop`, not `tool_calls`. The 2026-09-10 merge moves the Kask drain from the deleted OpenAI/OpenRouter mappers into the live shared `ChatCompletionEventMapper`. A stop with pending, non-rejected calls uses the existing tool-call drain and emits `StopReason::ToolUse`; a stop without calls remains EndTurn. The upstream ordered accumulator, signatures, JSON parse-error events and attribution-error guard remain intact. Length remains MaxTokens (D25). Pinned by `stream_maps_stop_with_tool_calls_to_tool_use_stop`, `stop_without_tool_calls_ends_turn`, `stop_with_invalid_tool_arguments_surfaces_parse_error`, and the strengthened `does_not_emit_tool_calls_after_an_attribution_error`. Migrated provider usage-ordering tests are covered by shared `usage_update_precedes_text_and_stop_events_from_the_same_chunk` and `usage_update_precedes_tool_use_and_stop_events_from_the_same_chunk`; reasoning metadata/signatures remain covered by the shared accumulator and mapper tests. |

| D37 | Commit-message and thread-title generation respect reasoning-model requirements (guard + self-healing) | `crates/git_ui/src/git_panel.rs` + `crates/agent/src/thread.rs` | Upstream's `GitPanel::generate_commit_message` hardcodes `thinking_allowed: false` and `reasoning_effort: None` on the `LanguageModelRequest`. When the resolved commit-message model (which falls back to `default_fast_model` → `default_model` — the user's reasoning model) is a reasoning-only model (gpt-5, o3, claude-opus-5, etc.), the provider rejects the request with a 400 `InvalidRequest` because reasoning cannot be disabled. The error surfaces as "Failed to generate commit message: …" and the generate button appears stuck (the task ends in error) while the rest of the zed-kask session reasons along normally. **Why not kask-side?** Commit-message generation calls `model.stream_completion_text` directly on the `LanguageModel` from `LanguageModelRegistry::commit_message_model` — it does NOT route through `kask_bridge` (D8). There is no kask-side hook in this path; the only interception points are upstream (`registry.rs` model resolution, `git_panel.rs` request building, or routing through the bridge). A kask-side fix would require a strictly larger upstream edit (adding a hook + bridge indirection, or rerouting through the bridge). The minimal divergence is at the call site. **Fix (two layers):** (1) **Guard** — `thinking_allowed = model.supports_thinking()` and `reasoning_effort = thinking_allowed.then(|| "low".to_string())`. Commit messages are simple, so low effort minimizes tokens while satisfying the model's reasoning requirement. (2) **Self-healing retry** — if the initial request fails with a `ProviderRejection` whose category is `InvalidRequest` (400) and thinking was not allowed, retry once with `thinking_allowed = true` and `reasoning_effort = Some("low")`. This catches reasoning-only models whose `supports_thinking()` returns false because the provider doesn't advertise `reasoning_effort` in model metadata (common for OpenAI-compatible providers where `/v1/models` discovery leaves `reasoning_effort: None`). The retry is bounded to one attempt and only fires on `InvalidRequest` (not rate limits, auth, or transport errors). Mid-stream errors do not retry (partial text may already be in the buffer). The `is_reasoning_required_error` helper classifies the error. Pinned by `test_commit_message_guard_enables_thinking_for_reasoning_model`, `test_commit_message_guard_disables_thinking_for_non_reasoning_model`, and `test_is_reasoning_required_error_classifies_invalid_request` in `git_panel.rs`. **Thread-title generation (absorbs retired D38 — same class, same fix pattern):** `Thread::spawn_title_generation` (`crates/agent/src/thread.rs`) carries the same guard + self-healing retry — upstream's `build_thread_title_request` leaves `thinking_allowed` at default `false`, so a reasoning-only summarization model rejects with 400 `InvalidRequest` and the title never generates (recurring `ERROR [agent::thread]` every ~30s, placeholder title). Fix mirrors this seam: guard (`thinking_allowed = model.supports_thinking()`, `reasoning_effort = "low"` when supported) + one bounded retry on `InvalidRequest` via `is_reasoning_required_title_error` — catches reasoning-only models whose `supports_thinking()` returns false because the provider doesn't advertise `reasoning_effort` in metadata (the GLM 5.2 case). 2026-09-10 merge: the request builder now receives `&self.id`; the fork's `reasoning_effort` naming retained (`5269cf00ca`). Pinned by `test_is_reasoning_required_title_error_classifies_invalid_request` and `test_thread_title_guard_preserves_thread_id_and_reasoning_effort`. |

| D39 | PaymentRequired errors name the rejecting provider | `crates/agent_ui/src/conversation_view.rs` + `crates/agent_ui/src/conversation_view/thread_view.rs` | Upstream renders every `ProviderErrorCategory::PaymentRequired` rejection as "You reached your free usage limit. Upgrade to Zed Pro for more prompts." with an upgrade button. That copy is only correct when Zed's own backend (`ZED_CLOUD_PROVIDER_NAME`) rejected the request. Any other provider returning 402/billing codes — OpenRouter credit exhaustion (`insufficient_quota`/402), Anthropic `BillingError`, OpenAI `credit_balance_exhausted` — is reporting that *its* account is out of credit, and upstream's copy misattributes it to the user's Zed plan. Observed in the wild: an OpenRouter account out of credit surfaced as "Free Usage Exceeded / Upgrade to Zed Pro" for a logged-in Pro subscriber, sending the user debugging their Zed billing instead of topping up OpenRouter. **Why not kask-side?** The mislabeling lives entirely in upstream's error-to-UI mapping (`ThreadError::From<anyhow::Error>` + `render_payment_required_error` in `agent_ui`); there is no kask-side hook for agent-panel error copy. **Fix:** `ThreadError::PaymentRequired` now carries `provider: SharedString` (populated from the `ProviderRejection` provider in the `From` impl); `payment_required_message(provider)` returns the Zed-Pro upgrade copy only for `ZED_CLOUD_PROVIDER_NAME`, and "{provider} rejected the request because your {provider} account is out of credit or over its spend limit…" for every other provider; the upgrade button renders only for Zed-cloud rejections (title switches from "Free Usage Exceeded" to "Payment Required"); telemetry carries the same provider-aware message. Pinned by `test_payment_required_preserves_provider` (conversation_view.rs — From-impl preserves the rejecting provider's name) and `payment_required_message_tests::{zed_cloud_provider_keeps_upgrade_copy, third_party_provider_names_its_credit_balance}` (thread_view.rs — Zed copy unchanged; third-party copy names the provider, never offers a Zed upgrade). |
| D40 | Division of Responsibilities — the four-moves interaction loop | `crates/agent/src/templates/system_prompt.hbs` (new `## Division of Responsibilities (kask)` section at `:294` + amended autonomy bullet in `## Task Execution` + functional-first bullet in `## Final Message`) + `crates/agent/src/templates.rs` (7 pinning tests) | Implements Phase A of the functional-interaction spec ([`kask/docs/architecture/functional-interaction-spec.md`](kask/docs/architecture/functional-interaction-spec.md)): the user is the product manager (keeper of functional requirements, judge of what work is for); the agent fits the technical implementation to those requirements. The section carries the four moves — point at the same target (intake interpretation, submitted for user correction; the agent interprets the functional requirement, never revises it), bring choices to the user as experiences (options + recommendation, user decides), report outcomes not artifacts (functional outcome before technical detail), bank the learning. **Framing discipline:** the text is preference-framed (evaluation axis: "the user evaluates this work by what it lets them do") per the spec's gradient-over-constraint principle — no prohibition walls, which habituate and provoke escape-seeking. The autonomy bullet's ask-trigger is extended from "information unavailable / risky" to include "a choice changes what the user will experience" — without this, the bullet licenses unilateral functional decisions (the agent has the information; the question is authority, not information). The same bullet's "rather than coming back to the user prematurely" is reframed to "for information you can find yourself" — "prematurely" framed user-contact as cost and biased against interpretation rounds and choice-surfacing. The `## Ambition vs. Precision` bullet's "high-value, creative touches when scope is vague" is replaced with "high-value additions when they serve the functional goal" + "When scope is vague, resolve the vagueness with the user (see Division of Responsibilities) rather than filling it with initiative" — the upstream phrase licensed unilateral product decisions in exactly the situation (vague scope) that is the four-moves' ask-trigger. The Final Message gains a functional-outcome-first bullet — the prior bullet alone rehearsed a file-list summary format every turn. **Loop-closing wiring (2026-08-29):** the section carries a conditional block — `{{#if (contains available_tools 'kanban_goal_create')}}` — mapping the moves to the native goal tools (`Move 1 → kanban_goal_create` at intake, `Move 3 → kanban_goal_judge` at report, `Move 4 → kanban_goal_score` at resolution), so the model runs the loop on the native system when the kata-kanban server is connected and the wiring vanishes when it isn't (the §3.1 principle: never advertise a capability the turn doesn't have). **Skill rubrics (2026-09-04, operator-ratified):** a paragraph after the four moves names each side's operational rubric — the `program-manager` skill (the agent's side: spec recovery, design-before-coding, definition of done, closure ledger) and the `product-manager` skill (the user's side: the intake contract — requirement, spec provenance, acceptance criteria, constraints, decisions, confirmation) — and directs underspecified intake to the product-manager skill rather than improvisation. Pinned by `test_system_prompt_contains_division_of_responsibilities` (section + all four moves + authority boundary + both skill references + the no-improvisation directive), `test_division_skill_references_resolve_on_disk` (both named skills exist on disk — a prompt reference to a missing skill is a ghost instruction), `test_system_prompt_autonomy_bullet_includes_experience_trigger`, `test_system_prompt_autonomy_bullet_reframes_prematurely` (also asserts "prematurely" absent), `test_system_prompt_vagueness_resolved_with_user` (also asserts the upstream "creative touches when scope is vague" phrase absent), and `test_system_prompt_final_message_leads_with_functional_outcome` (templates.rs). **Evolution (operator ruling 2026-09-10):** the mid-prompt section underperformed — agents read it as an overlay, kept asking technical questions at intake and ignoring functional requirements (observed live in the companies-fix session: technical questions where functional ones were required, three times). Roles now live in the OPENING of the prompt, not the section: the first line renames the agent ("You are the Z-K agent running inside the Zed-Kask fork of Zed.dev") and fixes the roles (user = product manager owning functional requirements; agent = technical program manager / VP of engineering owning technical implementation), followed by a role paragraph carrying the question-class rule (functional questions are the user's to answer, asked in functional terms; technical questions are the agent's to decide — "do not route technical decisions to the user as questions"). The section at `:304` is now the working loop: it opens "The roles are fixed in the opening of this prompt" and move 2 is **Decide by class** — functional decisions surfaced as experiences with a recommendation, technical decisions decided and presented with their functional consequence, "never route one to the user as a question". Pinned by the updated `test_system_prompt_contains_division_of_responsibilities` (opening name + roles linkage + decide-by-class + both question-class rules) and `test_system_prompt_template` (the Z-K name). |
| D41 | Config-file watchers are directory-anchored, not file-anchored | `crates/settings/src/settings_file.rs` (`watch_config_file`) | Upstream's `watch_config_file` watches the config file's own path. On Linux, `RealFs::watch` translates that to an inotify watch on the file's inode — and `RealFs::atomic_write` (tempfile + `persist()`, i.e. rename) replaces the inode on every zed-driven config write. The kernel drops the inode-anchored watch at that moment and nothing re-arms it, so **every later external change to the file is invisible for the rest of the session**. Live root cause of the settings-reactivity defect (2026-08-30): zed's own startup settings write — the kask namespace guard (D3) cleaning `context_servers` — renamed a new inode over `settings.json` one second after launch; from then on "Settings file reloaded by watcher" never fired, `SettingsStore` observers registered in `main()` (including `sync_kask_mcp_servers` and `sync_kask_mcp_runtime_servers`) never fired on external changes, and the operator's live settings edits were ignored until restart. Verified live: the settings file's inode was absent from the app's inotify watch set (133 surviving watches, ~1947 removed since launch), and an in-place inode-preserving modify produced no watcher line. **Fix:** `watch_config_file` now watches the file's containing directory (mirroring `watch_config_dir`'s existing directory-anchored design) and filters event batches to the file's own path (plus `Rescan` markers), so rename-overs and in-place writes both survive while sibling activity in the config directory does not trigger reloads. Benefits every `watch_config_file` consumer: user/global settings, keymap, `.editorconfig`, `AGENTS.md`, recent-projects, remote-server, and project local settings. Should be reported upstream — any atomic-saving editor (sed -i, jq > tmp && mv) breaks upstream's file-anchored watch the same way. Pinned by `test_watch_config_file_is_directory_anchored_and_ignores_siblings` (asserts the watch root is the parent directory via `watched_paths` — the line that fails if the watch reverts to file-anchored — that an inode-replacing `atomic_write` is still delivered, and that sibling-file events do not reload the config file). |
| D42 | Reasoning-capable OpenRouter models get uncapped thinking | `crates/open_router/src/open_router.rs` (`model_from_entry`) + `crates/language_models/src/provider/open_router.rs` (`into_open_router`) + `crates/anthropic/src/anthropic.rs` + `crates/bedrock/src/models.rs` + `crates/language_models_cloud/src/language_models_cloud.rs` (residual budget removal — absorbs retired D49) | Discovered models advertising reasoning retain `Thinking { budget_tokens: None }`, not upstream's Adaptive discovery mapping. The former hardcoded 4096-token reasoning cap silently truncated reasoning-heavy turns; reasoning depth is the model's business, not a hidden client budget. **2026-09-10 merge:** preserve this operator requirement while carrying upstream supported/default efforts, supports-max-tokens and mandatory-reasoning metadata. Explicit user Thinking budgets still pass through; Adaptive settings use upstream typed `ReasoningEffort` with the fork's `reasoning_effort` field. Recovered policy from `5f939570bb` and `a2134949e2` remains authoritative: explicit user disable sends `effort: none` regardless of mandatory-reasoning metadata; the removed `supports_disabling_thinking` and Responses API `supports_none_reasoning_effort` gates are not restored. OpenRouter Thinking requests retain `exclude: false` and the existing parallel-tool-call policy. Pinned by strengthened `reasoning_models_get_uncapped_thinking_budget`, `test_discovered_thinking_is_uncapped_in_request` (uncapped discovery and explicit budget passthrough), `test_explicit_reasoning_disable_overrides_mode_and_mandatory_metadata`, and upstream Adaptive request tests. Related Anthropic/Bedrock/cloud hidden caps were removed in the same ruling (absorbs retired D49): the D13 budget removal (`ce9cb47c6c`) left hardcoded `budget_tokens: Some(4_096)` enforcement in three provider mode constructors — the same defect class as the open_router 4096 that silently killed reasoning-heavy turns. Removed 2026-09-04 under the operator's budget-deprecation ruling ("the budget concept is deprecated and should have been removed; timeouts are the enforcement/kill mechanism for runaway or looping processes") — `budget_tokens: None`, thinking stays enabled, reasoning depth is the model's business. The user-facing `budget_tokens` Option in settings remains the operator's explicit knob (the passthroughs in `crates/language_models/src/provider/{anthropic,anthropic_compatible,bedrock,open_router}.rs` carry the user's setting, not a hidden cap — untouched). Pinned by the updated bedrock `thinking_mode` assertions (`Some(4096)` → `None`) and the strengthened anthropic `from_listed` mode assert; the cloud site had no prior pin. Residual same-class sites pending the operator's ruling: the `max_output_tokens.unwrap_or(4_096)` custom-model metadata fallbacks (`crates/bedrock/src/models.rs:513/:977`, `crates/language_models/src/provider/anthropic_compatible.rs:72`) — output-limit metadata, not thinking budgets. |
| D43 | Turn-limit diagnostics and native compaction recovery control | `crates/agent_ui/src/conversation_view/thread_view.rs` (`render_compact_control`) + `crates/open_router/src/open_router.rs` (`OpenRouterErrorBody::message_with_details`) + `crates/agent/src/thread.rs` (turn-end `CompletionError::MaxTokens` arm + turn-failure `Err` arm) + `crates/agent/src/kask_thread_state.rs` (`max_tokens_turn_end_warning` + `provider_rejection_turn_end_warning` + tests) + `kask/crates/kask_bridge/src/inference_chat.rs` (`completion_error_detail` + both conversion sites + tests — kask-side, no D-seam) | **Native recovery control (2026-09-10, uncommitted):** a Compact context icon follows the reasoning control in the composer's existing wrapping group. It uses the existing native command-turn dispatch, preserving the unsent draft; native command availability controls visibility, and busy/loading/empty state disables activation with a click-time recheck. No new compaction lifecycle or persistence format. `test_compact_command_gate_requires_native_category` pins native-command recognition; rendered click/draft and narrow-layout behavior remain unverified. **OpenRouter rejection detail (2026-09-10, `4cdde2c6e5`):** both HTTP and SSE error conversion retain `metadata.provider_name` and `metadata.raw` in the message instead of dropping the upstream reason behind "Provider returned error". Other metadata is not appended. A live synthetic 720,023-token prompt reproduced the reported 400: provider context 1,048,576, combined requested tokens 1,303,727, raw code `context_length_exceeded`. Retaining that code also enables the existing `PromptTooLarge` classification and Context Too Large UI; no new retry, output-limit or compaction policy. Pinned through the HTTP transport and SSE decoder by `provider_error_details_survive_http_and_streaming_responses`, including metadata-free responses. Upstream sends `acp::StopReason::MaxTokens` with no operator-visible signal — no error, no warn, no UI message. Combined with D42's root cause (a provider-side reasoning cap), this was the silence that made the 2026-08-30 silent-turn-stop incident diagnosable only by inference: the log showed prompt → silence → partial ingest with nothing explaining the stop. **Why not kask-side?** The stop is emitted in `Thread`'s turn-completion handler; there is no kask hook on that path. **Fix:** the `MaxTokens` arm now warns via `log::warn!` (target `agent.thread`) with the thread id, the model, and `KaskThreadState::max_tokens_turn_end_warning(model, produced_content)` — which distinguishes the zero-content signature (prompt → silence → nothing) from partial-content truncation. `produced_content` is computed from the messages appended after the turn's `message_ix` boundary (the same boundary the Refusal arm truncates to). **Extension (2026-09-10, same match block):** the turn-failure `Err` arm had the mirror-image gap for provider rejections — `log::error!("Turn execution failed: {:?}", error)` prints the anyhow Display, and `ProviderRejection`'s Display is the provider's wire message only, so the status, provider code, category and retry-after that the variant deliberately preserves for diagnostics reached no log. Made live by the 2026-09-10 incident: five turn failures across three sessions in three minutes, all logging only `Provider returned error` (OpenRouter's generic upstream-failure wrapper for GLM 5.3) — credit exhaustion (402), rate limiting (429), an upstream outage (502, which `retry_strategy_for` retries four times over ~75 s before the turn fails) and a statusless mid-stream rejection (classified permanent, fails sub-second with no retry) are indistinguishable in the log yet demand different operator action. The `Err` arm now downcasts to `LanguageModelCompletionError` and routes `ProviderRejection`s through `KaskThreadState::provider_rejection_turn_end_warning` — one `log::error!` (target `agent.thread`) carrying provider, status, code, category, retry_after, the wire message, and the `is_transient` verdict (the exact predicate `retry_strategy_for` gates on, so the line states whether the failure followed retry exhaustion or happened on the first attempt). **Why not kask-side (extension):** the same handler, the same reason — the main-thread completion path (`Thread::send` → `model.stream_completion` on the registry model) never routes through `kask_bridge` (D8 serves MCP-server inference over IPC only), so no bridge-side code can observe these errors; the helper lives in `KaskThreadState` (the kask-side home inside the agent crate, per the D2/D6 pattern), keeping the zed-side footprint to one log block + its pin. **Bridge-path fix (same session, kask-side — no D-seam):** the bridge had the mirror-image gap — `StreamAccumulator::process_event` flattened every mid-stream error to `InferenceError::Generation(e.to_string())` and the stream-establishment `map_err` to `Connection(error.to_string())`, so MCP-server inference (corpus QA, condenser) lost the same wire fields. `completion_error_detail` (kask_bridge `inference_chat.rs`) now enriches both strings at the boundary — the one place that sees both worlds; `InferenceError` stays string-carrying because hkask-types must not depend on zed-side types (`StatusCode`, `ProviderErrorCategory`), so the variant remains the programmatic classification and the string carries the detail. The classification verdict states the class only (`transient`/`permanent`) — retry behavior is the caller's policy, not the bridge's to claim. Pinned by `max_tokens_warning_names_the_stop_reason_and_content_state` (kask_thread_state.rs — asserts the warning names `StopReason::MaxTokens`, the model, and the zero-content vs partial-content distinction), `provider_rejection_detail_names_wire_fields_and_transient_verdict` + `provider_rejection_detail_marks_statusless_permanent_rejections` + `provider_rejection_detail_is_none_for_non_rejections` (kask_thread_state.rs — wire fields, the statusless/permanent signature, and the non-rejection pass-through), and `turn_failure_surfaces_provider_rejection_detail` (thread.rs — source-structure pin, D48 style; the needle is assembled via `concat!` so the test's own source cannot satisfy it). Bridge-path pins: `completion_error_detail_carries_rejection_wire_fields` + `completion_error_detail_marks_statusless_rejections_permanent` + `completion_error_detail_passes_non_rejections_through` (formatter), `mid_stream_rejection_surfaces_wire_detail_through_the_port` (behavioral — full port → `InferenceError::Generation` carries the fields), and `establishment_errors_route_through_the_detail_helper` (source-structure pin — the fake cannot inject a ProviderRejection at establishment). |
| D44 | Full MCP tool surface + visibility marker + discovery meta-tool (LazyToolRouter removed) | `crates/agent/src/tools/list_mcp_tools_tool.rs` (new `list_mcp_tools` tool) + `crates/agent/src/tools.rs` + `crates/agent/src/thread.rs` (`count_hidden_mcp_tools`, `render_system_prompt`, `add_default_tools`) + `crates/agent/src/templates.rs` + `crates/agent/src/templates/system_prompt.hbs` + `assets/settings/default.json` (profile allowlists) + `crates/settings_ui/src/pages/tool_permissions_setup.rs` (EXCLUDED_TOOLS) | **2026-09-10 merge:** `build_completion_request` retains Kask's UTF-8-safe, 200-byte description cutoff while consuming upstream's no-argument `input_schema() -> Value`; it no longer calls the removed format adapter or treats schema retrieval as fallible. The full-tool surface, visibility marker and cached conditional-rules prompt remain. The `LazyToolRouter` — a per-turn keyword-scoring filter that retained ≤ 40 of ~361 registered MCP tools, scored against the last user message only — was **removed** (2026-08-30) after a live incident exposed its failure mode: the model read the pruned list as the complete toolset and reported registered tools as "unavailable" (an agent denied `web_ping` existed; the operator caught the false report). The removal case: (1) six consumer escape hatches (built-ins, skills, scoped threads, exact names) plus three fail-open gates — every special consumer bypassed it and every uncertainty path surrendered to full surface, so it acted only in the narrow band "complex message, confident keyword match, no skill, no scope" — exactly where main sessions live; (2) its recall-1.00 eval was self-graded; (3) per-turn selection changes the tools array every turn, busting provider prefix caching (a stable full list caches; a changing 40-tool subset may not); (4) within-domain sampling gave sessions incoherent slices (8 of media's 68 tools — enough to look covered, not enough to complete a workflow). **What replaced it (three parts):** (a) the full registered surface is presented every turn — no per-turn filtering in `enabled_tools`; (b) the D44 visibility marker: `render_system_prompt` counts registered-but-hidden MCP tools (`count_hidden_mcp_tools`) and the system prompt renders, only when non-zero, a Tool Use bullet naming the count, forbidding absence claims, and pointing at the recovery path — the remaining filter layers (agent-profile allowlists and per-tab server scope) still hide tools, and the marker keeps those absences legible; (c) the `list_mcp_tools` meta-tool: the model pulls the registered surface on demand (grouped by server, name + description, optional case-insensitive substring filter over server id / tool name / description) — discovery by pull instead of pre-selection by push, the pattern that scales in the tool-retrieval literature. The `kask.tool_router` settings section, its UI toggle, and the `set_tool_router` hook were removed with the router; existing settings files with a `kask.tool_router` key ignore it (no `deny_unknown_fields`). If the fleet grows to where schema tokens are a material context fraction (~2x today), the revisit design is whole-server (domain) selection per user message — never within-domain sampling. Pinned by `test_list_mcp_tools_enumerates_and_filters` (tests/mod.rs — real path: source → registry poll → listing; grouping + all three filter axes + empty-on-no-match), `test_system_prompt_names_tools_hidden_by_profile` (tests/mod.rs — integration: profile filters a tool, marker names the count), `test_system_prompt_names_hidden_mcp_tools_when_router_prunes` + `test_system_prompt_omits_hidden_mcp_section_when_nothing_hidden` (templates.rs — marker render states), and `count_hidden_mcp_tools_excludes_visible_and_counts_hidden` (thread.rs). |
| D45 | Kask managed servers in Settings → AI → MCP Servers + runtime load/unload | `crates/settings_ui/src/pages/mcp_servers_page.rs` + `crates/ui/src/components/ai/ai_setting_item.rs` (`AiSettingItemSource::BuiltIn`) + `crates/zed/src/main.rs` (`sync_kask_mcp_runtime_servers` + launch-pass latch) + `crates/settings_ui/src/pages/kask_page/mcp_servers.rs` + `crates/settings_ui/src/pages/kask_page.rs` (stale-count copy) + `kask/crates/kask_bridge/src/settings.rs` (stale-count doc) + `kask/docs/reference/kask-settings.md` | The Settings → AI → MCP Servers page reads only the per-project `ContextServerStore`, so the 11 built-in kask servers (single spawn authority, D3) were invisible there — their only management surface was Settings → Kask → MCP Servers (load toggles). **Part 1 — UI:** a "Kask Managed Servers" section on the AI MCP page renders every `BUILT_IN_MCP_SERVERS` entry as an `AiSettingItem` with a new `AiSettingItemSource::BuiltIn` variant (Kask icon; "is a built-in kask MCP server, managed by the kask runtime" tooltip), live status derived from the `kask.mcp` load set + the process-global `KaskToolSource` tool cache (`Running` with an "N tools" label / `Starting` / `Stopped`), the registry description as the row detail, and a toggle writing `kask.mcp.overrides` — the same write the Kask page performs. The section renders without a project (kask servers are app-global). No configure/uninstall buttons: the command/env are managed (`build_mcp_server_env`), not user-editable. **Part 2 — runtime load/unload:** the `sync_kask_mcp_runtime_servers` observer previously only restarted servers whose *env* changed — a load toggle does not change env (`kask_server_env` excludes the override), so toggling a server OFF left it running and toggling a never-launched server ON never started it. The observer now also computes the load set (the same expression as the launch loop: `load_default && overrides.get(id).unwrap_or(true)`) and stops/starts explicitly through the runtime's own primitives, preserving the self-healing semantics: `stop_server` is idempotent and drops the launch spec (a deliberately unloaded server is not resurrected by the health supervisor or the on-demand reconnect); `register_server` + `start_server_with_env` are idempotent and carry the startup retry/backoff, launch-spec recording, and health supervisor wiring. The start path is gated on a **launch-pass-complete `AtomicBool` latch** (set at the end of the deferred launch loop, unconditionally — an empty `servers_to_start` is still a completed pass): before the latch, "no baseline" means the launch pass hasn't decided, and an observer-side start could race the launch loop with an env computed before the inference socket exists (the socketless-server defect class). The latch also makes the launch loop's "on start failure the baseline is dropped so a later settings change retries" comment true — previously the observer skipped no-baseline servers, so nothing retried a failed start until a tool call hit the recorded launch spec. The latch threads through `wire_kask_inference_stack` (its post-socket re-sync uses the same observer). **Stale copy fixed in the same pass:** "10"/"12 servers" → count-free (the registry has 11; the counts had drifted in both directions). Pinned by `kask_managed_server_status_maps_load_and_live_tools`, `kask_tool_counts_group_descriptors_by_server`, `kask_server_loaded_follows_master_toggle_and_overrides` (settings_ui `mcp_servers_page.rs` tests), `builtin_source_tooltip_names_the_kask_runtime` (ui `ai_setting_item.rs`), and the updated `kask_wiring_symbols_exist` fn-pointer pins for `sync_kask_mcp_runtime_servers` + `wire_kask_inference_stack` (zed `main.rs` — the latch parameter). Known residual: the settings page renders a snapshot at render time — a server that finishes starting while the page sits open shows `Starting` until the next settings-driven re-render (the tool cache refreshes every 2s but nothing re-renders the page on its own). |
| D46 | Build profile seam + self-observing install | root `Cargo.toml` (`[profile.release-mcp]` + `[profile.release.package]`) + `kask/scripts/build/install.sh` + `kask/scripts/build/build-monitor.sh` + `kask/scripts/build/check-build-profile.sh` (CI: `kask-invariants.yml`) | **Only the `zed` binary builds on the full `release` profile** (thin LTO + codegen-units=1 — worth it for the editor). The 11 MCP servers build on `[profile.release-mcp]` (inherits release, `lto = false`, `codegen-units = 16`): they are I/O daemons, LTO buys them nothing, and building them on `release` pinned one core per crate for minutes — the install CPU-burn defect (an install pegged all 24 cores / ~25 GB RSS on the dev box and crashed the machine). `install.sh` splits the build into two invocations (`--release --package zed`, then `--profile release-mcp` + the `mcp-servers.txt` packages), caps both with `--jobs min(nproc, 16)` (override: `HKASK_BUILD_JOBS`; raised 8→16 2026-09-02 — the 8 cap left the 24-core dev box at 1/3 capacity, and CI runs no cargo so only local machines observe the cap), and copies servers from `target/release-mcp`. **Every install observes itself**: `build-monitor.sh` samples the compile processes (count, total CPU%, total RSS, busiest) into `target/build-cpu-trace.log` (override: `HKASK_BUILD_TRACE`) and appends a peak summary — a burn leaves a number, not a user report. The sampler self-terminates on parent death. `script/clippy` carries the same jobs cap. **sccache wiring (2026-09-02; wrapper relocated 2026-09-07):** `install.sh` and `script/clippy` export `RUSTC_WRAPPER` when the binary is present and log a warning naming the enable path when it isn't — a missing wrapper must not break the build, but "not configured" must be distinguishable from "configured but broken". `script/setup-sccache` installs the wrapper to `~/.local/lib/kask-sccache/` (`HKASK_SCCACHE_DIR` overrides) — OUTSIDE `target/`, because `cargo clean` deletes the target tree and on 2026-09-07 a clean deleted the wrapper so a full install ran uncached with a healthy 4 GiB disk cache (the silent-uncache defect); `check-build-profile.sh` fails if any of the three scripts references `target/sccache`. `script/setup-sccache` defaults the server cache to 20 GiB (`SCCACHE_CACHE_SIZE` override; the 10 GiB sccache default evicts before covering one full profile set of ~800 deps × 5 profiles) and restarts the server so the size applies even when the binary was already cached. The workspace-wide `.cargo/config.toml` `[build] jobs` cap was raised 8→16 in the same pass. Pinned by `check-build-profile.sh` (profile exists with cheap settings, install.sh uses the split + cap + trace + sccache wiring, jobs = 16 in `.cargo/config.toml`) wired into `kask-invariants.yml`. **Release dependency codegen memory bound (absorbs retired D50; operator-authorized 2026-09-07, applied 2026-09-08):** `[profile.release.package]` carries `"*" = { codegen-units = 16 }` — external dependencies no longer inherit single-unit release codegen (concurrent builds and large cgu=1 jobs caused swap thrashing). Thin LTO and the explicit `zed` override unchanged; the wildcard applies to non-workspace dependencies, not workspace crates. Pinned by the TOML-parsed check in `check-build-profile.sh`: observed RED without the override, GREEN with it. One build at a time remains required. |
| D47 | Tool-schema `description` keyword/property-name collision fix | `crates/language_model_core/src/tool_schema.rs` (`strip_verbose_property_fields`) | Upstream's `strip_verbose_property_fields` removed the `description` key from EVERY object in the schema tree — including `properties` maps, whose keys are property NAMES. Any tool input with a field named `description` had the property silently deleted from `properties` while its `required` entry survived — a schema that demands a field it no longer declares. Models emit empty objects for it and the call fails with "missing field `description`" (observed 7/7 across two sessions on `kanban_goal_create` — the goal loop was unusable). Silent casualty with no `required` entry to betray it: `kanban_task_create`'s optional `description` parameter (`TaskCreateRequest.description`, kata-kanban types.rs) was stripped from the model-visible schema, so the model could not set task descriptions at all. **Why not kask-side?** The strip belongs in zed's shared model-request normalization path; there is no kask hook between that sanitizer and the model. **2026-09-10 mapped reapplication:** upstream removed `adapt_schema_to_format` and the format enum; D47 now runs inside the live `normalize_tool_schema`, after upstream's transactional reference inlining. No dead adapter, subset converter or compatibility API remains. Upstream metadata cleanup, explicit empty-object properties, recursive-reference handling and unresolved-reference warning/retention are preserved. `KaskServerTool::input_schema` in `context_server_registry.rs` now returns normalized JSON directly (including valid empty-object schemas), pinned by `kask_server_tool_normalizes_schemas_without_losing_named_properties` through the actual managed-tool trait method. RunPod no longer implements the removed `tool_input_format`. RunPod streams use `ChatCompletionEventMapper`, preserving the D20/D36 shared behavior. **Fix:** the stripper treats `properties`/`patternProperties`/`$defs`/`definitions` values as name maps — it recurses into each named schema (stripping that schema's `description`/`default` keywords) but never strips the map's own keys. Keyword stripping elsewhere is unchanged, so the ~30-40% schema-size win is preserved. Genuine upstream bug benefiting every Zed user whose MCP tools have a `description` field — should be reported upstream to zed-industries/zed. Both `test_strip_verbose_property_fields_preserves_property_named_description` and `test_strip_verbose_property_fields_removes_defaults_and_descriptions` now exercise the live normalizer, not the helper alone. Coverage includes required nested criteria and optional descriptions, plus fields named `default`. Added `normalization_preserves_names_in_definitions_and_pattern_properties` covers modern/legacy definitions with resolved and unresolved references. The upstream sibling-keyword test now asserts `minLength` survives while descriptions are stripped. Historical red/green evidence is from 2026-09-02; the migrated normalizer tests passed in the 80-test `language_model_core` suite during merge recovery. |
| D48 | Model-discovery failure warns must log the error source chain | `crates/language_models/src/provider/open_ai_compatible.rs` (discovery-failure `log::warn!`) | Upstream's discovery-failure warn formatted the anyhow error with plain `{e}`, which prints only the top-level message — for transport failures that is reqwest's "error sending request for url (...)" with NO cause: connect refused vs DNS vs TLS vs timeout is invisible. Observed 2026-09-04: the DeepInfra discovery failure ("failed to send request: error sending request for url (https://api.deepinfra.com/v1/openai/models)") could not be root-caused from the log while the same host was reachable from curl — the missing source chain made the failure undiagnosable after the fact. **Fix:** format with `{e:#}` (anyhow's alternate Display includes the full source chain). The same instrumentation was applied kask-side to the RunPod provider's discovery warn (`runpod.rs`, already a D29 surface). **Why not kask-side?** The warn lives in the shared `open_ai_compatible` provider state used by every OpenAI-compatible provider (DeepInfra, OpenRouter-compatible entries, custom providers); there is no kask hook between the fetch task and the log call. Genuine upstream observability fix benefiting every Zed user debugging provider connectivity — should be reported upstream to zed-industries/zed. Pinned by `discovery_failure_warn_includes_error_source_chain` (source-structure pin, mermaid-precedent style: reads the file's own source and asserts the `{e:#}` form). |
| D51 | Stop the managed MCP runtime on session quit | `crates/zed/src/main.rs` (`wire_kask_mcp_shutdown`) | Operator-authorized T19 (2026-09-07). Register `on_app_quit` next to runtime construction and detach its subscription so it remains registered. The quit future drives `McpRuntime::shutdown_all` on the tokio handle before process exit, which otherwise skips Rust destructors. Lifecycle serialization is retained, with a terminal shutdown latch rejecting queued starts. A real-process RED exposed a second defect: rmcp's detached/graceful cleanup let a child survive after maps were cleared. `McpRuntime` now owns each child before handshake, gives rmcp only its pipes, and tracks kill-and-reap tasks that stop/shutdown await. Launch generations, connections/tools, keeper/supervisor tokens, reconnect stamps, and health failures are cleared. The added duplicate shutdown definition was removed after the operator observed E0592. Kask children already use `kill_on_drop(true)` in `start_connection`; upstream's separate `StdioTransport::Drop` also kills its child, but is not the Kask runtime's transport. Fn-pointer pin: `kask_wiring_symbols_exist`; runtime regression: `shutdown_all_clears_every_reconnect_path`. Validated: 17 runtime unit + 14 real-process integration tests, including `shutdown_all_kills_live_and_pending_children` (connected child, blocked discovery, repeated shutdown, rejected late startup). Observed RED for the leaked child and late-start resurrection before the repairs. After the initial timeout, the full Zed check and scoped lint passed in the operator-approved incremental window. No live application-quit smoke test is claimed; that remains an operator checkpoint. |
| D52 | Development-profile Clippy by default | `script/clippy` + `kask/scripts/build/check-build-profile.sh` | Operator-ratified 2026-09-08: ordinary lint runs must not force a separate release dependency build. Removes the wrapper's unconditional `--release`; Cargo now uses its dev default unless the caller explicitly supplies `--release` or `--profile`. Supersedes the wrapper's prior forced-release policy, not the release/install profiles (D46/D50). Package selection, jobs cap, sccache detection, `--all-targets`, `--all-features`, `--deny warnings`, failure propagation, and ancillary checks are unchanged. The interrupted three-package lint run logged 266 compilation and 657 check entries through the bridge's Zed dependency graph; no speedup is claimed without a timed run. Pinned by stub-Cargo execution in `check-build-profile.sh`: default scoped/workspace runs, explicit release and named profiles, exact lint flags, and nonzero exit propagation. Observed RED with the old forced-release argument, GREEN after removal; the pin launches no compiler. |
| D54 | Native `onto_anchor` tool — canonical fallback-ladder term resolution | `crates/agent/src/tools/onto_anchor_tool.rs` (new) + `crates/agent/src/tools.rs` (mod + re-export + `tools!` list) + `crates/agent/src/thread.rs` (`add_default_tools` construction) + `crates/agent/Cargo.toml` (`hkask-bridge-ontology.workspace` dep — the bridge is a leaf crate, serde/schemars only) + `assets/settings/default.json` (`onto_anchor: true` in the write and ask profile allowlists) + `crates/settings_ui/src/pages/tool_permissions_setup.rs` (EXCLUDED_TOOLS, lisp_eval precedent: read-only, no side effects) + `crates/agent/src/templates/system_prompt.hbs` (the Tool Use anchoring bullet names the tool and the ladder; the identity clause in the opening) + `.agents/skills/onto-anchor/SKILL.md` (the canonical pattern's process surface) + `kask/crates/hkask-bridge-ontology/src/derived.rs` (the derived rung — recorded compositions, authority-cited) | Operator rulings 2026-09-10 (two): (1) ontological anchoring was prompt text with no mechanical capability — an agent instructed to "resolve against the bridge" with no tool to resolve with reasons from private associations and calls it resolution (observed: the net-margin incident); (2) the tool must follow the pre-existing anchoring pattern, not a parallel implementation. **Absorbs retired D53** (one feature, one seam): the professional-identity framing lives in the opening ("no professional works in a private language" — a physician reasons in ICD/SNOMED, an accountant in IFRS/GAAP) and the required-tool bullet in `## Tool Use`; the standalone `## Ontology-anchored reasoning (kask)` section D53 originally added was removed the same day it landed — de-ghettoized, because a third "(kask)" overlay reads as optional. The net-margin incident (a pre-interest formula wearing the net-margin label through two operator corrections and a 112-green suite) is the trigger of record; the 2026-08-29 FIBO purge (63 of 70 URIs fabricated) is the fabrication precedent. **Canonical design (redesigned same day to follow the pattern):** the tool walks the fallback ladder (P8.3, `axis.rs` + bridge root docs) in term-resolution form — the same walk the portfolio widget implements per metric (`hkask-portfolio-widget/src/view.rs`: IRR → FIBO rung 1; FIBO-less metrics → `sumo:Quantity` rung 3). Rungs: (1) domain supplements — the fixture-pinned registries (`fibo::ALL_TERMS`, `pko::ALL_TERMS`, `sepio::ALL_TERMS`, `golem::ALL_TERMS`, `sdmx::ALL_CONCEPTS`, `ml_schema::ALL_CONCEPTS`, `omc::ALL_CONCEPTS`, `schema_org::ALL_TERMS`, `rdf::ALL_TERMS`), exact on the full prefixed URI then case/separator-insensitive on the term name, never fuzzy; (2) derived concepts — `derived::DERIVED_CONCEPTS`, recorded compositions carrying identity + authority (operator rulings become durable anchors here; "net margin" resolves on this rung with identity "net income / revenue" and authority "operator ruling 2026-09-10"); (3) SUMO upper; (4) the 5W1H core. **Invariant: nothing is ever untagged** — there is no "unanchored" verdict; a core-rung anchor is real but coarse and carries the ruling path (the ruling lands in the derived registry and the term resolves there ever after). Dublin Core/BIBO is deliberately absent from the walk per the BIBO-deprecation ruling (2026-09-10: BIBO was tried and deprecated in favor of SUMO; the state axis types artifacts server-side, not terms). Stateless, registered alongside `lisp_eval`/`render_template` in `add_default_tools`; all three registration gates wired (profile allowlists, permissions-UI exclusion, no feature flag). Pinned by `domain_terms_resolve_on_rung_one`, `net_margin_resolves_on_the_derived_rung`, `upper_ontology_terms_resolve_on_rung_three`, `unmatched_terms_terminate_on_the_core_rung`, and `the_ladder_always_terminates_on_a_real_anchor` (onto_anchor_tool.rs). **Compile repair (2026-09-10, uncommitted):** `thread.rs` imports the registered `OntoAnchorTool`; output text fields own their strings to satisfy `AgentTool::Output`'s lifetime-independent deserialization contract and retain dynamically formatted input errors. The ladder and JSON shape are unchanged. Regression pins: `output_round_trips_through_owned_json` (all four rungs through `serde_json::from_value`) and `invalid_input_returns_an_owned_error` (actual tool execution with malformed input, error text retained after deserialization). |
| D55 | Deterministic test-profile load in the agent test harness `setup()` | `crates/agent/src/tests/mod.rs` (`setup` — `cx.run_until_parked()` after the `watch_settings` spawn) | `Thread::new` fixes `profile_id` from `AgentSettings.default_profile` at construction, and `setup()`'s settings load is async (`watch_settings` spawns a watcher task; the file content reaches the store only when the foreground executor polls it). When the thread is constructed before the watcher delivers the test profile, it is permanently on the default `write` profile, whose tool allowlist (`assets/settings/default.json`) omits every test-only tool — `enabled_tools` filters them all out (`is_tool_enabled` is must-be-listed: `tools.get(name) == Some(&true)`), and every tool call fails with "No tool named X exists". The race's resolution tracked the test prologue: the `init_test(cx)` + `always_allow_tools(cx)`-before-`setup` prologue lost it (every failing test had it), bare `setup` won it. Observed 2026-09-11 running the agent suite for the first time after the onto_anchor arc: five streaming/partial-input tests failed — `test_streaming_tool_completes_when_llm_stream_ends_without_final_input`, `test_non_streaming_tool_partial_input_then_retryable_error_flushes_canceled_message`, `test_streaming_tool_json_parse_error_is_forwarded_to_running_tool`, `test_streaming_tool_error_breaks_stream_loop_immediately` (upstream origin, f69ab1d04e), `test_streaming_tool_error_waits_for_prior_tools_to_complete` — while `test_non_streaming_tool_partial_input_then_max_tokens_gets_canceled_message` and both queued-message tests passed only because they never assert on tool results: they ran with a silently empty tool surface (probe-verified: `profile_id="write"`, `Request includes 0 tools`, "No tool named echo exists" tool results inside passing tests). **Fix:** `setup()` drives the watcher to completion before constructing the thread, so `profile_id` is deterministically `test-profile` and the test tools are always enabled. Pinned by the five tests above (all RED before, GREEN after) plus the previously-silent tests now exercising the real tool surface. |

Retired seams (numbers are never reused): **D4** — guard layer (`hkask-guard` crate removed in b89a28359e; the `RoleOverride` scanner's false positives blocked legitimate template rendering); **D10** — kask panel (redundant with the agent panel + curator threads; standalone viz views replaced by the D18 inline widgets); **D17, D19** — retired (kask-seam-audit); **D30** — local skill marketplace (deleted; skill sharing moved to the ABW cloud); **D38** — folded into D37 (same bug class — reasoning-model request guards — same fix pattern, two surfaces); **D49** — folded into D42 (D13's residual budget removal belongs under the uncapped-thinking ruling); **D50** — folded into D46 (explicitly "extends D46"); **D53** — folded into D54 (one anchoring feature, one seam — the prompt identity, the tool, and the skill).

## Other zed-kask-modified files (supporting D1–D55)

These files carry `// zed-kask:` comments but are supporting edits, not
primary divergence seams:

- `crates/cli/src/main.rs` — `zed-kask://` added to `URL_PREFIX` array.
- `crates/zed_actions/src/lib.rs` — `OpenApplicationUrl` action doc references `zed-kask://`.
- `crates/gpui/src/app.rs` — `register_url_scheme` doc example uses `zed-kask`.
- `crates/terminal/src/alacritty/hyperlinks.rs` — `zed-kask://` added to URL regex (hardcoded — terminal doesn't depend on `paths` crate).
- `crates/agent/src/agent.rs` — **thread persistence (D28, 2026-09-10 merge):** `Session` holds a weak ACP thread and retains child ACP threads; release observers replace manual reference counts. `release_session` checks the released entity id, enqueues its final draft before removing project state, and detaches the coalescing save worker. The worker drains the last payload after its watch sender closes and retains only a `WeakEntity<NativeAgent>` for Kask's `ThreadSaveFailed` event on database-connect/write errors; failed delivery is logged, not silently dropped. The existing `ThreadView` subscription remains the UI sink. Quit flushing is retained. Added `test_save_worker_reports_database_failure_after_sender_closes`; retained upstream release/subagent-leak, save-burst, last-owner and quit-flush tests. The final-save failure test and both release/session-subagent tests passed during merge recovery. No live application-shutdown smoke test is claimed.
- `crates/agent/src/tools/list_mcp_tools_tool.rs` — the D44 discovery meta-tool (`list_mcp_tools`): enumerates the registered MCP surface on demand, the pull mechanism that replaced the removed LazyToolRouter's per-turn push.
- `crates/agent/src/kask_thread_state.rs` — **KaskThreadState**: all kask-specific per-thread state (D2/D6/D25) grouped into one struct so upstream `Thread`'s struct definition stays clean and upstream rebases touch one field init instead of nine. Methods on this struct encapsulate the kask-specific turn-loop behaviors that were previously inline in `Thread`'s methods (`render_system_prompt`, `enabled_tools`, `handle_tool_use_event`, `handle_completion_event`, `flush_pending_message`, `cancel`, `run_turn_internal`, `new_subagent`). See `kask/docs/plans/thread-hooks-refactor-2026-08-26.md` for the full refactoring plan. Pinned by `test_last_completion_truncated_distinguishes_max_tokens_from_cancel`, `test_system_prompt_override_bypasses_template`, `test_cached_filtered_context_reuses_on_unchanged_inputs`, `test_tool_retry_tracker_integration_in_thread` (the Phase 0 pinning tests that survive the D6 curator-gate removal — `test_curator_memory_edit_tool_classification` and `test_curator_memory_edit_tools_gated_to_curator_threads` were deleted with the gate), plus `context_injector_dispatches_by_agent_id` (D6 dispatch-by-agent_id), `test_mcp_server_scope_excludes_out_of_scope_servers` (D2 per-tab MCP scoping), and `test_system_prompt_names_tools_hidden_by_profile` (D44 visibility marker).
- `crates/agent/src/templates.rs` (and `system_prompt.hbs`) — **full structure + divergence catalogue: [`kask/docs/architecture/skills-and-composition.md`](kask/docs/architecture/skills-and-composition.md) Part I** (includes the rebase procedure for this file and the four prompt surfaces — base template plus the Curator/swarm/kanban overlays; formerly `AGENT_SYSTEM_PROMPT.md`, folded 2026-08-28). The Division of Responsibilities section and its bullet amendments are D40, designed against [`kask/docs/architecture/functional-interaction-spec.md`](kask/docs/architecture/functional-interaction-spec.md). Agent Skills system-prompt section follows upstream wording (body retrieval via `skill` tool). Additional `system_prompt.hbs` divergences in shared upstream sections: (1) a loop-termination guardrail added to `## Task Execution` ("If a tool loop repeats without measurable progress… stop, summarize what you tried, and ask the user rather than continuing indefinitely"); (2) the mermaid diagram-type list names the exact directives merman requires (`sankey-beta`, `xychart-beta`, `architecture-beta`, `radar-beta`) rather than bare forms the renderer drops, keeps `kanban` (it **is** a supported mermaid directive — see `markdown::mermaid::SUPPORTED_PREFIXES`), and separately notes that the `graph`/`media`/`portfolio`/`scenarios` fenced blocks are kask viz widgets, plus that a ```kanban fenced block is a widget while `kanban` inside a ```mermaid block is a diagram. The prompt is pinned against the renderer allowlist by `test_system_prompt_advertises_every_supported_diagram_type` in `crates/markdown/src/mermaid.rs`, which replaces the former "if updating this list, also update the system prompt!" manual-sync comment; (3) the `## Session Context` (`static_context`) block is a sibling of the `{{#if (or user_agents_md has_rules)}}` custom-instructions guard, not nested inside it (see D2). Pinned by `templates::tests::test_system_prompt_contains_loop_termination_guardrail` (which also asserts the guardrail states a concrete iteration count, not a vague quantifier), `..._mermaid_list_uses_renderer_directives`, and `..._renders_session_context_without_rules_or_agents_md`.
- `crates/agent_ui/src/conversation_view/thread_view.rs` — `render_skill_loading_issues` surfaces `LoadFailed`, `DescriptionTooLong`, and `CatalogBudgetExceeded` (the former disabling of description-length + catalog-budget issues was deliberately reversed in d2f8d76b3a, 2026-08-20, "Restore skill loading warnings" — the disabled code paths were removed).
- `crates/agent_skills/agent_skills.rs` — `global_skills_dir()` relocated to the kask data root via `set_global_skills_dir_override` hook (D28 — Standardized Artifact Storage). Legacy migration from `~/.agents/skills/` removed (pre-release). `GLOBAL_SKILLS_DIR_DISPLAY` updated. `SkillSource::Global` docs updated. `load_marketplace_skills` and `SkillSource::Public` removed (skill marketplace retired).
- `crates/settings_ui/src/pages/skills_setup.rs` — skill listing/editing UI. `pages/skill_creator.rs` retains the URL-import removal from `fe2bfc050c` (D30); the 2026-09-10 merge accepts upstream Unicode description counting but does not resurrect importer helpers or tests.
- `crates/zed/src/zed/open_listener.rs` — `zed-kask://` URL scheme parsing + tests.
- `crates/windows_resources/src/windows_resources.rs` — dev-channel window title `Kask` (D7 rebrand).
- `crates/gpui_tokio/src/gpui_tokio.rs` — `Tokio::handle_async` helper (get the tokio handle from `AsyncApp`); 2 call sites in `main.rs` deferred wiring.
- `crates/prompt_store/src/prompts.rs` + `crates/agent/src/agent.rs` + `crates/agent/src/thread.rs` — `RuleFrontmatter` (Cline-compatible `alwaysApply`/`globs` rules frontmatter) + `filter_conditional_rules` enforcement on the prompt-render path.
- `crates/agent/src/tools/terminal_tool.rs` — truncation spillover file (full output saved to temp file, path returned for `read_file`), head/tail overlap fix in `select_terminal_output_lines` (upstream candidate), relaxed shell-substitution doc wording (single-quoted `${...}` is safe).
- `crates/agent/src/tools/context_server_registry.rs` + `crates/agent/src/agent.rs` + `crates/zed/src/main.rs` + `crates/project/src/context_server_store.rs` (the store half: non-auth transport-death state moves + the public self-heal trigger) — (existing, previously undocumented here) on-demand server self-healing restart in `ContextServerTool::run` + timeout-vs-transport-death retry classification. **Kask merge removal semantics (2026-09-08):** `reload_kask_tools` now REMOVES previously-kask-sourced servers the source no longer lists (tracked via `kask_server_ids` on the registry; the merge previously only ever inserted, so an unloaded kask server — D45 toggle → `McpRuntime::stop_server` → dropped from the runtime's registered set — kept ghost tools on the agent surface forever). Store-registered servers are untouched; only ids the previous pass sourced from kask are eligible. The merge is extracted into `merge_kask_tool_descriptors` (testable without the store machinery) and emits `ToolsChanged` only when the visible surface changed (no subscriber churn on steady-state poll ticks). Pinned by `kask_merge_removes_servers_absent_from_the_source` (context_server_registry.rs — insert, removal, store-server survival, steady-state no-op). **T-V1 (2026-08-28): agent-path MCP tool outcome recording.** `ContextServerTool::run` wraps `run_inner` to record every agent-path MCP tool outcome (server name, tool name, success, error kind) via the process-global `agent::record_mcp_tool_outcome` hook (re-settable `Mutex` slot, `set_mcp_tool_outcome_recorder`), wired in `main.rs` to the shared `RegulationLedger::record_outcome` on the GPUI-global tokio runtime. **Error-path collapse (2026-08-28, second pass):** all kask MCP servers now return `Result<String, McpToolError>` and rmcp sets `is_error` natively with the typed kind in `structured_content`; `run_inner`'s `is_error` branch carries that value in the error output's `raw_output` so outcome recording and the retry tracker (`is_authorization_error`) classify structurally — the in-band envelope sniffing and `[kind] ` prefix parsing are deleted. **Invalid-JSON classification (2026-09-09):** `run_inner` consumes the `ToolInput` payload stream directly (`ToolInput::next`) instead of `recv`, so the typed `InvalidJson` payload — the model's tool-call arguments were unparseable (the truncation signature, see `Thread::handle_tool_use_json_parse_error_event`) — returns a typed `invalid_argument` envelope (`invalid_json_tool_output`) instead of raw error text; `mcp_run_outcome` classifies it structurally and the ledger excludes it from the tool-reliability success rate (`hkask_types::tool_response::is_not_tool_fault_kind`) while keeping it in the per-kind breakdown. The LLM-visible parse-error message is unchanged (pinned end-to-end by `test_streaming_tool_json_parse_error_is_forwarded_to_running_tool`). **rmcp deserialization-rejection classification (2026-09-09):** `mcp_run_outcome`'s untyped fallback routes through `hkask_types::tool_response::error_kind_from_display`, which classifies rmcp's macro-layer rejections ("failed to deserialize parameters: …") as `invalid_argument` — the third model-caused class (schema-invalid args rejected below the kask typed-error surface; live-observed via the curator_memory_recall missing-`entity` probe), excluded from the reliability rate and visible in the per-kind breakdown. Pinned by `error_kind_from_display_classifies_rmcp_deserialization_rejection` + `test_mcp_run_outcome_untyped_deserialization_rejection_classifies_invalid_argument`. **Structural display hints:** `run_inner` pushes `display_hint`/`display_hints` fenced blocks as tool content so the D18 media renderer shows them deterministically (no model cooperation). Pinned by `agent::internal_tests::mcp_outcome_recorder_records_and_is_replaceable` + `context_server_registry::tests::{test_mcp_run_outcome_maps_success, test_mcp_run_outcome_maps_error_text, test_mcp_run_outcome_empty_error_text_falls_back, test_mcp_run_outcome_extracts_typed_kind, test_invalid_json_input_classifies_as_invalid_argument}`. The kask tool source (single spawn authority) is pinned by `kask_tool_source_hook_is_settable_replaceable_and_absent_when_unwired` + `kask_server_tool_dispatches_through_kask_tool_source`; the unwired-source degradation is surfaced (2026-08-30): `reload_kask_tools` warns once — a latch that re-arms when the source is wired, so the startup window produces at most one line — naming the startup-ordering caveat, pinned by `kask_tool_source_unwired_warn_latch_fires_once_and_rearms`; **startup-race poll (2026-08-30):** the registry re-reads the source on a 2s GPUI-native timer (`KASK_TOOL_SOURCE_POLL_INTERVAL`) — kask servers register with the governed runtime, not the ContextServerStore, so their late registration never fires a store event, and a registry created during window restore (before the deferred MCP launch registers tools) otherwise stays empty for the whole app session (observed live 2026-08-30: a fresh agent-panel session had zero kask tools until a store event was fired by hand). The merge re-inserts every entry so the wrapped descriptors stay fresh across kask server restarts, but emits `ToolsChanged` only when the visible tool-name surface changed. Pinned by `agent::tests::test_kask_tools_surface_when_source_populates_after_registry_creation` (tests/mod.rs — the shared-slot leak hazard the in-file NOT-covered note once cited is handled there with a distinctive server id, a one-`run_until_parked` populated window, and an empty-source reset); the timeout-vs-transport-death retry classification by `timeout_and_transport_death_classify_into_distinct_retry_verdicts` + `test_mcp_tool_timeout_does_not_retry` (the timeout half of the self-heal path, full-path: exactly one CallTool, no restart-and-retry). **T15 (channel a) advice-apply bridge (2026-09-09):** the same `run` wrapper fires `agent::record_operator_feedback(skill_id, true, None)` when a successful `curator_advice_mark_applied` response names a skill — the operator confirming application of a skill's recommendation IS acceptance feedback (lora-training: "the operator reacts to a recommendation"). The decision is extracted into `advice_apply_feedback_skill` so it is pinnable without an MCP-server fixture (the `mcp_run_outcome` precedent); the success path's `raw_output` is the response text as a JSON *string* (the `{"content": ...}` envelope), so the skill id is read through `hkask_types::tool_response::parse_tool_response` — the first committed version read `raw_output` as an object and structurally never fired (`Value::get` on a string is always `None`; observed RED on the real response shape, fixed with the envelope parse). Pinned by `test_advice_apply_bridge_fires_only_on_skill_naming_success` (fires on success+skill; never on absent skill, failure, other tools, or unparsable text).
- `crates/agent/src/tools.rs` — `deserialize_optional_u64_from_maybe_string` (models sometimes send `timeout_ms` as a string).
- `crates/agent_ui/src/agent_panel.rs` — eager `SkillIndex` population + `create_thread_with_options` returns the agent used (Curator support, D2).
- `crates/agent_ui/src/conversation_view.rs` — `render_agent_markdown` wires `.media_block_renderer(hkask_viz_core::block_renderer())` for D18 (`media and `graph fenced block rendering); the `hkask-viz-core` registry composes the media + graph widget renderers.
- `crates/language_model/src/language_model.rs` + `crates/language_model/src/registry.rs` — `api_url()` accessor.
- `crates/open_ai/src/list_models.rs` — generic `/v1/models` discovery for OpenAI-compatible providers (D12 support).
- `crates/zed/src/main.rs` (beyond the composition-root blocks) — kask MCP server auto-launch, `sync_kask_mcp_servers` re-sync when the inference socket becomes available. **Single spawn authority (2026-08-29):** kask servers are no longer registered with zed's per-project `ContextServerStore` — `KaskMcpDescriptor` and the per-project spawn path are deleted. The governed `McpRuntime` owns every kask server process; the agent's tool surface routes through `agent::set_kask_tool_source` (a process-global `KaskToolSource` wired in `main.rs`, cache refreshed by a background poll of `McpRuntime::registered_servers`). `sync_kask_mcp_servers` now only defensively unregisters stale descriptors and removes raw `context_servers` entries for kask IDs (the namespace guard — a raw entry would still spawn a keyless instance through the generic store path). The `.env` file loading has been fully removed — the keychain (zed's `CredentialsProvider`: data-service keys under `kask://credentials/<key>`, inference-provider keys at their provider `api_url` slots, see D5) is the single source of truth for API keys. `sync_kask_mcp_servers` also removes raw `context_servers` settings entries whose IDs match built-in kask servers: `ContextServerStore::maintain_servers` merges registry descriptors with `or_insert`, so a settings entry silently shadows the managed registration (the only path that composes env via `build_mcp_server_env`) — a stale `env: {}` entry shipped a keyless, socketless server to the agent's tools with no warning. Removals summarize in one warn per pass (count + IDs — a per-entry warn made every startup emit a wall of near-identical lines); the write is self-terminating (next observer pass is a no-op; D32's no-op-write skip covers the boundary). Removal logic pinned by `kask_bridge::mcp_servers::tests::{remove_shadowing_context_server_entries_removes_builtins_and_keeps_others, remove_shadowing_context_server_entries_is_noop_without_builtins}`. **Load/unload (D45, 2026-08-31):** `sync_kask_mcp_runtime_servers` now also handles `kask.mcp` load/unload toggles — it computes the load set (same expression as the launch loop) and stops/starts servers through the runtime's own primitives (`stop_server` / `register_server` + `start_server_with_env`), gated on a launch-pass-complete latch set at the end of the deferred launch loop; see D45 for the full mechanism. **Logging (2026-08-29):** upstream logs to stdout only when stdout is a PTY and to the log file otherwise; zed-kask always initializes the file sink and additionally mirrors to stdout on a PTY — post-hoc debugging of live defects (observer wiring, MCP restarts, credential resolution) must not depend on terminal scrollback. The settings-reactivity investigation was blocked because warns went to a scrolled-away PTY and no `Zed-Kask.log` existed. Composition-root code; pinned by review + the DIVERGENCE note (no harness reaches `main()`'s logging init).
- `crates/client/src/zed_urls.rs` — shared-session URL uses `ZED_URL_SCHEME` (D7).
- `crates/zed/src/reliability.rs` — 2026-09-10 merge retains upstream's weak `ProjectRegistry` for orphan-project diagnostics alongside Kask's existing fleet-health polling. `start_context_server_health_polling` + `classify_fleet_health` + `context_server_fleet_observable`: polls all open projects' `ContextServerStore`s every 10s (`read_context_server_statuses`), merges them with the governed `McpRuntime`'s live connections (`merge_fleet_health` — a server is healthy if `Running` in either path; the total is the union, no double-counting), and feeds a health snapshot to the cybernetics loop's `ContextServerHealthSensor` via `BridgeContextServerHealthSource`. Only `Starting`, `Running`, and `Error` count toward the fleet total; `Stopped` and OAuth-waiting states (`AuthRequired`, `ClientSecretRequired`, `Authenticating`) are excluded — they are legitimate non-running states, not fleet degradation. Counting them produced constant false-positive `ContextServerFleetDegraded` alerts. The pre-login gate (`context_server_fleet_observable`, suppressing the snapshot while `Client::user_id()` is `None`) is **restored (2026-08-30)**: it was removed with the single-spawn-authority change on the theory that the kask servers it guarded against had left `ContextServerStore`, but code analysis showed `maintain_servers` has no login gating — user-configured servers keep the storm path open (a server legitimately sitting in `Starting` pre-login counts toward the fleet total and fires `ContextServerFleetDegraded` every tick). Pinned by `reliability::tests::{all_running_is_fully_healthy, starting_counts_toward_total_but_not_healthy, error_counts_toward_total_but_not_healthy, stopped_servers_are_excluded_from_total, oauth_waiting_states_are_excluded_from_total, all_stopped_produces_zero_total_no_alert, all_starting_signals_degradation, mixed_real_and_excluded_states, empty_slice_is_zero_zero, pre_login_fleet_is_unobservable, resolved_user_opens_the_gate}` (11 tests).
- `crates/agent/src/tool_retry_tracker.rs` — **tool retry death-spiral guard** (hard enforcement in the agent loop): two tracking dimensions — per-input `(tool_name, input_hash) → failure_count` (identical retries) and per-tool consecutive failures (trivially different inputs) — after 3 failures the result carries a warning with the Bayesian success probability `1/(N+2)` and a switch-tools directive; after 5 the tool hard-refuses (per-tool override: `skill` at 12 — its multi-step PDCA cascade legitimately retries; `skill_tool_uses_override_hard_cap`). `is_authorization_error` skips `permission_denied` (a denied call cannot succeed on identical retry — counting it produces bogus "switch tools" statistics). Dispatched from `handle_tool_use_event` → `run_tool` for every tool (built-in + MCP); lives on `Thread` (per-thread, never persisted). Pinned by the tracker's unit tests + `test_tool_retry_tracker_integration_in_thread` (thread integration).
- `crates/language_model/src/api_key.rs` — **API-key env-var lock removed**: upstream locks `ApiKeyState::store` while the key's env var is set (the UI cannot update the key); zed-kask removes the lock — the keychain is the single source of truth (D5), and a UI-entered key must win over a stale env value (`load_status` becomes `SystemKeychain`, the next `load_if_needed` early-returns with the UI key). Pinned by `store_overrides_an_env_var_key`.
- `crates/collab/src/main.rs` + `crates/collab/tests/integration/db_tests.rs` — **local SQLite bootstrap schema**: for local dev the collab database starts empty; zed-kask applies a bootstrap schema guarded for idempotence (the bootstrap SQL uses `CREATE TABLE "users"` without `IF NOT EXISTS` — a second `collab serve` against the same file would crash; the guard skips re-application when the `users` table exists). Pinned by `setup_app_database_is_idempotent_for_sqlite`. The postgres test variants require a live Postgres (skipped without it).
- `crates/zed_credentials_provider/src/zed_credentials_provider.rs` — **credentials provider is always the OS-keychain one**: upstream uses a file-backed development provider in dev mode; zed-kask's release channel is always "dev", so an upstream revert would silently route every `kask://credentials/*` write to a JSON file while `build_mcp_server_env` keeps reading the OS keychain — every API key invisible to MCP servers while the UI reports healthy. Pinned by `new_always_constructs_the_keychain_provider`.
- `crates/remote_server/src/remote_editing_tests.rs` — test accommodations only: telemetry metrics disabled by default in the remote-editing test harness; skills execute via body injection (D1's mechanism) in the remote tests.
- `crates/marketplace_ui_common/src/marketplace_ui_common.rs` — the marketplace chrome crate survives the D30 marketplace retirement: its shared card/list UI is reused by kask catalog pages (the in-file marker documents why the crate is still compiled).

## hKask workspace members (additive — upstream never touches)

The following are added to the root `Cargo.toml` `[workspace.members]` array.
Upstream merges never conflict with these paths.

- `kask/crates/` — hKask crates (`hkask-types`, `hkask-storage`, `hkask-memory`, `hkask-regulation`, `hkask-tool-port`, `hkask-keystore`, `hkask-ledger`, `hkask-event-store`, `hkask-mcp`, `hkask-mcp-server`, `hkask-inference`, `hkask-condenser`, `hkask-bridge-ontology`, `hkask-services-core`, `hkask-email`, `hkask-forecast`, `hkask-lisp`) plus the `kask_bridge` bidirectional seam (D8, zed-kask-side). The `hkask-{media,graph}-widget` and `hkask-viz-core` viz crates live under `crates/` (zed-kask-side, see D18) because they render GPUI elements and must depend on `gpui`/`theme`, which §13.1 forbids for hKask crates.
- `kask/mcp-servers/` — 11 MCP server crates (`hkask-mcp-{companies,corpus,curator,kata-kanban,media,portfolio,prediction-markets,research,scenarios,swarm,training}`). The condenser library crate (`kask/crates/hkask-condenser`) provides in-process thread condensation via `kask_bridge::BridgeThreadCondenser`.
- `crates/marketplace_ui_common` — shared catalog-page chrome (`MarketplaceCard`, search bar, empty state) used by `swarm_panel` and `kanban_panel`

## Governing invariant (§13.1)

**hKask crates NEVER depend on zed-kask crates; zed-kask depends on hKask.**
The sole bidirectional seam is `kask_bridge` (D8), which lives under
`kask/crates/` and depends on both sides. Enforced by
`kask/scripts/check-hkask-no-zed-deps.sh` (wired into CI).

## Upstream-sync runbook

1. `git fetch upstream && git merge upstream/main`
2. Conflicts will only appear in:
   - The D-seam files listed above (D1–D55; D4, D10, D17, D19, D30, D38, D49, D50 and D53 are retired — numbers are never reused)
   - `[workspace.members]` / `[workspace.dependencies]` in root `Cargo.toml`
     and `crates/zed/Cargo.toml` (kask deps + upstream's crate renames, e.g.
     `csv_preview` → `tabular_data_preview`).
   - `Cargo.lock` — regenerate from the merged manifests (do not hand-merge;
     `cargo check --workspace` rewrites it).
   - `kask/deny.toml` `allow-git` list — upstream may adopt new git forks
     (e.g. `zed-industries/async-tar`, `tree-sitter/tree-sitter`). Add the new
     source URLs; verify with `cargo deny --config kask/deny.toml check`.
   - `typos.toml` — upstream may add new identifiers; merge into the kask
     allowlist (kask's is the superset).
   - Modify/delete conflicts — upstream may restore files zed-kask deliberately
     deleted under D7/D16 (icons, `.desktop` templates, bundle scripts, release
     workflows, community-bot workflows). Resolve by re-deleting; verify with
     `bash kask/scripts/build/check-zed-isolation.sh`.
3. Everything under `kask/` is additive → never conflicts. (Exception: kask
   tracing targets using the `reg.*` prefix must be registered in
   `CANONICAL_NAMESPACES` or retargeted to `hkask.*` — the reg-canonical gate
   scans `kask/crates/` and `kask/mcp-servers/`. Performative log targets
   belong on `hkask.*`, not `reg.*`.)
4. After resolving, run `bash kask/scripts/check-hkask-no-zed-deps.sh` to
   verify the §13.1 invariant still holds.
5. Run `cargo check -p kask_bridge -p hkask-types -p hkask-mcp-server` to
   verify the bridge + foundation still compile.
6. Run `bash kask/scripts/build/check-zed-isolation.sh` — verifies deleted
   upstream surfaces (icons, `.desktop` templates, bundle scripts, release
   workflows) were not silently restored by the merge.
7. Run `./script/clippy` (per `.rules` build guidelines — use this instead of
   `cargo clippy`; runs under `--deny warnings`). A `rust-toolchain.toml` bump
   may promote previously-allowed lints to denied — clippy catches these.
8. Run the full verification gate:
   - `cargo fmt --all -- --check`
   - `cargo nextest run --no-fail-fast -p 'hkask-*' -p kask_bridge`
   - `cargo deny --manifest-path Cargo.toml --config kask/deny.toml check`
   - `cargo machete`
   - `typos --config typos.toml`
   - `buf lint crates/proto/proto` && `buf format --exit-code crates/proto/proto`
   - `cd kask && bash scripts/check-unused-deps.sh` (nightly)
   - All `kask/scripts/check-*-selftest.sh` scripts
