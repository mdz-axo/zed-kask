# DIVERGENCE.md — zed-kask fork divergence from upstream Zed

> **Purpose:** the headline doc for upstream-sync conflict resolution. Every
> `git fetch upstream && git merge upstream/main` consults this file to know
> which crates/files are divergent and why. Referenced from
> `kask/docs/architecture/zed-host-architecture-plan.md` (§13.4).
>
> **Rule:** everything under `kask/` is ours (additive; upstream never touches
> here → near-zero merge conflict). Everything else tracks upstream; the only
> divergences are the D-seams listed below + the `[workspace.members]` /
> `[workspace.dependencies]` arrays in the root `Cargo.toml`.

## The divergence surface (D1–D36)

Every hKask integration maps to a named, isolated change in zed-kask. These
are the _only_ edits to zed-kask's tree outside `kask/`. Any hKask behavior
that would require touching other Zed crates is a smell — push the logic into
an hKask crate behind one of these seams instead.

| D   | Surface                                                                                 | zed-kask crate / file                                                                                                                                                                                                                                                                                                                                | What's wired                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --- | --------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | Skill execution (`lisp_eval` + `render_template` tools)                             | `crates/agent/src/tools/skill_tool.rs` + `crates/agent/src/tools/lisp_eval_tool.rs` + `crates/agent/src/tools/render_template_tool.rs` + `crates/agent_skills/agent_skills.rs` + `crates/zed/src/main.rs`                                                                                                                           | `SkillTool` follows **upstream Zed**: reads the `SKILL.md` body from disk via `agent_skills::read_skill_body` and injects it via `render_skill_envelope`. The model reads the body and follows the instructions. `SkillTool::new(skills, fs)` matches the upstream constructor. **`lisp_eval` tool:** built-in (`crates/agent/src/tools/lisp_eval_tool.rs`), wraps `hkask_lisp::eval_sandboxed_with_budget`. Registered in `add_default_tools`. `hkask-lisp` workspace dependency in `crates/agent/Cargo.toml`. **`render_template` tool:** built-in (`crates/agent/src/tools/render_template_tool.rs`), renders prompt templates from `kask/registry/templates/` with `minijinja`. Path traversal protection via `canonicalize` + `starts_with`. Template base path wired via `agent::set_template_base_path()` (OnceLock) in `main.rs` at startup. `minijinja` workspace dependency in `crates/agent/Cargo.toml`. Registered in `add_default_tools` (alongside `lisp_eval`). **Production template seeding:** `agent_skills::seed_templates` (build.rs embeds `kask/registry/templates/*.j2` via `include_str!`) writes the compiled-in templates to `{kask_data_dir}/skills/registry/templates/` at startup, always (dev and production), so MCP servers (corpus, training) that read `HKASK_TEMPLATE_ROOT` find them regardless of CWD. `kask_bridge::mcp_env()` always emits `HKASK_TEMPLATE_ROOT` — when the operator hasn't overridden `corpus.template_root`, it resolves to `{data_dir}/skills/registry/`; when overridden, it uses the operator's value. Pinned by `test_seed_templates_writes_all_shipped_templates_to_disk`, `test_seed_templates_overwrites_existing_files` (agent_skills), `mcp_env_always_emits_template_root`, `mcp_env_template_root_follows_data_dir_override`, `mcp_env_template_root_override_emits_user_value` (kask_bridge). **PDCA:** model-coordinated — the SKILL.md body describes convergence criteria; the model uses `lisp_eval` for deterministic checks and `render_template` for structured prompt scaffolding. **Core-skill frontmatter enforcement** (retained): `parse_skill_file_content_for_loading` refuses `core: true` on non-`CORE_SKILL_NAMES` skills. Marketplace-installed skills (`SkillSource::Public`) and `load_marketplace_skills` removed (skill marketplace retired). D28 storage remains. Pinned by `test_skill_tool_returns_content`, `test_system_prompt_skills_section_describes_body_retrieval`, `test_refuse_skill_catalog_read_allows_skill_md`. |
| D2  | Curator agent                                                                           | `crates/agent/src/agent.rs` + `crates/agent_ui/src/agent_ui.rs` + `crates/agent/src/kask_thread_state.rs` + `crates/hkask-steer/`                                                                                                                                                      | `Agent::Curator` variant; `CURATOR_AGENT_ID`; selectable in Agent Panel. The Curator overlay's `CURATOR_STATIC_CONTEXT` (`crates/agent/src/curator_agent_server.rs`) is appended via `Thread::set_static_context` (delegates to `KaskThreadState::set_static_context`) and rendered by the `## Session Context` block in `system_prompt.hbs`; that block is a **sibling** of the `{{#if (or user_agents_md has_rules)}}` custom-instructions guard (not nested inside it), so it renders even for projects with no `.rules` and no personal `AGENTS.md` (regression: previously nested → silently dropped). Pinned by `test_system_prompt_renders_session_context_without_rules_or_agents_md` in `templates.rs` and `test_system_prompt_override_bypasses_template` in `thread.rs`. **`with_extra_static_context` is zed-kask's mode mechanism.** Upstream Zed has no modes; Cline/Roo deliver phase constraint via a runtime mode token. zed-kask instead composes a per-context overlay onto the base prompt through this one channel: `CURATOR_STATIC_CONTEXT` (~1.1 KB) plus, in Steer, `swarm_panel::steer_system_prompt` (~9.7 KB) or `kanban_panel::steer_system_prompt` (~2.2 KB), each also `with_mcp_server_scope`-d to one MCP server so the tool surface matches what the overlay advertises. Two consequences follow, and both have bitten: (1) an overlay is captured **once** at conversation construction, so any panel state it bakes in must invalidate the conversation on change — the shared `hkask_steer::SteerSurface` ( `crates/hkask-steer/` ) owns this lifecycle (`ensure` / `invalidate`); `set_swarm_mode` and `select_board` call `steer.invalidate()` for this reason; (2) an overlay that advertises tool names must pin them against the server's actual tool list, or a rename degrades to "tool not found" at dispatch — `hkask_steer::verify_tool_advertisement` checks the prompt against each server's build.rs-generated `TOOL_NAMES` (`hkask_mcp_swarm::TOOL_NAMES`, `hkask_mcp_kata_kanban::TOOL_NAMES`), and the panels' prompt-token tests are the CI enforcement. Treat these as the two standing obligations for any new overlay. **Refactored 2026-08-27:** the Steer conversation lifecycle (previously hand-rolled per panel as `ensure_steer_conversation` + `steer_conversation`/`steer_connection_store` fields) is shared in `crates/hkask-steer/`; the hand-mirrored kanban tool lists (`ADVERTISED_KANBAN_TOOLS`, `parse::KANBAN_TOOLS`) are deleted in favor of the kanban server's generated `TOOL_NAMES`. **One-call helper added 2026-08-28:** `hkask_steer::ensure_steer` combines `verify_tool_advertisement` + `SteerSurface::ensure`; panels pass a `SteerContext` + prefixes and the helper bakes the prompt, lands the view, and caches it. It is the panel-facing API; the raw `ensure` remains exposed for tests with a stub factory. **Refactored 2026-08-26:** `agent_static_context`, `system_prompt_override`, and `mcp_server_scope` moved from `Thread` fields to `KaskThreadState` (`crates/agent/src/kask_thread_state.rs`). The turn-loop call sites use `self.kask.static_context()` / `self.kask.system_prompt_override()` / `self.kask.mcp_server_in_scope()` etc. See `kask/docs/plans/thread-hooks-refactor-2026-08-26.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D3  | hKask tools in-process                                                                  | `crates/zed/src/main.rs`                                                                                                                                                                                                                                                                                                                             | `McpRuntime` implements `ToolPort` directly (call metering + `reg.tool.*` spans) and is passed wherever a `ToolPort` is needed — no bridge adapter. MCP servers run as child processes (stdio). Daemon transport deleted; identity from `ServerContext.webid` (`HKASK_WEBID`). **`McpRuntime::invoke` performs NO per-call authorization.** The former capability-match gate was deleted 2026-08-12 (RR-0056): all three production mint sites derived the `DelegationToken`'s `resource_id` from the same tool name they passed to `invoke`, so `is_valid_for` compared a caller-supplied value against itself and could not deny — it ran on every tool call and denied nothing while being advertised here as "the enforced gate." `DelegationToken`, `panel_default_token`, `capabilities_match`, `capability_from_server_id`, and `ToolPortError::CapabilityDenied` are gone; `invoke` now takes `agent: WebID` (accounting identity, not a credential). Authority lives at the boundaries whose list the caller does not choose: the inference IPC per-request `tool_allowlist` (fail-closed, D8), the swarm card `mcp_tools` allowlist, and the per-server MCP env/credential allowlists (RR-0038). Previously pinned by `tests/invoke_gate.rs::{unregistered_agent_is_auto_registered_not_denied, exhausted_ceiling_trips_the_runaway_breaker, no_governance_dispatches_unmetered, unreachable_server_reports_unavailable_not_failed, only_unavailable_is_retryable, interrupted_is_never_auto_retried, interrupted_and_unavailable_are_distinguishable, unknown_tool_is_not_found_not_unavailable}` (public-API metering + retry-classification tests); that file and the inline `reconnect_path_tests` module were both deleted with the condenser teardown and **not restored** — `runtime.rs` has no `#[cfg(test)]` section and the `kask/crates/hkask-mcp/tests/` directory no longer exists. **Restored 2026-08-24:** the metering + retry-classification behavior is now re-pinned by `runtime.rs::metering_tests::{unregistered_agent_is_auto_registered_not_denied, exhausted_ceiling_trips_the_runaway_breaker, no_governance_dispatches_unmetered, only_unavailable_is_retryable, interrupted_is_never_auto_retried, interrupted_and_unavailable_are_distinguishable, unknown_tool_is_not_found_not_unavailable}` (7 inline tests). Settings-change restart: governed McpRuntime instances are started once at login; `sync_kask_mcp_runtime_servers` (a `SettingsStore` observer) diffs each server's computed env (`kask_server_env`) against the launch baseline and restarts changed servers via `McpRuntime::stop_server` + `start_server_with_env`, so e.g. a `kask.swarm.mode` toggle re-routes the governed path; the restart logs the *changed env keys* (never values — several are credentials) because a restart tears down live connections and fails every in-flight panel call, so "env changed" alone could not distinguish a deliberate toggle from a startup-ordering artifact. **Connections self-heal.** A dead connection used to be sticky and unrecoverable: the keeper task's `running.waiting()` arm exited without removing the entry, `start_server_with_env` early-returned on a presence-based `connections.contains_key` check, and `get_peer` handed out the corpse forever — so every call returned `Transport closed` until an operator changed a setting. Four mechanisms now close that loop (`kask/crates/hkask-mcp/src/runtime.rs`): (1) the keeper task reaps its own connection when the service loop exits on its own, guarded by a generation stamp so a late-exiting keeper cannot remove a healthy replacement installed by a reconnect; (2) `get_peer` filters peers whose `is_transport_closed()` is already true, covering the window before the keeper is scheduled; (3) `start_server_with_env` records a `LaunchSpec` so `call_tool_inner` reconnects on demand and retries **once**, bounded by `RECONNECT_COOLDOWN` (5s) so a crash-looping binary cannot become a process-spawn storm; **(4) a per-server health supervisor** checks transport liveness every 60s (`HEALTH_CHECK_INTERVAL`) and proactively removes dead connections before the next tool call discovers them, with a circuit breaker that stops auto-healing after `MAX_CONSECUTIVE_HEALTH_FAILURES` (3) consecutive failures and logs an operator-actionable error. The supervisor does not call `try_reconnect` directly (the future is not `Send`); it removes the dead connection and relies on mechanism (3) for healing. `start_server_with_env` also retries spawn+handshake up to `STARTUP_MAX_RETRIES` (3) with exponential backoff (500ms→10s) before reporting failure, and pipes child stderr to `tracing` with `target: "hkask.mcp.child"` and the `server_id` field for attributed diagnostics. A TOCTOU race between concurrent `start_server_with_env` calls (e.g. startup + on-demand reconnect) is handled by checking at insert time: the loser's keeper is cancelled and its child process killed, the winner's connection is kept. `stop_server`/`shutdown_all` drop the launch spec and clear `health_failures` so a deliberate stop is not resurrected. The idempotency check is now liveness-based, not presence-based. **Retry safety is classified by provable delivery, not by "was it a transport error".** `ToolPortError` gained two variants: `Unavailable` (no live peer accepted the request — the tool provably did not run, so a retry cannot double a side effect) and `Interrupted` (a live peer accepted it and the connection then failed — **the outcome is unknown**). The distinction is forced by rmcp, which reports both a failed send (`service.rs:921`) and a dropped response channel (`service.rs:555,566`) as the same `ServiceError::TransportClosed`: once a request reaches a live peer, a transport loss is not proof of non-delivery. `is_retryable()` is true for `Unavailable` only; `Interrupted` is never auto-retried, at any layer, because doing so would duplicate state-changing effects (two `kanban_task_create`s, a `swarm_hire` charging credits twice). Internally `DispatchError::{NotDelivered, Interrupted, Failed}` carries this, and only `NotDelivered` drives the one-shot reconnect-and-retry. Previously pinned by `runtime.rs::reconnect_path_tests::{failed_start_still_records_a_launch_spec_for_later_reconnect, stop_server_clears_the_reconnect_path, shutdown_all_clears_every_reconnect_path, reconnect_is_rate_limited_by_the_cooldown, metadata_only_server_cannot_be_reconnected}` (inline module asserting on the private `launch_specs` map and `try_reconnect`); that module was deleted with the condenser teardown and **not restored** — `runtime.rs` has no `#[cfg(test)]` section. **Restored 2026-08-24:** the reconnect-path recording and cooldown behavior is now re-pinned by `runtime.rs::reconnect_path_tests::{metadata_only_server_cannot_be_reconnected, stop_server_clears_the_reconnect_path, shutdown_all_clears_every_reconnect_path, reconnect_is_rate_limited_by_the_cooldown, failed_start_still_records_a_launch_spec_for_later_reconnect}` (5 inline tests). Healing was previously verified against real child processes by `tests/reconnect_integration.rs` (5 tests, `--features test-fixture`), which SIGKILLs a controllable fixture MCP server (`src/bin/mcp_test_fixture.rs`) and asserts the next call reconnects and is served by a *new* pid; that file was deleted with the condenser teardown and **not restored** — the `kask/crates/hkask-mcp/tests/` directory no longer exists. The mutation tests (`killed_server_is_reconnected_on_the_next_call`, `dead_connection_is_reaped_from_the_runtime`) are gone with it, so the four self-heal mechanisms are currently unpinned. **Restored 2026-08-24:** `tests/reconnect_integration.rs` is restored with 5 integration tests (`--features test-fixture`, `--test-threads=1`): `killed_server_is_reconnected_on_the_next_call` (mechanism 3 — on-demand reconnect), `dead_connection_is_reaped_from_the_runtime` (mechanism 1 — keeper reaps), `stop_server_then_call_returns_unavailable` (deliberate-stop clearing), `startup_retry_with_backoff` (startup retry), `health_supervisor_removes_dead_connection_without_tool_call` (mechanism 4 — health supervisor, using `HKASK_MCP_HEALTH_CHECK_INTERVAL_SECS=1` to reduce the 60s wait). The four self-heal mechanisms are now pinned. The fixture's kill path uses `libc::kill` and **asserts** success, because an earlier version that swallowed the result with `let _ =` made the whole suite pass vacuously with all healing disabled. **Server-side replay protection resolves the `Interrupted` ambiguity for the three tools that cannot absorb a duplicate.** `Interrupted` is unavoidable at the client, so it can only be *resolved* at the server: `hkask-mcp-kata-kanban/src/idempotency.rs` stores `(tool, key) → first response` in the kanban database, and `kanban_task_create` / `kanban_board_create` / `kanban_task_spawn` accept an optional `idempotency_key` and return the original response (plus `replayed: true`) instead of re-running. The claim is a single `INSERT OR IGNORE` whose zero-rowcount *is* the already-seen signal, so two concurrent replays cannot both win — no read-then-write race, and no transaction (`dyn DatabaseDriver` exposes none). Reservation is two-phase (`reserve` → work → `record`) because the work happens between: a key claimed but never recorded is `Pending`, reported as unknown rather than silently re-run, since whether it landed is precisely what is unknown; a cleanly-failed call calls `release` so a retry starts fresh instead of inheriting that verdict. Only these three needed it — `task_update`/`task_assign`/`task_delete`/`board_delete` already converge, and `swarm_clone_to_local`/`swarm_push_to_cloud`/`swarm_create_local_agent` are keyed by `agent_name`. **`swarm_hire` was already protected** by `spend_gate`'s consume→spend→refund over a single-use `ConsentStore` token (`consent.rs:440-454`, atomic across processes via `DELETE`-rowcount), so a replayed hire fails closed rather than double-charging; the client-side warning remains because the *outcome* is still unknown, which is a query problem, not a safety one. `kanban_panel` generates one key per operator **gesture** (before the retry loop, not per attempt — per-attempt keys would defeat the mechanism entirely) and widens its retry to cover `Interrupted` for exactly `IDEMPOTENT_TOOLS`. Because the panel injects the key into a raw JSON object, nothing ties the two sides together at compile time and a rename would silently disable protection via `#[serde(default)]` — pinned by `wire_field_name_matches_what_clients_send`. Non-durable degradation is labelled (`idempotency_durable: false`) when the kanban DB is in-memory, per the advertised-invariant rule. Previously pinned by `tests/idempotent_creates.rs` (15, incl. cross-process replay, row-count oracles, and `wire_field_name_matches_what_clients_send`) + `kanban_panel::tests` key-policy tests; the in-crate `idempotency::tests` module, `kanban_panel::tests`, and `tests/idempotent_creates.rs` were all deleted with the condenser teardown and **not restored** — `idempotency.rs` and the entire `hkask-mcp-kata-kanban/src/` tree have no `#[cfg(test)]` section, and the `tests/` directory no longer exists. The idempotency store and its wiring are currently unpinned.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D5  | Keychain access                                                                         | `kask/crates/hkask-keystore`                                                                                                                                                                                                                                                                                                                         | **Single keychain namespace architecture.** API keys (Exa, Tavily, Brave, SerpAPI, Firecrawl, FMP, EODHD, FRED, RunPod, Nebius, HF Token, ABW, etc.) are stored in zed's `CredentialsProvider` keychain namespace under `kask://credentials/<key>` (label `zed-github-account`, attribute `url=kask://credentials/<key>`). `build_mcp_server_env` reads from this namespace and injects as env vars into MCP server child processes. The server's `resolve_credential` reads API keys from env vars only — there is NO `service=hkask` keychain fallback for API keys. The `service=hkask` namespace (via the `keyring` crate) is reserved for internal `hkask-keystore` keys (DB passphrase `hkask-db-passphrase`, swarm memory passphrase `hkask-swarm-memory-passphrase`, master key, signing keys) that predate the zed integration and are read by `hkask-keystore` directly. There are NO `*_enabled` settings toggles — the key's presence in the keychain IS the toggle. The `.env` file loading has been fully removed — the keychain is the single source of truth for API keys. The a2a secret threading was removed (self-referential token verification — security theater); the in-process `DelegationToken` that replaced it was itself removed 2026-08-12 (see D3, RR-0056).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D6  | Thread → memory                                                                         | `crates/agent/src/thread.rs` + `crates/agent/src/agent.rs` + `crates/agent/src/kask_thread_state.rs` + `kask/crates/hkask-types` + `kask/crates/kask_bridge/src/memory.rs`                                                                                                                                                     | `MemoryPort` trait in `hkask-types` (carries `agent_id` so the port routes by owning agent). `BridgeMemoryPort` + `RealMemoryPort` in `kask_bridge`. Global hook `agent::set_memory_port()` (Mutex — re-settable), wired once in the deferred task after the Zed user resolves; before that the hook is `None` and turn ingest no-ops. Thread turn completion ingests via `cx.background_spawn()`. `Thread` carries `agent_id: Option<AgentId>` (now on `KaskThreadState`, accessed via `thread.kask.agent_id()`) set by `NativeAgent::new_session` when the agent is the Curator. **The user/zed agent has NO memory** — the user is human and has their own memory. Only curator turns are ingested. `RealMemoryPort::ingest_turn` branches on `agent_id`: Curator turns go to the curator's sovereign `agents/curator/curator.db` as a curator-perspective h_mem (Private, `curator_webid`) + a shared copy (Shared) for curator recall. Non-curator turns return `Ok(())` immediately without writing anything. `recall_context` and `recall_thread` (the `MemoryPort` trait impls) return empty vecs — the user/zed agent has no memory to recall. The curator recalls its own memory via `recall_context_curator` / `recall_thread_curator` (inherent methods on `RealMemoryPort`, not on the `MemoryPort` trait). **The episodic/semantic distinction has been completely removed** — all h_mems are unified; the ontology blob carries dual-axis anchoring (PKO process + DC state) but there is no type distinction. Consolidation is confidence-based cleanup only (delete low-confidence, prune to budget) — no promotion, no re-tagging. **Swarm memory lives in the swarm MCP server, not the bridge.** Local swarm agents execute via `LocalSwarmRuntime::delegate` inside the `hkask-mcp-swarm` process, so their turns never reach the bridge's `Thread`/`ingest_turn` — the bridge cannot observe them. The shared `swarm_memory.db` (one DB for all swarms and all agents — a single knowledgebase swarms share) is written and read entirely in the swarm server: `local_knowledge::ingest_turn` stores each delegation's full turn (task + response + model) as an h_mem under a per-turn entity (`agent:<agent_id>:turn:<uuid>`) plus an embedding of the task, wired into `swarm_delegate_local` + `swarm_execute_plan_local`. `local_knowledge::recall_turns` + the `swarm_recall_local` MCP tool recall prior turns by semantic similarity via the real `InferencePort::embed` (IPC bridge → `LanguageModelEmbeddingPort`); `search_similar` has no agent filter, so a turn any agent produced is retrievable by any other (the shared-knowledgebase property). The bridge-side `SwarmStore`, `recall_context_swarm`, the `swarm_store` field, and `memory_health_json`'s `swarm_store` entry were **deleted** as dead surface. Pinned by `local_knowledge::tests::{ingest_turn_stores_full_turn_retrievable_by_recall, recall_spans_all_agents_shared_knowledgebase, ingest_turn_skips_and_recall_errors_when_store_unavailable, multiple_turns_accumulate_in_shared_knowledgebase}` + the updated `memory_health_json_reports_degraded_without_healing` (curator-only `degraded` now). **Refactored 2026-08-26:** the `agent_id` field and all kask-specific per-thread state moved from `Thread` to `KaskThreadState` (`crates/agent/src/kask_thread_state.rs`). The turn-loop call sites use `self.kask.agent_id()` / `self.kask.is_curator_thread()` / `self.kask.check_tool_retry()` etc. See `kask/docs/plans/thread-hooks-refactor-2026-08-26.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D7  | App-identity                                                                            | `crates/paths/src/paths.rs`, `crates/cli/src/main.rs`, `crates/release_channel/src/lib.rs`, `crates/zed/src/zed/mac_only_instance.rs`, `crates/zed/Cargo.toml`, `kask/scripts/build/install.sh`, `kask/scripts/build/install-binary.sh`, `kask/scripts/build/update-zed-kask.sh`, `script/bundle-linux`, `script/bundle-mac`, `script/bundle-windows.ps1`, `script/snap-build`, `nix/build.nix`, `nix/modules/packages.nix`, `flake.nix` | `APP_NAME`→`Zed-Kask`, `app_identifier`→`Zed-Kask-Editor`, `app_id`→`dev.zed-kask.Zed-Kask`, `display_name`→`Zed-Kask`, port offset +500, binary `zed-kask`, remote dirs `.zed-kask_server` / `.zed-kask_wsl_server`, bundle IDs `dev.zed-kask.*`, URL scheme `zed-kask://`. **Flatpak runtime code removed (2026-08-27):** upstream's CLI `mod flatpak` (sandbox escape via `flatpak-spawn --host`, hard-coding upstream Zed's `FLATPAK_ID=dev.zed.Zed` and binary layout) and the `FLATPAK_XDG_{CONFIG,DATA,STATE,CACHE}_HOME` overrides in `paths.rs` are deleted — zed-kask is not distributed as a flatpak, and honoring upstream Zed's sandbox env vars would launch upstream Zed's editor / point zed-kask at upstream Zed's data dirs when the CLI runs inside upstream Zed's sandbox. Pinned by `check-zed-isolation.sh` (`mod flatpak|flatpak-spawn|FLATPAK_ID` in `crates/cli/src/main.rs`, `FLATPAK_XDG_` in `crates/paths/src/paths.rs`). **Hard Zed-isolation invariant:** zed-kask is installed and updated only through `kask/scripts/build/`; it may write only `zed-kask`, `hkask-mcp-*`, zed-kask config/data, and zed-kask icons. Upstream Zed's updater is not initialized or imported by the zed binary. The `Update Zed-Kask` menu action launches only the installed `share/zed-kask/install/update-zed-kask.sh` in a visible terminal; that wrapper locks updates, derives its prefix from its own location, and invokes the checksum-verified flat-binary installer. Legacy Zed bundlers for Linux, macOS, Windows, and Snap are fail-closed; upstream release workflows, the Zed desktop template, and Flatpak/Snap resources are deleted; macOS `[package.metadata.bundle-*]` sections (including `osx_url_schemes = ["zed"]`) are removed — zed-kask is Linux-only and does not bundle for macOS. The nix derivation (`nix/build.nix`) is build-only (consumed by the devshell for dev tooling); the installPhase produces `zed-kask` (not `zed`), installs no icons or `.desktop` files, and `nix/modules/packages.nix` exports no installable packages. `flake.nix` carries zed-kask identity (description, no `zed.cachix.org`). Installer destinations are canonicalized and rejected if they resolve inside Zed's app, data, config, command, or launcher paths. Enforced dynamically at the installer mutation boundary and by `kask/scripts/build/check-zed-isolation.sh` (CI invariants job). |
| D8  | Bridge + adapters                                                                       | `kask/crates/kask_bridge/` (incl. `src/inference_socket.rs`)                                                                                                                                                                                                                                                                                       | **THE bidirectional seam** — implements all ports over zed-kask facilities. `InferencePort` over `LanguageModel`, `LanguageModelEmbeddingPort` (resolves credentials from `INFERENCE_PROVIDERS` + env var, no `LanguageModelRegistry` lookup), `resolve_model_names` (generic provider-prefixed model-name resolution), `BridgeMemoryPort`, `BridgeContextInjector`, `BridgeCuratorContextInjector` (curator-scoped recall via `RealMemoryPort::recall_context_curator`), `BridgeThreadCondenser`, `BridgeMetacognitionProvider`, `InferenceIpcServer`, `KaskSettings`. Channel pattern solves GPUI/tokio `Send`+`Sync` boundary. `LanguageModelInferencePort` holds **two** mpsc channels: `tx` (non-streaming `InferenceRequest` → `InferenceResult`) and `stream_tx` (streaming `StreamInferenceRequest` → `InferenceStreamChunk` stream). The GPUI-side receiver task polls both via `tokio::select!`. The non-streaming path (`handle_non_streaming`) collects all events into a single `InferenceResult` via `StreamAccumulator`; the streaming path (`handle_streaming`) forwards `Text`/`Thinking` events immediately as `InferenceStreamChunk`s (live thinking-trace for skill execution) and accumulates only metadata (`ToolUse`, `Stop`, `UsageUpdate`) into a final chunk. `StreamAccumulator` is the shared event-processing struct used by both paths. The `generate_stream` trait override uses `futures_util::stream::unfold` to convert the tokio mpsc receiver into a futures stream (no `tokio-stream` dependency). `generate_stream` does **not** support `model_override` (hardcoded to `None`) — skill execution's `call_inference_stream` calls `generate_stream` without an override; `generate_stream_with_model` (added 2026-08) threads `model_override` through `StreamInferenceRequest.model_override` to `handle_streaming`, which calls `resolve_model` with the override — so streaming + override preserves the live trace (the prior claim that it fell back to non-streaming `generate_with_model` was stale). The inference IPC protocol carries one MCP-server-to-zed dispatch method: `tool_invoke` (governed `McpRuntime` via the injected `ToolPort`). The per-request `tool_allowlist` on `tool_invoke` is the authority boundary for delegated dispatch (fail-closed on missing/empty, enforced before dispatch so it does not depend on the child's own matching) — pinned by `dispatch_tool_invoke_rejects_unallowed_tool`. The call meter is **fail-open on an unregistered agent** (RR-0057): it auto-registers at `DEFAULT_RUNAWAY_CALL_CEILING` and logs the wiring gap rather than refusing, because a missing seed is a wiring omission, not an authorization decision. The prior fail-closed cap silently broke delegated paths — `main.rs` seeded only the `swarm-panel` persona while this dispatch mints `kask-panel`, which derive different WebIDs, so every IPC tool call was refused with `EnergyBudgetExceeded` while this row claimed the seed made them "succeed while remaining bounded." The one pre-dispatch refusal is an exhausted per-tick ceiling (runaway-loop breaker; resets each regulation tick). **Inference IPC socket path management:** `kask_bridge::inference_socket` (a re-settable `Mutex<String>`) owns the socket path state, replacing a local `OnceLock<String>` in `main.rs` that could not be updated when the IPC server restarted (e.g. when the real model resolved after the no-op fallback). `main.rs` calls `kask_bridge::set_inference_socket_path` after `InferenceIpcServer::start` succeeds, and `kask_bridge::get_inference_socket_path` when building MCP server env maps via `build_mcp_server_env`. The `OnceLock`'s `set()` was write-once — a second IPC server start (model resolved after no-op fallback) silently dropped the new socket path, leaving MCP servers with a stale or empty `HKASK_INFERENCE_SOCKET` env var and all inference calls failing with "IPC bridge not configured." **Both MCP server paths re-synced on socket set:** after `set_inference_socket_path`, `main.rs` calls both `sync_kask_mcp_servers` (ContextServerStore path) AND `sync_kask_mcp_runtime_servers` (governed McpRuntime path). Previously only the ContextServerStore path was re-synced, so governed McpRuntime servers kept their stale env (no socket) and all skill execution / panel inference calls failed even though the socket was live. |
| D9  | Settings + credentials                                                                  | `kask/crates/kask_bridge/src/settings.rs` + `crates/settings_content/src/settings_content.rs` + `crates/settings_ui/src/pages/kask_page.rs` + `crates/settings_ui/src/page_data.rs`                                                                                                                                                                  | `KaskSettings` struct + `"kask"` section in settings.json. **Single keychain namespace:** all API keys (Exa, Tavily, Brave, SerpAPI, Firecrawl, FMP, EODHD, FRED, RunPod, Nebius, HF Token, ABW, etc.) are stored in zed's `CredentialsProvider` keychain under `kask://credentials/<key>` (label `zed-github-account`, attribute `url=kask://credentials/<key>`). There are NO `*_enabled` settings toggles — the key's presence in the keychain IS the toggle. The `KaskDataServiceSettings` struct and its `*_enabled` bool fields have been deleted (2026-08-24); `credential_urls_for_mcp()` emits all credential URLs unconditionally. The `service=hkask` keychain namespace (via the `keyring` crate) is reserved for internal `hkask-keystore` keys (DB passphrase, swarm memory passphrase, master key, signing keys) that predate the zed integration. `resolve_credential` reads API keys from env vars only (no `service=hkask` fallback); `HKASK_DB_PASSPHRASE` and `HKASK_SWARM_MEMORY_PASSPHRASE` retain their `hkask-keystore` keychain fallback. **`nudge_mcp_servers` restart trigger:** after `write_credential` / `delete_credential` writes a `kask://credentials/...` URL to the keychain, it calls `nudge_mcp_servers(cx)` which calls `SettingsStore::notify_observers(cx)` — a direct `Effect::NotifyGlobalObservers` push via `global_mut` that bypasses the file-write path entirely. This fires the `SettingsStore` observer → `sync_kask_mcp_runtime_servers` → `build_mcp_server_env` (re-reads keychain) → restart. Only fires for `kask://credentials/...` URLs. **`permission_denied` classification standard:** missing credentials must surface as `McpToolError::permission_denied` with the env var named. **Canonical `HKASK_DB_PASSPHRASE` resolution helper:** `hkask_mcp_server::server::resolve_db_passphrase(&ctx.credentials)` — 2-tier chain (ctx.credentials → `resolve_credential` which does env → `hkask-keystore` keychain `hkask-db-passphrase`). **First-run provisioning bridge:** `kask_bridge::identity::mirror_provisioned_db_passphrase` mirrors the passphrase into `kask://credentials/hkask_db_passphrase`; `.await`ed in the deferred post-login task before governed MCP server launch.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| D11 | `time::format_description::parse` deprecation allow                                     | `crates/git_ui/src/git_graph.rs`                                                                                                                                                                                                                                                                                                                     | Upstream `git_graph.rs` calls `time::format_description::parse`, which is `#[deprecated]` in `time 0.3.54+` (the version the workspace resolves to; lower versions break `plist`/`project`). Two call sites (`timestamp_format`, the commit-date formatter) carry `#[allow(deprecated)]` with a `// zed-kask:` pointer to this seam. Remove this seam when upstream migrates to `parse_borrowed`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D12 | OpenAI/Anthropic-compatible env var name                                                | `crates/language_models/src/provider/api_compatible.rs`                                                                                                                                                                                                                                                                                              | Upstream computes the API-key env var name as `format!("{}_API_KEY", id).to_case(Case::UpperSnake)`, which splits multi-word provider ids on case boundaries (e.g. `SomeProvider` → `SOME_PROVIDER_API_KEY`) and leaves `fal.ai` as an invalid env var name (`FAL.AI_API_KEY`). The entire kask ecosystem (MCP servers, keystore, UI text, docs) uses the concatenated alphanumeric form (`OPENROUTER_API_KEY`, `FALAI_API_KEY`), so the upstream computation never matches the env vars kask users set. The `// zed-kask:` block in `ApiCompatibleProviderState::new` strips non-alphanumerics and uppercases instead of using `convert_case`. `convert_case` was removed from `crates/language_models/Cargo.toml` (still used by other crates, so the workspace dep stays). Pinned by `test_api_key_env_var_name_kask_contract` in `api_compatible.rs`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D13 | OpenRouter output budget                                                                | `crates/open_router/src/open_router.rs` + `crates/language_models/src/provider/open_router.rs` + `crates/open_router/Cargo.toml` + `crates/language_model_core/src/request.rs` + `kask/crates/kask_bridge/src/inference_chat.rs`                                                                                                                                                                                                                     | Upstream omits `max_tokens` from completion requests entirely (`Model::max_output_tokens()` hardcodes `None`). OpenRouter then reserves the model's full default output size (e.g. 64k for claude-haiku-4.5, 128k for glm-5.2) against the key's credit limit before dispatching, and rejects with 402 on keys whose remaining limit can't cover the reservation — even for a one-line prompt. Zed-kask parses `top_provider.max_completion_tokens` from the `/models` and `/models/user` responses into a new `Model::max_output_tokens` field, which flows into the request as an explicit `max_tokens` budget. Settings-defined `available_models[].max_output_tokens` (already in `OpenRouterAvailableModel` upstream, previously unwired) overrides the API-derived value. The API-derived value is capped at half the model's `context_length`: some providers (e.g. GLM 5.2) advertise `max_completion_tokens == context_length` (1048576), which is mathematically impossible (input + output can never exceed the context window), so sending it raw made OpenRouter reject every non-empty request with 400 "maximum context length exceeded" and broke the compaction math (`max_input_tokens = context - max_output_tokens` collapsed to 0, triggering auto-compact on every turn). The half-context cap only affects broken models; every sane model advertises a cap already below 50% of its context. Zed-kask also adds a per-request `max_tokens: Option<u64>` field to `LanguageModelRequest` (`language_model_core/src/request.rs`); `into_open_router` prefers `request.max_tokens` over the model default when set. (Historical note: `kask_bridge::LanguageModelInferencePort::build_request` previously set this from a since-removed `LLMParameters.max_tokens` field for the skill-cascade executor; the cascade teardown removed that wiring, and `build_request` now leaves `max_tokens: None` — the model-default budget from `Model::max_output_tokens` covers every current kask inference call. No kask caller sets `request.max_tokens` today; the per-request override mechanism remains wired and pinned for future use.) Pinned by `test_max_completion_tokens_from_api_becomes_request_budget`, `test_max_output_tokens_capped_at_half_context_length`, and `test_per_request_max_tokens_overrides_model_default`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D14 | Streaming text reveal timer interval                                                    | `crates/acp_thread/src/acp_thread.rs`                                                                                                                                                                                                                                                                                                                | Upstream hardcodes `StreamingTextBuffer::TASK_UPDATE_MS = 16` (60fps). During active streaming, this fires a `Markdown::append` + `cx.notify()` on the foreground thread 60 times per second, saturating the render loop and starving the `BlinkManager`'s 500ms timer — the cursor flashes irregularly/rapidly. Zed-kask increases it to 50ms (20fps): still smooth for text reveal (the human eye can't perceive text appearing at >20fps) but 3x less render pressure. Pinned by `test_streaming_reveal_timer_interval_kask_contract`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| D15 | Bounded cursor-blink timers                                                             | `crates/editor/src/blink_manager.rs`                                                                                                                                                                                                                                                                                                                 | Two upstream timer-accumulation vectors make the cursor strobe at (N+1)× the normal rate. (1) `pause_blinking` detaches a new 500ms resume task on every selection change — rapid typing accumulates uncancellable foreground wake-ups, and blur leaves pending blink/resume callbacks valid across a later refocus. Zed-kask stores one restartable resume task, keeps `blinking_paused` consistent with the actual state, and invalidates pending callbacks on disable. (2) The `SettingsStore` observer unconditionally calls `blink_cursors` on every settings update, and `blink_cursors` spawns a fresh 500ms timer on top of any already running — each unrelated settings change (profile switch, model selection, settings.json save) stacks another blink cycle. Zed-kask only (re)starts blinking on the disabled→enabled transition. Pinned by `test_pause_blinking_restarts_single_resume_deadline`, `test_disable_cancels_pending_resume`, and `test_settings_updates_do_not_accumulate_blink_timers`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D16 | App menu rename + safe zed-kask updater                                                 | `crates/zed/src/zed.rs` + `crates/zed/src/zed/app_menus.rs`                                                                                                                                                                                                                                                                                          | Leftmost app menu `name` changed from `"Zed"` to `"z-k"`. On macOS the platform overrides this title with the bundle name ("Zed-Kask" via D7); the rename only takes effect on the cross-platform title-bar `ApplicationMenu` (Linux/Windows, `crates/title_bar/src/application_menu.rs`). The upstream update actions remain removed because upstream Zed's installer writes `zed*.app` into `~/.local` and can replace the user's real Zed. `Update Zed-Kask` is a distinct `RunZedKaskUpdate` action: it resolves only the installed zed-kask updater relative to the running executable and launches it in a visible terminal task. It has no `auto_update` dependency or call path. Pinned by `test_leftmost_menu_name_is_zk`, `test_leftmost_menu_has_zed_kask_update_item`, and `kask/scripts/build/check-zed-isolation.sh`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| D18 | Graph + kanban + portfolio + scenarios block renderer (viz widgets in markdown) | `crates/markdown/src/markdown.rs` + `crates/agent_ui/src/conversation_view.rs` + `crates/hkask-viz-core/` + `crates/hkask-graph-widget/` + `crates/hkask-kanban-widget/` + `crates/hkask-portfolio-widget/` + `crates/hkask-scenarios-widget/`                                                                        | New `media_block_renderer: Option<MediaBlockRendererFn>` field on `MarkdownElement` (an optional callback that intercepts fenced code blocks before the default code block renderer). Called for every fenced code block — returns `Some(div)` to render a viz widget (the event-tree DAG for `graph`, kanban board columns for `kanban`, portfolio dashboard for `portfolio`, scenario pipeline/matrix/timeline for `scenarios`), or `None` to fall through to default code block rendering. The callback type `MediaBlockRendererFn` and the `.media_block_renderer()` builder method are the only upstream additions. The widget implementations live in `crates/hkask-graph-widget/`, `crates/hkask-kanban-widget/`, `crates/hkask-portfolio-widget/`, and `crates/hkask-scenarios-widget/` (zed-kask-side — they render GPUI elements, so they depend on `gpui`/`theme` and live outside `kask/` to preserve the §13.1 invariant that hKask crates never depend on zed-kask crates). Wired in `render_agent_markdown` (`conversation_view.rs`) via `.media_block_renderer(hkask_viz_core::block_renderer())` — the `hkask-viz-core` registry composes all widget renderers into one callback so the upstream D18 field/builder/dispatch stay unchanged. The kanban/portfolio/scenarios widgets are inline chat-stream renderers for their respective fenced blocks. Pinned by `selects_event_tree_body` / `falls_through_non_graph_bodies` in `hkask-graph-widget` and the layout tests in `hkask-graph-widget/src/layout.rs`. |
| D20 | Observed per-call USD cost in `TokenUsage`                                              | `crates/language_model_core/src/language_model_core.rs` + `crates/open_ai/src/open_ai.rs` + `crates/open_ai/src/completion.rs` + `crates/copilot_chat/src/model.rs` + `kask/crates/kask_bridge/src/inference_chat.rs`                                                                                                                                     | Upstream's `TokenUsage` carries only token counts; the provider's reported USD cost (`usage.cost` / `estimated_cost` / `market_cost`) is discarded, so the IPC-bridge path (the primary inference route — the bridge sees only `LanguageModelCompletionEvent::UsageUpdate(TokenUsage)`) cannot observe the per-call USD cost. Zed-kask adds a `cost: Option<f64>` field to `TokenUsage` (serde-optional, backward-compatible). The OpenAI-compatible provider impl (`open_ai` crate, used by kask's `openai_compatible` providers) parses `usage.cost`/`estimated_cost`/`market_cost` into it (preferring `market_cost` — real compute energy — for BYOK), and `kask_bridge::LanguageModelInferencePort` reads `TokenUsage.cost` from the `UsageUpdate` event into `InferenceResult.cost_usd`. `Add`/`Sub` for `TokenUsage` sum/subtract `cost` for cumulative accounting. The other provider impls (Anthropic, Google AI, Bedrock, Copilot, DeepSeek, LlamaCpp, LMStudio, Mistral, Ollama, zed's OpenRouter adapter) set `cost: None` (they don't report USD cost in `usage`). **`crates/copilot_chat/src/model.rs`** was added by upstream's `6943d7362e` (Copilot Chat cleanup — moved `CopilotChatLanguageModel` out of `language_models/src/provider/copilot_chat.rs` into the `copilot_chat` crate); the three `TokenUsage` construction sites in `map_events` carry `// zed-kask: D20` markers and set `cost: None` (Copilot doesn't report USD cost). Pinned by `test_token_usage_cost_round_trips` + `test_token_usage_cost_adds_and_subs` in `language_model_core`, `test_map_event_populates_cost_from_usage` in `open_ai`, and `responses_stream_usage_carries_no_cost` in `copilot_chat::model`.                                                                                                               |
| D21 | Widget→agent compose-back seam (the "I disagree" gesture)                               | `crates/agent_ui/src/conversation_view.rs` + `crates/hkask-conversation-injector/`                                                                                                                                                                                                                                                                   | Kask viz widgets (D18) are leaves with no access to the active agent conversation thread, so they cannot compose a structured message back into it (a provenance-scoped revision request / "I disagree"). The `hkask-conversation-injector` leaf crate hosts the `ConversationInjector` trait + a process-global accessor (`set_active_injector` / `shared_injector`), mirroring the `hkask-tool-invoker` (widget→MCP) seam but for widget→agent. The production impl `ThreadConversationInjector` (in `agent_ui`, the D-seam) holds the active `Entity<ThreadView>` and pre-fills its message editor with the composed body via `MessageEditor::clear` + `insert_text` (both production-available; `set_text` is test-gated); the user reviews and submits via the existing Send button so the turn-loop's checkpoints/telemetry are preserved (auto-send would bypass them, and `ThreadView::send`/`send_content` are deliberately untouched). `ConversationView::publish_injector` reads `active_thread()` and publishes/clears the global; it is called from the active-thread-change points `navigate_to_thread` and `set_server_state` (covers connect + disconnect). The global is re-settable (Mutex, not OnceLock) so each activation replaces the prior. No cross-thread channel is needed — `inject` is called from a foreground widget `on_click` (`window` + `cx` passed in). The trait + accessor live in the leaf crate so kask widgets can compose back without depending on the heavy `agent_ui` crate (§13.1 invariant preserved). The `render_agent_markdown` block renderer is untouched. Pinned by `tests::publish_injector_wires_global_on_activation_and_clears_on_disconnect` in `conversation_view.rs`, and the `hkask-conversation-injector` leaf tests (`shared_injector_returns_none_by_default`, `set_then_shared_returns_some`, `set_none_clears_a_prior_injector`).                                                                           |
| D22 | Block-reachability pins in `main.rs`                                                    | `crates/zed/src/main.rs`                                                                                                                                                                                                                                                                                                                             | Two block-reachability pins in `main.rs` used constructs that do not compile under `--deny warnings`. (1) The F4 pin used `std::any::TypeId::of::<hkask_regulation::DEFAULT_VARIETY_MAX_DEFICIT>()`, which treats an `f64` const as a type — replaced with a plain const read (`let _ = hkask_regulation::DEFAULT_VARIETY_MAX_DEFICIT;`). (2) The F23 pin used `std::ptr::addr_of!(kask_server_env)`, which takes the address of a temporary (a function name is not an lvalue) — replaced with a plain name reference (`let _ = kask_server_env;`), which evaluates to a function pointer and pins the symbol. Both fixes are isolated to the pin lines; the other block-reachability pins are upstream `TypeId::of::<Type>()` calls and are untouched. No behavioral change — the pins are compile-time reachability guards, not runtime paths.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D23 | `AgentPanelSiblingHost` visibility + worktree spawn wiring                              | `crates/agent_ui/src/agent_panel.rs` + `crates/agent_ui/src/agent_ui.rs` + `crates/zed/src/main.rs`                                                                                                                                                                                                                                                  | `AgentPanelSiblingHost` (was `pub(crate)`) is now `pub` and re-exported from `agent_ui` so `kask_bridge` can use it via the `WorktreeSpawner` trait. `main.rs` adds `AgentPanelWorktreeSpawner` (impl `WorktreeSpawner`) + a `cx.observe_new` on `AgentPanel` that sets the process-global `WORKTREE_SPAWNER` when a panel is created and clears it when dropped. The `InferenceIpcServer` GPUI-side task reads the global to dispatch `CreateWorktreeThread` IPC requests → `AgentPanelSiblingHost::create_sibling_thread` with `use_new_worktree: true`. The `kanban_task_spawn` MCP tool tries worktree spawn first, falls back to in-memory `LazyLocalSwarmRuntime`. No upstream behavioral change — the visibility widening is additive (the struct was already used cross-crate within `agent_ui`; making it `pub` extends that to `kask_bridge`). Pinned by the `kask_bridge` IPC dispatch tests (`dispatch_generate_returns_canned_result`, etc.) which pass `None` for the worktree spawn channel.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D24 | Edit predictions via `LanguageModelRegistry` (glm-5.2)                                 | `crates/edit_prediction/src/open_ai_compatible.rs` + `crates/edit_prediction/src/edit_prediction_tests.rs` + `crates/language_models/src/provider/open_router.rs` + `crates/zed/src/main.rs` + `kask/crates/kask_bridge/src/inference_edit_prediction.rs` + `assets/settings/default.json`                                                                                                                               | zed-kask routes edit predictions through the `LanguageModelRegistry` using `DEFAULT_FALLBACK_MODEL` (`OpenRouter/z-ai/glm-5.2`), collapsing the edit-prediction model onto the same model + credentials the agent uses, instead of Zed's cloud Zeta endpoint (the 429-rate-limited default). Three edits: (1) `OpenRouterLanguageModel` gains `api_url()`/`api_key()` trait overrides (returning the OpenRouter base URL + API key from provider state) — the trait default returned `None`, forcing callers to resolve credentials from env vars instead of the registry; the kask embedding port already uses this pattern. (2) `open_ai_compatible.rs` gains a process-global `Mutex<Option<Arc<dyn KaskCompletionPort>>>` hook (`set_kask_completion_port`); when wired, `send_custom_server_request` delegates to the port instead of making the HTTP call. (3) `kask_bridge::BridgeEditPredictionPort` implements `KaskCompletionPort` — at construction it resolves `DEFAULT_FALLBACK_MODEL` from the registry via `resolve_model_names`, extracts `api_url()`/`api_key()`, strips the provider prefix, and makes raw `/completions` POSTs via `HttpClient` (same pattern as `LanguageModelEmbeddingPort` but `/completions` instead of `/embeddings`). Wired in `main.rs` from the model-dependent task (fires when the registry resolves a model). `default.json` changes `edit_predictions.provider` from `"zed"` to `"open_ai_compatible_api"` with `prompt_format: "glm"` and `model: "z-ai/glm-5.2"` — the `Glm` FIM format (`fim.rs:189`) formats as bare `{prefix}`, and the `edit_prediction_provider_config_for_settings` router sends `OpenAiCompatibleApi` + non-Zeta format to the `Fim` model path, which calls `send_custom_server_request` (intercepted by the kask port). The `api_url`/`model` in settings are fallbacks for the pre-wiring window. When the port is `None` (before the model resolves), `send_custom_server_request` falls through to the existing HTTP path (upstream behavior). Pinned by `test_kask_completion_port_intercepts_send_custom_server_request` in `open_ai_compatible.rs`. **Test provider override:** `default.json`'s `provider: "open_ai_compatible_api"` changes what `test_settings()` resolves, breaking 21 `edit_prediction_tests` — the test `FakeHttpClient` handlers only serve the Zeta cloud endpoints (`/predict_edits/v3`, `/predict_edits/v4`), but with `OpenAiCompatibleApi` the request routes through `send_custom_server_request` to an unhandled URL, causing deadlocks or 404s. `override_edit_prediction_provider_to_zed` in `edit_prediction_tests.rs` resets the provider to `Zed` in `init_test` + `init_test_with_fake_client_and_legacy_data_collection`, restoring the upstream test path. Tests that need `OpenAiCompatibleApi` (`make_sweep_prompt_test_ep_store`, `test_unauthenticated_without_custom_url_blocks_prediction_impl`) already override the provider explicitly. |
| D25 | Chat Completions `finish_reason: "length"` → `MaxTokens`                                  | `crates/language_models/src/provider/open_router.rs` + `crates/open_ai/src/completion.rs` + `crates/agent/src/kask_thread_state.rs`                                                                                                                                                                                              | Upstream's Chat Completions event mappers (`OpenAiEventMapper`, `OpenRouterEventMapper`) lump `finish_reason: "length"` (output truncation) into the catch-all arm, log it as `"Unexpected ... stop_reason"`, and map it to `StopReason::EndTurn` — disguising a truncated response as a clean finish. (Historical note: this originally bit the since-removed skill-cascade executor, whose `step_actions::execute_select` fell back to text parsing on truncated generations; the truncation-detection value is general — any structured-output consumer that offers a tool call loses it when the generation is cut off.) Zed-kask adds an explicit `Some("length") => StopReason::MaxTokens` arm ahead of the catch-all, mirroring the existing Responses-API `"max_tokens"`/`"max_output_tokens"` handling (`OpenAiResponseEventMapper`) and the Google AI `"MAX_TOKENS"` handling, so downstream consumers (agent loop, UI) detect truncation instead of treating a partial response as complete. The `last_completion_truncated` flag (now on `KaskThreadState`, accessed via `thread.kask.on_max_tokens()` / `thread.kask.last_completion_truncated()` / `thread.kask.reset_truncation_flag()`) is set in `handle_completion_event` on `StopReason::MaxTokens`, read in `flush_pending_message` to distinguish stream-truncated tool calls from user cancellations, and reset at the start of each completion request. Pinned by `test_length_finish_reason_maps_to_max_tokens` in `open_router.rs`, `stream_maps_length_finish_reason_to_max_tokens` in `open_ai/src/completion.rs`, and `test_last_completion_truncated_distinguishes_max_tokens_from_cancel` in `thread.rs`. |
| D27 | Sandboxed terminal non-interactive shell                                                 | `crates/acp_thread/src/acp_thread.rs`                                                                                                                                                                                                                                                                                                                | Upstream's `AcpThread::create_terminal` only calls `ShellBuilder::non_interactive()` when `HeadlessTerminal::is_enabled(cx)` is true (eval CLI only). In the GUI, `HeadlessTerminal` is not set, so the sandboxed shell is launched with `-i` (interactive mode). But the sandboxed command has stdin redirected to `/dev/null` (`redirect_stdin_to_dev_null`) and runs in a new session (`setsid()` in the `pre_exec` hook) with no controlling terminal — bwrap's user namespace severs the PTY's controlling-terminal assignment. When an interactive shell (`/bin/sh -i`) starts without a controlling terminal, it tries `tcsetpgrp()`, fails, prints `"Cannot set tty process group (No such process)"` to stderr, and exits with code 2 — even though the actual command ran fine. This made **every** sandboxed terminal command appear to fail (346 instances in a single agent thread), with agents seeing exit code 2 and the tty warning appended to every command output. Zed-kask adds `|| sandbox_wrap.is_some()` to the `non_interactive()` condition on the non-Windows path, matching the Windows sandboxed path (which already always uses `non_interactive()`). The unsandboxed GUI path (when `sandbox_wrap` is `None` and `headless` is false) still uses interactive mode, preserving existing behavior for non-sandboxed terminals. Pinned by `test_sandboxed_terminal_runs_non_interactively` in `acp_thread.rs`. |
| D28 | Standardized Artifact Storage                                                              | `crates/agent/src/db.rs` + `crates/agent/src/agent.rs` + `crates/agent/src/tools/read_file_tool.rs` + `crates/agent_skills/agent_skills.rs` + `crates/zed/src/main.rs` + `kask/crates/hkask-types/src/agent_paths.rs` + `kask/crates/kask_bridge/src/settings.rs` + `kask/crates/kask_bridge/src/mcp_servers.rs` + `kask/crates/kask_bridge/src/memory/curator_stores.rs` + `kask/mcp-servers/hkask-mcp-kata-kanban/` + `kask/mcp-servers/hkask-mcp-research/` + `kask/mcp-servers/hkask-mcp-portfolio/` + `kask/mcp-servers/hkask-mcp-companies/` + `kask/mcp-servers/hkask-mcp-prediction-markets/` + `kask/mcp-servers/hkask-mcp-scenarios/` + `kask/mcp-servers/hkask-mcp-swarm/` + `kask/mcp-servers/hkask-mcp-training/` + `kask/mcp-servers/hkask-mcp-curator/` | Establishes a single rooted data tree (`{kask_data_dir}/`) with four class subdirs (`agents/`, `mcp/`, `skills/`, `threads/`) for all persistent kask artifacts. **Ownership principle:** an artifact lives under the class subdir of the entity that owns it — agent-owned under `agents/{name}/`, server-owned under `mcp/{server_id}/`, user-owned under flat class dirs. **Archived threads** relocated from `paths::data_dir()/threads/` to `{kask_data_dir}/threads/threads.db` via `THREADS_DB_PATH_OVERRIDE` hook (`agent.rs`), pinned by `test_threads_db_override_hook_round_trips`. **Skills** relocated from `paths::data_dir()/agents/skills/` to `{kask_data_dir}/skills/` via `GLOBAL_SKILLS_DIR_OVERRIDE` hook (`agent_skills.rs`), pinned by `test_is_skill_catalog_file_detects_skill_catalog_reads` (D28 path case). **MCP server DBs** migrated to `mcp/{server_id}/{purpose}.db`: kata-kanban (`mcp/kata-kanban/kanban.db`), research (`mcp/research/rss.db`), portfolio (`mcp/portfolio/{owner}/master.db`), companies (shares portfolio path), prediction-markets (`mcp/prediction-markets/`), scenarios (`mcp/scenarios/`), swarm (`mcp/swarm/ledger.db` + `mcp/swarm/consent.db`), training (`mcp/training/training.db` + `mcp/training/adapters/`). **Curator DB** renamed from `pod.db` to `curator.db` (the "pod" concept was deprecated); `agent_pod_db()` renamed to `agent_db()` producing `{agent_name}.db`. Stays under `agents/curator/` (agent artifact, not server artifact). **`HKASK_TRANSACTIONS_DIR`** wired: emitted in `mcp_env()` (default `mcp/portfolio/transactions/`), added to portfolio server's `config_env` allowlist. **`dirs` crate** removed from portfolio, companies, and swarm crates (replaced by `resolve_under_data_dir`). Previously pinned by `mcp_server_db_paths_follow_standardized_layout` + `storage_layout_all_classes_resolve_under_one_root` in `tests/agent_paths.rs`; that file was deleted with the condenser teardown and **not restored** — `kask/crates/hkask-types/src/agent_paths.rs` has no `#[cfg(test)]` section and the `tests/` directory no longer exists. The standardized artifact-storage layout is currently unpinned. See `kask/docs/architecture/standardized-artifact-storage.md` for the full layout spec. |
| D29 | RunPod serverless endpoint provider                                                      | `crates/language_models/src/provider/runpod.rs` + `crates/language_models/src/settings.rs` + `crates/language_models/src/language_models.rs` + `crates/language_models/src/provider.rs` + `crates/settings_content/src/language_model.rs` + `crates/settings_content/src/settings_content.rs` + `kask/crates/hkask-inference/src/model_constants.rs` + `kask/crates/hkask-inference/src/config.rs` + `kask/crates/kask_bridge/src/inference_providers.rs` + `kask/crates/kask_bridge/src/identity.rs` + `kask/crates/kask_bridge/src/kask_bridge.rs` + `crates/zed/src/main.rs` + `kask/mcp-servers/hkask-mcp-corpus/src/services/convert.rs` + `kask/mcp-servers/hkask-mcp-corpus/src/ocr/pipeline.rs` + `kask/mcp-servers/hkask-mcp-corpus/src/ocr/config.rs` | Registers RunPod as a dedicated `LanguageModelProvider` (like DeepSeek, not via `openai_compatible`) so the IPC bridge can resolve `RunPod/kask-ocr` for the corpus OCR pipeline. Each endpoint carries its own API URL (`https://api.runpod.io/v2/{endpoint_id}/openai/v1`); discovery via the RunPod GraphQL API (`{api_url}/graphql`), not `/v1/models`. `DEFAULT_OCR_MODEL` changed from `RunPod/allenai/olmocr-2-7b-1025` to `RunPod/kask-ocr` (the endpoint name is the model id). `LanguageModel::name()` returns the endpoint name (the resolution key), NOT the display_name — the IPC server builds `ModelListEntry.name` as `{provider_id}/{model.name()}`, and a display_name suffix like `(OLMOCR-2)` would break `resolve_ocr_model`'s match. `supports_tools`/`supports_streaming_tools` are `false` (OLMOCR-2 is a specialized OCR model, not tool-capable). **Credential bridge:** RunPod is added to `INFERENCE_PROVIDERS` (not just `DATA_SERVICES`) so `mirror_kask_credentials_to_providers` writes the key to the Zed keychain at `api_url` (`https://api.runpod.io`) where the provider's `ApiKeyState` reads it. Skipped in `credential_urls_for_mcp`'s `INFERENCE_PROVIDERS` loop (MCP injection is handled by the `DATA_SERVICES` loop — the key's presence is the toggle, no `runpod_enabled` settings field). `mirror_kask_credentials_to_providers` (in `inference_providers.rs`, wired in `main.rs`'s deferred task) reads from `kask://credentials/runpod` and writes to `https://api.runpod.io` — but ONLY if the Zed keychain doesn't already have a key at that URL (doesn't clobber a UI-entered key). This handles users who set the key via the kask Data Services UI. The former RunPod-only mirror (`mirror_runpod_api_key` in `identity.rs`) and the former env-var mirror (`mirror_env_keys_to_keychain`) have been removed and replaced by the general `mirror_kask_credentials_to_providers` which mirrors all inference providers. The `.env` file loading has been fully removed — the keychain is the single source of truth for API keys. **`resolve_ocr_model` case-insensitivity fix:** the corpus `resolve_ocr_model` gate used case-sensitive `==` against `ModelEntry.prefixed_name`/`model`, but the IPC server produces lowercase `runpod/...` while `DEFAULT_OCR_MODEL` is `RunPod/...`. Changed to `eq_ignore_ascii_case` to align with `resolve_model_names`'s case-insensitive contract. Pinned by `endpoint_url_derives_from_api_url_and_endpoint_id`, `telemetry_id_format_is_runpod_prefixed_endpoint_name`, `runpod_provider_id_matches_runpod_case_insensitively`, `runpod_telemetry_id_matches_configured_name_case_insensitively`, `graphql_response_parses_into_available_models` (in `runpod.rs`), and `runpod_is_mirrored_to_keychain_but_not_registered_as_openai_compatible` (in `inference_providers.rs`). |
| D31 | wasmtime `ResourceTable` leak in `WasmExtension` call sites | `crates/extension_host/src/wasm_host.rs` | Upstream's `WasmExtension` impl of `extension::Extension` pushes a resource (worktree, project, kv_store, delegate) into `store.data_mut().table` at 8 call sites (`language_server_command`, `language_server_initialization_options`, `language_server_workspace_configuration`, `language_server_initialization_options_schema`, `language_server_workspace_configuration_schema`, `language_server_additional_initialization_options`, `language_server_additional_workspace_configuration`, `run_slash_command`, `context_server_command`, `context_server_configuration`, `index_docs`, `get_dap_binary`) and **never calls `table.delete`** to reclaim the entry. The wasmtime `ResourceTable` (`wasmtime-36.0.13/src/runtime/component/resource_table.rs`) has a default `max_capacity` of 1,000,000 entries; once exhausted, every subsequent `push` returns `ResourceTableError::Full` ("resource table has no free keys"), which `lsp_store.rs:4111` logs at ERROR via `.log_err()`. The leak manifested as a flood of `getting additional workspace configuration for X from Y: resource table has no free keys` errors because `workspace_configuration_for_adapter` (`lsp_store.rs:4096-4116`) loops over every registered LSP adapter and calls `additional_workspace_configuration` on each — with 6 adapters (rust-analyzer, yaml-language-server, bash-language-server [built-in, no leak] + buf, protobuf-language-server, protols [wasm-backed `proto` extension, leaking]) the cross-product leaked 30 entries per cycle, hitting the cap in ~33,000 cycles and breaking all wasm-backed LSP adapters. The downstream symptoms were rust-analyzer `textDocument/diagnostic` and `textDocument/formatting` timeouts (120s cancellation) and a foreground hang on `app_will_quit`. **Fix:** each call site now captures `resource.rep()` (the `u32` handle) before passing `resource` into the extension call (since `Resource<T>` is not `Copy` — it contains `AtomicU32` state), then calls `store.data_mut().table.delete(Resource::<T>::new_own(rep))` after the call resolves, on both success and error paths. The `delete` runs before the first `?` that propagates the wasmtime error, so it runs regardless of whether the extension call succeeded. **Why not the bridge?** The leak is in the wasm host's `Extension` trait impl, which is upstream code that kask crates don't touch; the bridge is for kask-specific behavior, not for patching upstream trait impls. This is a genuine D-seam: an upstream bug fix that benefits all Zed users, not just kask. Should be reported upstream to `zed-industries/zed`. Pinned by `test_resource_table_push_delete_round_trip_reclaims_entries` (verifies the push-then-delete pattern reclaims table entries — if a `delete` is removed, the free-list reuse assertion fails) and `test_resource_table_full_error_message_matches_log_symptom` (documents the failure mode so future maintainers recognize the log symptom). |
| D32 | No-op `update_settings_file` write skips file write + observer notification | `crates/settings/src/settings_store.rs` + `crates/language_models/src/provider/runpod.rs` | Upstream's `SettingsStore::update_settings_file_inner` unconditionally calls `fs.atomic_write` (triggering the settings file watcher) and `cx.update_global::<SettingsStore>` (whose `end_global_lease` pushes `Effect::NotifyGlobalObservers` even when `set_user_settings` returns `Unchanged`) on every call, even when the `new_text` is byte-for-byte identical to `old_text`. This created a self-sustaining feedback loop: `ensure_openai_compatible_entries` (kask, wired to a `SettingsStore` observer in `main.rs`) called `update_settings_file` on every settings notification → the file write triggered the file watcher → `cx.update_global` pushed `NotifyGlobalObservers` → the observer re-fired → `ensure_openai_compatible_entries` re-wrote the same file → loop. The RunPod "auto_discover is enabled but no API key is set" warning fired on every iteration because the RunPod `State`'s `SettingsStore` observer called `cx.notify()` unconditionally (unlike its siblings `ollama`, `llama_cpp`, `open_router`, `api_compatible`, which all guard with `if settings_changed`). **Fix 1 (root cause):** `update_settings_file_inner` now compares `new_text` to `old_text` and skips both the `fs.atomic_write` and the `cx.update_global` when they are equal, breaking the loop for all callers. `set_user_settings` already early-returns `Unchanged` on identical content, so skipping the `update_global` entirely is consistent with the in-memory store's own idempotency. **Fix 2 (amplifier):** the RunPod `State` observer now tracks `last_settings` and only calls `cx.notify()` when the settings actually changed, matching the `ollama`/`llama_cpp`/`open_router`/`api_compatible` pattern. This stops the warning spam even if some unrelated settings write fires the observer. **Why not the bridge?** The `update_settings_file_inner` fix is in upstream's `SettingsStore` impl — the bridge is for kask-specific behavior, not for patching upstream settings-store internals. This is a genuine D-seam: an upstream bug fix that benefits all Zed users (any caller that writes the same settings content twice — e.g. `nudge_mcp_servers` re-writing `kask.mcp.load_default` to itself, D9 — now skips the file write and observer storm). Should be reported upstream to `zed-industries/zed`. Pinned by `test_update_settings_file_skips_noop_write` (verifies that a no-op write does not touch the filesystem or fire observers) and `test_update_settings_file_writes_when_content_differs` (verifies the write still happens when content actually changes). |
| D33 | Swarm panel full CRUD + cloud↔local movement: composition editing, confirmation flow, cloud delete, metadata edit, swarm copy, port display, push/pull | `crates/swarm_panel/src/{detail.rs,card.rs,swarm_ops.rs,swarm_panel.rs,parse.rs,fetch.rs,hire.rs}` + `kask/mcp-servers/hkask-mcp-swarm/src/{local_swarms.rs,local_tools.rs,request_types.rs}` | Extends the swarm agent panel from read+add/remove to full management: **Edit swarm metadata** (local only — `swarm_update_local_swarm` tool; ABW has no PATCH endpoint, 405 verified), **Copy/Duplicate swarm** (`swarm_clone_local_swarm` for local; Compose pre-fill for ABW), **Delete swarm (both backends)** (`swarm_delete_local_swarm` for local, `swarm_delete_swarm` for ABW — `DELETE /api/teams/{id}`), **Destructive-op confirmation** (two-step: stage → confirm; shows active-run warning for ABW deletes), **Roster composition view** (each agent row shows `accepts`/`produces` port labels enriched from the local agent catalogue; local rosters cross-reference `swarm_list_local_agents`), **Cloud↔local swarm movement** (`swarm_push_local_swarm` creates ABW workspace from local swarm with consent-gated member hires; `swarm_pull_swarm_to_local` creates local swarm copy from ABW workspace, no cost), **`cloud_workspace_id` link tracking** (new `LocalSwarm` field; set by push/pull; shown as "synced with ABW" badge in detail view; drives `AgentSource::Synced` badge for swarms in browse list), and **UI terminology rename** ("Clone" → "Copy" everywhere). New MCP tools: `swarm_update_local_swarm`, `swarm_clone_local_swarm`, `swarm_push_local_swarm`, `swarm_pull_swarm_to_local`. New `LocalSwarmRegistry` methods: `update_metadata`, `clone_swarm`, `set_cloud_workspace_id`. New panel state: `DestructiveAction` enum (DeleteSwarm/RemoveAgent), `pending_destructive` confirmation flow, `editing_metadata` toggle, `edit_name_editor`/`edit_mission_editor`, `cloud_workspace_id` on `SwarmDetailView`. The `SwarmRosterAgent` struct gained `accepts`/`produces` fields; `parse_swarm_roster` extracts them from ABW roster objects; local rosters are enriched by cross-referencing `swarm_list_local_agents`. `SwarmCard` gained `cloud_workspace_id`; `LocalSwarmInfo` gained `cloud_workspace_id`; `filter_entries` includes `Synced` swarms in both Cloud and Local source filters. Swarm card "More actions" menu: Copy (both backends), Push to Cloud (local), Copy to Local (cloud). Pinned by `local_swarms::tests::{update_metadata_*, clone_swarm_*, cloud_workspace_id_*, set_cloud_workspace_id_*}` (backend, 9 tests) + swarm_panel tests for confirmation flow, tool-name pinning, and port parsing. |
| D34 | Batch inference API (OpenRouter + DeepInfra) via the IPC bridge | `kask/crates/hkask-types/src/{inference_ipc.rs, ports/inference_port.rs}` + `kask/crates/hkask-inference/src/{batch.rs, hkask_inference.rs, inference_ipc_client.rs}` + `kask/crates/kask_bridge/src/{inference_ipc_server.rs, inference_socket.rs, mcp_servers.rs, Cargo.toml}` + `kask/crates/hkask-mcp-server/src/server/validation.rs` + `kask/mcp-servers/hkask-mcp-corpus/src/{services/qa_pipeline.rs, tools/semantic/batch_api.rs, tools/semantic.rs, tools/storage.rs, services.rs, Cargo.toml}` | **Batch inference routing through the IPC bridge** — MCP servers submit batch LLM calls as a single `InferenceMethod::GenerateBatch` IPC request; the zed side holds the API keys (read from the keychain via a GPUI-side credential channel) and calls the provider's Batch API (OpenRouter `/api/beta/batches` at 50% cost discount, DeepInfra `/v1/openai/batches` at 20%). The MCP server never sees the credentials. **Provider detection** (`hkask_inference::batch::detect_batch_provider`): model names ending in `:batch` route to OpenRouter (suffix stripped); model names starting with `DeepInfra/` route to DeepInfra (prefix stripped); `HKASK_BATCH_PROVIDER` env var forces a provider. **IPC protocol additions**: `InferenceMethod::GenerateBatch` variant, `BatchPromptEntry`/`BatchResultEntry` structs, `batch_prompts`/`batch_max_tokens` fields on `InferenceParams`, `InferenceOutcome::BatchResults` variant. **`InferencePort::generate_batch` trait method** (default: error) implemented by `InferenceIpcClient` and `LazyInferencePort` — no direct fallback (batch requires the IPC bridge). **Batch IPC read timeout**: `IPC_BATCH_READ_TIMEOUT` (6h+60s) in `inference_ipc_client.rs` — batch calls use `read_response_line_with_timeout` instead of the default `ipc_read_timeout` (~330s) because batch processing can take hours. **Inference socket fallback file**: `set_inference_socket_path` writes the socket path to `$XDG_RUNTIME_DIR/kask/inference-socket-path`; `InferenceIpcClient::from_env` falls back to reading this file when `HKASK_INFERENCE_SOCKET` is absent (covers self-healing reconnects with stale `LaunchSpec` env). **Multi-root path containment** (`validation.rs`): `contain` now allows paths under CWD + kask data dir + kask artifacts dir (D28 standardized artifact storage) instead of CWD only. **Corpus server integration**: `corpus_generate_qa_batch` routes to `tools/semantic/batch_api.rs` when the model is batch-eligible; `services/qa_pipeline.rs` deduplicates the QA prompt formatting + result envelope construction shared by all three QA generation paths (single-chunk, concurrent batch, provider batch API); `tools/storage.rs` `corpus_query` accepts a Lisp S-expression query (`(list (list "query" "...") (list "top-k" 5) (list "include-text" t) (list "min-score" 0.6))`) parsed via `hkask_lisp::eval_sandboxed_with_budget`, plus `search_passages` extraction deduplicating the hydrated/in-memory search paths. **Dependencies**: `hkask-lisp` added to `hkask-mcp-corpus`; `zed_credentials_provider` added to `kask_bridge`. Pinned by `batch.rs::tests::{detect_openrouter_batch_suffix, detect_deepinfra_prefix, detect_non_batch_model_returns_none, detect_strips_batch_suffix_from_deepinfra_prefix, parse_successful_result, parse_error_result_includes_error_message, parse_result_with_no_choices_is_failed, parse_empty_lines_skipped, parse_mixed_success_and_failure, parse_malformed_line_returns_error, format_batch_jsonl_produces_valid_jsonl}` (11 tests) + `storage.rs::tests::{search_returns_results_sorted_by_score, search_top_k_truncates, search_min_score_filters, search_include_text_adds_text_field, search_empty_passages_returns_empty, parse_lisp_query_basic, parse_lisp_query_with_min_score, parse_lisp_query_missing_query_returns_error, parse_lisp_query_unknown_key_returns_error, parse_lisp_query_generate_answer}` (10 tests) + `validation.rs::tests::{contain_accepts_cwd_relative_path, contain_accepts_absolute_cwd_path, contain_accepts_data_dir_path, contain_accepts_artifacts_dir_path, contain_rejects_etc_passwd, contain_rejects_traversal_escape, contain_rejects_nonexistent_read_path}` (7 tests) + `qa_pipeline.rs::tests` (5 tests). |
| D35 | Media MCP server + MediaRouter + MovieLabs OMC ontology | `kask/mcp-servers/hkask-mcp-media/` (25 source files) + `kask/crates/hkask-inference/src/{media_router.rs, provider.rs, scoring.rs}` + `kask/crates/hkask-bridge-ontology/src/omc.rs` + `kask/crates/hkask-storage/src/gallery.rs` + `kask/crates/hkask-types/src/{voice.rs, ports/inference_port.rs (MediaGenerateParams, MediaFuture, media_generate trait method)}` + `kask/crates/hkask-inference/src/{inference_ipc_client.rs (call_media_generate + media_generate trait impl), model_constants.rs (DEFAULT_TTS/STT/VISION/IMAGE_GEN_MODEL)}` + `kask/crates/kask_bridge/src/{inference_ipc_server.rs (MediaGenerate dispatch), mcp_servers.rs (media server registration)}` + `kask/registry/templates/media/` (24 templates) + `.agents/skills/create-skill/SKILL.md` (OMC ontology row) + `Cargo.toml` (workspace member + nom-exif dep) | **Recovered media stack** — the media MCP server (36 tools: gallery management, image/video/audio generation, transcription, face recognition), the bespoke `MediaRouter` (pluggable `MediaProvider` trait + `ProviderRegistry` for non-chat media APIs that zed's `LanguageModel` cannot represent), and the MovieLabs OMC ontology bridge (9 seed concepts: CreativeWork, Scene, Shot, Sequence, Participant, MediaSource, Asset, Task, Version + `tool_to_omc` mapping for all 36 tools + `explain_tool_for` dispatch). **IPC bridge wiring**: `InferenceMethod::MediaGenerate` variant + `InferenceOutcome::Media` outcome + `InferenceParams::media_*` fields + `InferenceIpcClient::call_media_generate` + `InferencePort::media_generate` trait method (default: error; overridden by `InferenceIpcClient`). The zed-side `dispatch` function routes `MediaGenerate` to `port.media_generate()` which the `LanguageModelInferencePort` impl forwards to the `MediaRouter`. **GalleryStore**: SQLite-backed gallery with image indexing, tag persistence, face registry, generation lineage, and EXIF timeline grouping. Restored to `hkask-storage` with `SqliteDriver::file_pool` re-added for unencrypted file-backed pools. **Deprecated rJoule/budget/gas system stripped**: `budget.rs` deleted, all `charge_budget` calls removed from tool handlers, `VerificationStore` and `hkask-templates` dependencies removed. **Server registration**: `BuiltinMcpServer` entry with `HKASK_INFERENCE_SOCKET`, `HKASK_DATA_DIR`, `HKASK_MEDIA_DB`, and media model env vars in `config_env`; `OPENROUTER_API_KEY` in `credentials` (optional — vision routes through IPC bridge). Pinned by `media_router::tests::{media_router_without_keys_has_no_providers, media_generate_unknown_op_errors, media_generate_no_provider_errors_clearly}` + `media_providers::tests::{strip_prefix_removes_provider, extract_image_url_from_markdown, extract_image_url_from_raw_url, extract_image_url_from_json, extract_image_url_returns_none_for_plain_text, detect_audio_format_from_extension, detect_audio_format_defaults_to_mp3}` + `omc::tests` (14 tool-mapping tests) + `gallery.rs::tests` (25 tests including durability across restart) + `levenshtein_tests` (5 tests). |

| D36 | Chat Completions `finish_reason: "stop"` with accumulated tool calls → drain before stop | `crates/language_models/src/provider/open_router.rs` + `crates/open_ai/src/completion.rs` | Upstream's `OpenAiEventMapper` and `OpenRouterEventMapper` unconditionally emit `StopReason::EndTurn` on `finish_reason: "stop"`, discarding any tool calls accumulated during streaming. Some models (e.g., GLM 5.2 via OpenRouter) emit tool_call deltas during streaming but finish with `"stop"` instead of `"tool_calls"` — without the drain, the tool call is silently lost and only the preamble text appears in the chat. Zed-kask adds a guard: when `finish_reason: "stop"` and `tool_calls_by_index` is non-empty, drain the accumulated tool calls as `ToolUse` events and emit `StopReason::ToolUse` instead of `EndTurn`. Mirrors the existing `"tool_calls"` finish_reason arm (which already drains). Should be reported upstream — this is a general OpenAI-compatible provider bug, not kask-specific. Pinned by `test_tool_calls_drained_on_finish_reason_stop` in `open_router.rs`. |

## Other zed-kask-modified files (supporting D1–D36)

| D26 | Tool-use warnings in system prompt template | `crates/agent/src/templates/system_prompt.hbs` + `kask/crates/kask_bridge/src/context_injector.rs` | The upstream Zed agent harness prompt carries general tool-use guidance ("Do not waste tokens by re-reading files...", "send a brief preamble...", a 3-strikes loop rule), and the built-in tools (`read_file`, `edit_file`, `terminal`) are upstream code with known failure modes (`read_file` returning "tool input was not fully received", `edit_file` `old_text` mismatch loops, opaque `terminal` commands). The kask-specific failure-mode warnings (`read_file` re-read-after-success and "tool input was not fully received" fallback to `terminal`; `edit_file` surgical edits and one-retry-on-mismatch; `terminal` preamble requirement and `timeout_ms`) are rendered as an unconditional `## Tool failure-mode warnings (kask)` section in `system_prompt.hbs`. **Refactored 2026-08-25:** previously delivered via `BridgeContextInjector::inject_static_context` (a runtime injection returning `Some(TOOL_WARNING_PROMPT)`); now baked into the template directly. The `inject_static_context` method, `Thread.static_context` field, and `static_context_loaded` flag were deleted. Thread-scoped memory recall was folded into the per-turn `inject_context` path so memory is fresh at decision time. Pinned by `test_system_prompt_contains_tool_failure_mode_warnings` in `templates.rs`. |

These files carry `// zed-kask:` comments but are supporting edits, not
primary divergence seams:

- `crates/client/src/client.rs` — `Client::credentials()` accessor.
- `crates/cli/src/main.rs` — `zed-kask://` added to `URL_PREFIX` array.
- `crates/zed_actions/src/lib.rs` — `OpenApplicationUrl` action doc references `zed-kask://`.
- `crates/gpui/src/app.rs` — `register_url_scheme` doc example uses `zed-kask`.
- `crates/terminal/src/alacritty/hyperlinks.rs` — `zed-kask://` added to URL regex (hardcoded — terminal doesn't depend on `paths` crate).
- `crates/agent/src/tool_router.rs` — `LazyToolRouter` filters MCP tools only; built-in tools bypass the router.
- `crates/agent/src/kask_thread_state.rs` — **KaskThreadState**: all kask-specific per-thread state (D2/D6/D25) grouped into one struct so upstream `Thread`'s struct definition stays clean and upstream rebases touch one field init instead of nine. Methods on this struct encapsulate the kask-specific turn-loop behaviors that were previously inline in `Thread`'s methods (`render_system_prompt`, `enabled_tools`, `handle_tool_use_event`, `handle_completion_event`, `flush_pending_message`, `cancel`, `run_turn_internal`, `new_subagent`). See `kask/docs/plans/thread-hooks-refactor-2026-08-26.md` for the full refactoring plan. Pinned by `test_curator_memory_edit_tool_classification`, `test_last_completion_truncated_distinguishes_max_tokens_from_cancel`, `test_system_prompt_override_bypasses_template`, `test_cached_filtered_context_reuses_on_unchanged_inputs`, `test_tool_retry_tracker_integration_in_thread` (5 Phase 0 pinning tests added before the field moves).
- `crates/agent/src/templates.rs` (and `system_prompt.hbs`) — **full structure + divergence catalogue: [`kask/docs/architecture/AGENT_SYSTEM_PROMPT.md`](kask/docs/architecture/AGENT_SYSTEM_PROMPT.md)** (includes the rebase procedure for this file and the four prompt surfaces — base template plus the Curator/swarm/kanban overlays). Agent Skills system-prompt section follows upstream wording (body retrieval via `skill` tool). Additional `system_prompt.hbs` divergences in shared upstream sections: (1) a loop-termination guardrail added to `## Task Execution` ("If a tool loop repeats without measurable progress… stop, summarize what you tried, and ask the user rather than continuing indefinitely"); (2) the mermaid diagram-type list names the exact directives merman requires (`sankey-beta`, `xychart-beta`, `architecture-beta`, `radar-beta`) rather than bare forms the renderer drops, keeps `kanban` (it **is** a supported mermaid directive — see `markdown::mermaid::SUPPORTED_PREFIXES`), and separately notes that the `graph`/`media`/`portfolio`/`scenarios` fenced blocks are kask viz widgets, plus that a ```kanban fenced block is a widget while `kanban` inside a ```mermaid block is a diagram. The prompt is pinned against the renderer allowlist by `test_system_prompt_advertises_every_supported_diagram_type` in `crates/markdown/src/mermaid.rs`, which replaces the former "if updating this list, also update the system prompt!" manual-sync comment; (3) the `## Session Context` (`static_context`) block is a sibling of the `{{#if (or user_agents_md has_rules)}}` custom-instructions guard, not nested inside it (see D2). Pinned by `templates::tests::test_system_prompt_contains_loop_termination_guardrail` (which also asserts the guardrail states a concrete iteration count, not a vague quantifier), `..._mermaid_list_uses_renderer_directives`, and `..._renders_session_context_without_rules_or_agents_md`.
- `crates/agent_ui/src/conversation_view/thread_view.rs` — `render_skill_loading_issues` only shows `LoadFailed` (description-length + catalog-budget issues disabled).
- `crates/agent_skills/agent_skills.rs` — `global_skills_dir()` relocated to the kask data root via `set_global_skills_dir_override` hook (D28 — Standardized Artifact Storage). Legacy migration from `~/.agents/skills/` removed (pre-release). `GLOBAL_SKILLS_DIR_DISPLAY` updated. `SkillSource::Global` docs updated. `load_marketplace_skills` and `SkillSource::Public` removed (skill marketplace retired).
- `crates/settings_ui/src/pages/skills_setup.rs` — skill listing/editing UI.
- `crates/zed/src/zed/open_listener.rs` — `zed-kask://` URL scheme parsing + tests.
- `crates/windows_resources/src/windows_resources.rs` — dev-channel window title `Kask` (D7 rebrand).
- `crates/gpui_tokio/src/gpui_tokio.rs` — `Tokio::handle_async` helper (get the tokio handle from `AsyncApp`); 3 call sites in `main.rs` deferred wiring.
- `crates/prompt_store/src/prompts.rs` + `crates/agent/src/agent.rs` + `crates/agent/src/thread.rs` — `RuleFrontmatter` (Cline-compatible `alwaysApply`/`globs` rules frontmatter) + `filter_conditional_rules` enforcement on the prompt-render path.
- `crates/agent/src/tools/terminal_tool.rs` — truncation spillover file (full output saved to temp file, path returned for `read_file`), head/tail overlap fix in `select_terminal_output_lines` (upstream candidate), relaxed shell-substitution doc wording (single-quoted `${...}` is safe).
- `crates/agent/src/tools.rs` — `deserialize_optional_u64_from_maybe_string` (models sometimes send `timeout_ms` as a string).
- `crates/agent_ui/src/agent_panel.rs` — eager `SkillIndex` population + `create_thread_with_options` returns the agent used (Curator support, D2).
- `crates/agent_ui/src/conversation_view.rs` — `render_agent_markdown` wires `.media_block_renderer(hkask_viz_core::block_renderer())` for D18 (`media and `graph fenced block rendering); the `hkask-viz-core` registry composes the media + graph widget renderers.
- `crates/language_model/src/language_model.rs` + `crates/language_model/src/registry.rs` — `api_url()` accessor.
- `crates/open_ai/src/list_models.rs` — generic `/v1/models` discovery for OpenAI-compatible providers (D12 support).
- `crates/zed/src/main.rs` (beyond the composition-root blocks) — kask MCP server auto-launch, `sync_kask_mcp_servers` re-sync when the inference socket becomes available, `mirror_kask_credentials_to_providers` (mirrors keys from the kask credential store to each provider's `api_url`). The `.env` file loading has been fully removed — the keychain (zed's `CredentialsProvider` under `kask://credentials/<key>`) is the single source of truth for API keys.
- `crates/client/src/zed_urls.rs` — shared-session URL uses `ZED_URL_SCHEME` (D7).
- `crates/zed/src/reliability.rs` — `start_context_server_health_polling` + `compute_context_server_health_snapshot` + `classify_fleet_health` + `context_server_fleet_observable`: polls all open projects' `ContextServerStore`s every 10s and feeds a health snapshot to the cybernetics loop's `ContextServerHealthSensor` via `BridgeContextServerHealthSource`. Only `Starting`, `Running`, and `Error` count toward the fleet total; `Stopped` and OAuth-waiting states (`AuthRequired`, `ClientSecretRequired`, `Authenticating`) are excluded — they are legitimate non-running states, not fleet degradation. Counting them produced constant false-positive `ContextServerFleetDegraded` alerts. The `context_server_fleet_observable` gate suppresses the snapshot (returns `(0,0)`) while `Client::user_id()` is `None` — pre-login, kask's deferred servers haven't launched and credential-gated servers are stuck in `Starting`, which previously produced the `ContextServerFleetDegraded` alert storm every tick. Pinned by `reliability::tests::{all_running_is_fully_healthy, starting_counts_toward_total_but_not_healthy, error_counts_toward_total_but_not_healthy, stopped_servers_are_excluded_from_total, oauth_waiting_states_are_excluded_from_total, all_stopped_produces_zero_total_no_alert, all_starting_signals_degradation, mixed_real_and_excluded_states, empty_slice_is_zero_zero, pre_login_fleet_is_unobservable, resolved_user_opens_the_gate}`.

## hKask workspace members (additive — upstream never touches)

The following are added to the root `Cargo.toml` `[workspace.members]` array.
Upstream merges never conflict with these paths.

- `kask/crates/` — hKask crates (`hkask-types`, `hkask-storage`, `hkask-memory`, `hkask-regulation`, `hkask-tool-port`, `hkask-keystore`, `hkask-ledger`, `hkask-event-store`, `hkask-mcp`, `hkask-mcp-server`, `hkask-inference`, `hkask-condenser`, `hkask-bridge-ontology`, `hkask-services-core`, `hkask-email`, `hkask-forecast`, `hkask-lisp`) plus the `kask_bridge` bidirectional seam (D8, zed-kask-side). The `hkask-{media,graph}-widget` and `hkask-viz-core` viz crates live under `crates/` (zed-kask-side, see D18) because they render GPUI elements and must depend on `gpui`/`theme`, which §13.1 forbids for hKask crates.
- `kask/mcp-servers/` — 10 MCP server crates (`hkask-mcp-{companies,corpus,curator,kata-kanban,portfolio,prediction-markets,research,scenarios,swarm,training}`). The condenser library crate (`kask/crates/hkask-condenser`) provides in-process thread condensation via `kask_bridge::BridgeThreadCondenser`.
- `crates/marketplace_ui_common` — shared catalog-page chrome (`MarketplaceCard`, search bar, empty state) used by `swarm_panel` and `kanban_panel`

## Governing invariant (§13.1)

**hKask crates NEVER depend on zed-kask crates; zed-kask depends on hKask.**
The sole bidirectional seam is `kask_bridge` (D8), which lives under
`kask/crates/` and depends on both sides. Enforced by
`kask/scripts/check-hkask-no-zed-deps.sh` (wired into CI).

## Upstream-sync runbook

1. `git fetch upstream && git merge upstream/main`
2. Conflicts will only appear in:
   - The D-seam files listed above (D1–D33; D17 and D19 are retired)
   - `[workspace.members]` / `[workspace.dependencies]` in root `Cargo.toml`
     and `crates/zed/Cargo.toml` (kask deps + upstream's crate renames, e.g.
     `csv_preview` → `tabular_data_preview`).
   - `Cargo.lock` — regenerate from the merged manifests (do not hand-merge;
     `cargo check --workspace` rewrites it).
   - `kask/deny.toml` `allow-git` list — upstream may adopt new git forks
     (e.g. `zed-industries/async-tar`, `tree-sitter/tree-sitter`). Add the new
     source URLs; verify with `cargo deny --config kask/deny.toml check`.
   - `typos.toml` — upstream may add new identifiers; merge into the kask
     allowlist (kask's is the superset).
   - Modify/delete conflicts — upstream may restore files zed-kask deliberately
     deleted under D7/D16 (icons, `.desktop` templates, bundle scripts, release
     workflows, community-bot workflows). Resolve by re-deleting; verify with
     `bash kask/scripts/build/check-zed-isolation.sh`.
3. Everything under `kask/` is additive → never conflicts. (Exception: kask
   tracing targets using the `reg.*` prefix must be registered in
   `CANONICAL_NAMESPACES` or retargeted to `hkask.*` — the reg-canonical gate
   scans `kask/crates/` and `kask/mcp-servers/`. Performative log targets
   belong on `hkask.*`, not `reg.*`.)
4. After resolving, run `bash kask/scripts/check-hkask-no-zed-deps.sh` to
   verify the §13.1 invariant still holds.
5. Run `cargo check -p kask_bridge -p hkask-types -p hkask-mcp-server` to
   verify the bridge + foundation still compile.
6. Run `bash kask/scripts/build/check-zed-isolation.sh` — verifies deleted
   upstream surfaces (icons, `.desktop` templates, bundle scripts, release
   workflows) were not silently restored by the merge.
7. Run `./script/clippy` (per `.rules` build guidelines — use this instead of
   `cargo clippy`; runs under `--deny warnings`). A `rust-toolchain.toml` bump
   may promote previously-allowed lints to denied — clippy catches these.
8. Run the full verification gate:
   - `cargo fmt --all -- --check`
   - `cargo nextest run --no-fail-fast -p 'hkask-*' -p kask_bridge`
   - `cargo deny --manifest-path Cargo.toml --config kask/deny.toml check`
   - `cargo machete`
   - `typos --config typos.toml`
   - `buf lint crates/proto/proto` && `buf format --exit-code crates/proto/proto`
   - `cd kask && bash scripts/check-unused-deps.sh` (nightly)
   - All `kask/scripts/check-*-selftest.sh` scripts
