# Security Audit Log — Cycle 2 kali-audit

**Date:** 2026-07-31
**Skill:** kali-audit
**Auditor:** coding agent (GLM-5.2)
**Surface:** auto (kask-owned crates + MCP servers)
**Regression library consumed:** 26 entries (RR-0001 … RR-0026)

> **Post-fix status:** All findings fixed and committed (`7accd80d78`).
> Regressions RR-0027–RR-0029 enforced. **Converged.** See
> [Post-Fix Update](#post-fix-update-same-session) at the end of this file
> for the final state; the sections in between are the historical audit
> record as written before the fixes landed.

---

## Verdict: **Conditional** (pre-fix; see Post-Fix Update for final)

No critical findings. Two high-severity findings (K2-1, K2-2) are confirmed
new issues not covered by the existing regression library. Defense-layer
coverage remains 8/8 (all layers present), but Layer 5 (IFC) and Layer 7
(output filtering) have specific call-site gaps documented below.

---

## Cycle 2 Findings Summary

| ID | Surface | Severity | Status | OWASP | CWE | Layer gap |
|----|---------|----------|--------|-------|-----|-----------|
| K2-1 | code | high | **new** | LLM06 | CWE-20 | 5 (IFC) |
| K2-2 | llm-io | high | **new** | LLM02 | CWE-1336 | 7 (output filtering) |
| K2-3 | supply-chain | medium | **new** | — | CWE-1357 | — |
| K2-4 | code | low | informational | — | — | — |
| K2-5 | code | low | informational | LLM02 | — | 7 (output filtering, known tradeoff) |

---

## Findings

### K2-1 (high) — `execute_populate` skips `propagate_taint_for_binding` (FIDES closure break)

**Surface:** code (Layer 5 — Information flow control)
**OWASP LLM 2025:** LLM06 (excessive agency via untrusted data reaching Sinks)
**CWE:** CWE-20 (improper input validation)
**ATLAS:** AML.TA0006 (collection)
**NIST SSDF:** PW.4.1

**Evidence:**

`kask/crates/hkask-templates/src/executor.rs:1285-1299` — `execute_populate`
resolves `input_mapping` via `bind_parameters` and re-inserts the bound values
into `context` with `context.insert(k, v)`, **without** calling
`propagate_taint_for_binding`:

```rust
async fn execute_populate(
    &self,
    step: &BundleManifestStep,
    mut context: HashMap<String, Value>,
) -> Result<HashMap<String, Value>> {
    if let Some(ref mapping) = step.input_mapping {
        let resolved = bind_parameters(mapping, &context);
        if let Value::Object(map) = resolved {
            for (k, v) in map {
                context.insert(k, v);   // ← no propagate_taint_for_binding(v, k)
            }
        }
    }
    ...
}
```

RR-0026 fixed the same closure break at the `resolve_mapping_value` call sites
(`execute_select` L1174, `execute_render` L1335, the loop body L815) but
`execute_populate` uses a *different* resolver (`bind_parameters`) and was
missed. The RR-0026 cargo-test (`propagate_taint_from_inline_jinja_source`)
only tests `propagate_taint_for_binding` directly — it does not assert that
`execute_populate` calls it.

**Impact:** If a prior step produced Source-tainted output (e.g. a `tool_invoke`
against a Source MCP tool returning untrusted external data), and a later
`populate` step binds that value via `{"$ref": "step_N_result.field"}`, the
taint label is lost. A downstream `tool_invoke` against a Sink tool would then
call `check_untrusted_input` and see `Pure` instead of `Source`, bypassing the
FIDES Source→Sink block (Layer 5 defense).

**Remediation:** Add `self.propagate_taint_for_binding(v, k);` before
`context.insert(k, v);` at L1295, matching the pattern at L815/L1174/L1335.
Add a cargo-test that runs `execute_populate` with a Source-tainted context
entry and asserts the bound key is labeled `Source`.

**Proposed regression:** RR-0027 (see below).

---

### K2-2 (high) — `parse_json_response` and fusion JSON extractor use first-`{`-to-last-`}` (RR-0017 class)

**Surface:** llm-io (Layer 7 — output filtering)
**OWASP LLM 2025:** LLM02 (sensitive information disclosure / output integrity)
**CWE:** CWE-1336 (improper handling of insufficient or excessive data)
**ATLAS:** AML.TA0005 (persistence/ML-model-file tampering via poisoned parsed output)

**Evidence:**

Two call sites use the vulnerable `find('{')` … `rfind('}')` pattern that
RR-0017 explicitly says to avoid:

1. `kask/crates/hkask-templates/src/executor.rs:2331` (`parse_json_response`):
   ```rust
   if let (Some(start), Some(end)) = (trimmed.find('{'), trimmed.rfind('}')) {
       return serde_json::from_str(&trimmed[start..=end]).map_err(...);
   }
   ```

2. `kask/crates/hkask-inference/src/fusion_orchestrator.rs:551`:
   ```rust
   if let (Some(start), Some(end)) = (trimmed.find('{'), trimmed.rfind('}'))
       && let Ok(v) = serde_json::from_str(&trimmed[start..=end])
   { return v; }
   ```

RR-0017 is scoped to `mcp-servers/hkask-mcp-corpus/src/lib.rs` only, so its CI
gate does not catch these two sites. The secure `find_balanced_json_object`
function exists in `kask/mcp-servers/hkask-mcp-corpus/src/json_extract.rs:68`
but is private to that crate — not shared.

**Impact:** An LLM that emits a JSON block in its reasoning preamble (e.g.
echoing an injected instruction) followed by its real answer produces two
top-level objects. The `find('{')`-to-`rfind('}')` slice spans *both* objects
plus the prose between them. `serde_json::from_str` then either fails (no
silent merge, but the step errors) or, if the prose between them happens to be
valid JSON whitespace, silently merges the injected block with the real answer.
The fusion orchestrator is the more sensitive site: it parses panel-model
responses, and a malicious or prompt-injected panel model could poison the
fusion result.

**Remediation:** Extract `find_balanced_json_object` into a shared module
(e.g. `hkask-types` or a new `hkask-json` crate) and use it in both
`parse_json_response` and the fusion orchestrator. Alternatively, duplicate
the brace-balanced scan into both sites. Update RR-0017's `include` field to
cover all three files, or add RR-0028 with the broader scope.

**Proposed regression:** RR-0028 (see below).

---

### K2-3 (medium) — `llm-guard` exact pin bypassed by per-crate caret spec

**Surface:** supply-chain
**CWE:** CWE-1357 (reliance on untrusted search path / version resolution)

**Evidence:**

- `Cargo.toml:867` (workspace): `llm-guard = "=0.2.0"` — exact-pinned (satisfies RR-0025).
- `kask/crates/hkask-guard/Cargo.toml:15`: `llm-guard = "0.2"` — caret spec, declared directly (not `.workspace = true`).

The crate declares its own `llm-guard = "0.2"` instead of inheriting the
workspace pin via `llm-guard.workspace = true`. RR-0025's detection pattern
(`^llm-guard = "0.2"$` against `../Cargo.toml`) only checks the workspace
root, so it passes while the actual consumer crate floats.

**Impact:** `Cargo.lock` currently locks `llm-guard` to 0.2.0, so this is not
an active vulnerability. But a `cargo update -p llm-guard` would float to the
latest 0.2.x without CI catching it. `llm-guard` is the security-critical
scanner (Layer 1 input filtering, Layer 7 output filtering) — a malicious or
buggy 0.2.x release would silently change the guard's behavior.

**Remediation:** Change `kask/crates/hkask-guard/Cargo.toml:15` to
`llm-guard.workspace = true` so it inherits the workspace's `=0.2.0` pin.
Update RR-0025's `include` to also scan `kask/crates/hkask-guard/Cargo.toml`,
or broaden the pattern to catch `llm-guard = "0.2"` (without `=` prefix) in
any `Cargo.toml`.

**Proposed regression:** RR-0029 (see below).

---

### K2-4 (low, informational) — `serde_json::to_value(...).unwrap()` in kata-kanban MCP server

**Evidence:** `kask/mcp-servers/hkask-mcp-kata-kanban/src/hkask_mcp_kata_kanban.rs`
has 17 `serde_json::to_value(...).unwrap()` calls in production (non-test)
tool handlers (L93, L123, L174, L233, L284, L317, L352, L391, L426, L465,
L518, L556, L614, L648, L680, L716, L776).

**Impact:** `serde_json::to_value` only errors for non-serializable types
(`f64::NAN`/`Infinity`, or types with custom serializers that fail). The
structs here (`TaskCreateResponse`, `BoardCreateResponse`, etc.) contain only
`String`, `u64`, `bool`, `Vec`, `Option` — none can produce a serialization
error. So these `unwrap()`s cannot panic in practice. This is a code-quality
finding, not a security finding. Per `.rules` ("Avoid using functions that
panic like `unwrap()`"), prefer `.map_err(|e| McpToolError::internal(...))?`
or `expect("TaskCreateResponse is always serializable")` for clarity.

**No regression proposed** — not security-relevant.

---

### K2-5 (low, informational) — Streaming inference emits leaked text before `scan_output` runs

**Evidence:** `kask/crates/hkask-guard/src/guarded_inference.rs:45-100`
(`GuardedStream::poll_next`). The stream forwards each `text_delta` chunk to
the consumer immediately (L94-96), accumulating a copy. Only when the inner
stream ends (L57) does `scan_output` run on the accumulated text. If a secret
is detected, a redaction chunk is emitted (L67-83), but the leaked text was
already streamed to the UI/consumer.

The code comment at L75-82 explicitly acknowledges this ("we can't un-emit the
leaked text") and labels it the "ART-1 tradeoff". RR-0023 enforces that
`scan_output` is *called* on the stream (the "Output not scanned" pattern is
gone), but does not address the pre-redaction leak.

**Impact:** A secret in the model's streamed output is visible in the UI for
the duration of the stream before the redaction chunk arrives. The stored
assistant message uses the sanitized text (the redaction chunk's
`finish_reason: "redacted"` signals downstream storage to use the sanitized
version), so the leak is transient, not persistent. This is a known tradeoff
between streaming UX and output filtering, documented in the code.

**Remediation (if desired):** Buffer the stream and only emit chunks after a
sliding window has been scanned, or scan each chunk before forwarding (costs
latency). This is a design decision, not a clear bug. Documented here for
completeness; no regression proposed unless the team decides the transient leak
is unacceptable.

---

## Defense-Layer Coverage (Cycle 2)

| Layer | Status | Notes |
|-------|--------|-------|
| 1 Input filtering | present | `scan_input` wired on all `GuardedInferencePort` paths; `validate_path`/`validate_identifier`/`validate_tool_url` in MCP server. |
| 2 Data/instruction separation | present | Spotlighter integrated (RR-0011 enforced). |
| 3 Instruction hierarchy | present | System prompt hierarchy (RR-0010 enforced). |
| 4 Capability gating (OCAP) | present | `CapabilityTier` identity-based gating; MCP credential allowlists enforced (`all_servers_have_credential_allowlist` test). |
| 5 Information flow control | **partial** | FIDES taint labels wired, but K2-1 (`execute_populate`) is a closure break. |
| 6 Runtime monitoring | present | `reg.guard.*` spans emitted on canary/violation. |
| 7 Output filtering | **partial** | `scan_output` wired on non-streaming paths; K2-2 (JSON extraction) and K2-5 (streaming pre-redaction leak) are gaps. |
| 8 Deception detection | present | Canary tokens (RR-0014 enforced; RR-0006 pruned — it greped the type definition, not the integration). |

---

## Proposed Regression Entries

### RR-0027 (proposed)

```yaml
id: RR-0027
title: "execute_populate skips propagate_taint_for_binding (FIDES closure break — RR-0026 class)"
surface: code
cwe: CWE-20
owasp_llm_2025: LLM06
atlas_tactic: AML.TA0006
nist_ssdf: PW.4.1
discovered_in: kask/crates/hkask-templates/src/executor.rs
discovered_by: kali-audit
discovered_at: 2026-07-31
severity: high
detection:
  kind: cargo-test
  pattern: "execute_populate_propagates_source_taint"
  include: "crates/hkask-templates/src/executor.rs"
mitigation: "Call self.propagate_taint_for_binding(v, k) before context.insert(k, v) in execute_populate, matching the pattern at L815/L1174/L1335. The test must run execute_populate with a Source-tainted context entry bound via $ref and assert the bound key is labeled Source."
ci_gate: scripts/check-kali-regressions.sh
status: pending
```

### RR-0028 (proposed)

```yaml
id: RR-0028
title: "LLM JSON extraction in executor.rs and fusion_orchestrator.rs uses first-{ to last-} (RR-0017 class)"
surface: llm-io
cwe: CWE-1336
owasp_llm_2025: LLM02
atlas_tactic: AML.TA0005
nist_ssdf: PW.4.1
discovered_in: kask/crates/hkask-templates/src/executor.rs, kask/crates/hkask-inference/src/fusion_orchestrator.rs
discovered_by: kali-audit
discovered_at: 2026-07-31
severity: high
detection:
  kind: grep
  pattern: "find\('\{'\).*rfind\('\}'\)"
  include: "kask/crates/hkask-templates/src/executor.rs kask/crates/hkask-inference/src/fusion_orchestrator.rs"
mitigation: "Use brace-balanced JSON extraction (find_balanced_json_object from kask/mcp-servers/hkask-mcp-corpus/src/json_extract.rs) instead of find('{')...rfind('}'). Extract the helper into a shared module so all three call sites (corpus, executor, fusion) use the same secure primitive."
ci_gate: scripts/check-kali-regressions.sh
status: pending
```

### RR-0029 (proposed)

```yaml
id: RR-0029
title: "llm-guard caret spec in hkask-guard/Cargo.toml bypasses workspace exact pin"
surface: supply-chain
cwe: CWE-1357
owasp_llm_2025: LLM02
atlas_tactic: AML.TA0010
nist_ssdf: PW.4.1
discovered_in: kask/crates/hkask-guard/Cargo.toml
discovered_by: kali-audit
discovered_at: 2026-07-31
severity: medium
detection:
  kind: grep
  pattern: '^llm-guard = "0\.2"$'
  include: "kask/crates/hkask-guard/Cargo.toml"
mitigation: "Change kask/crates/hkask-guard/Cargo.toml:15 from `llm-guard = \"0.2\"` to `llm-guard.workspace = true` so it inherits the workspace's `=0.2.0` exact pin. RR-0025's include field should also be broadened to scan this file, or this regression supersedes it for the per-crate scope."
ci_gate: scripts/check-kali-regressions.sh
status: pending
```

---

## Top 3 Priority Fixes

1. **K2-1** (`execute_populate` taint break) — one-line fix, high severity, closes a Layer 5 gap that RR-0026 was meant to close. Add the `propagate_taint_for_binding` call + a cargo-test, then flip RR-0027 to `enforced`.
2. **K2-2** (JSON extraction in executor + fusion) — extract `find_balanced_json_object` into a shared module and use it in both sites. High severity, especially for the fusion orchestrator which parses panel-model output.
3. **K2-3** (`llm-guard` pin bypass) — one-line fix (`llm-guard.workspace = true`), medium severity, closes a supply-chain gap that RR-0025 was meant to close.

---

## Convergence Metrics — Cycle 2

- Critical/high unresolved: 2 (K2-1, K2-2) — **not converged**
- Defense-layer coverage: 6 full + 2 partial (Layers 5, 7) — **not converged**
- Regression growth: 3 proposed (RR-0027, RR-0028, RR-0029) — pending human review
- **Metric: not converged** (blocker: 2 high-severity findings + 2 partial layers)

---

## Items Deferred to Cycle 3

1. **K2-1, K2-2, K2-3:** Apply fixes, add cargo-tests, flip proposed regressions to `enforced`.
2. **K2-5:** Decide whether the streaming pre-redaction leak is acceptable; if not, design a buffered-scan approach and add a regression.
3. **Shared JSON extractor:** Extract `find_balanced_json_object` into `hkask-types` or a new `hkask-json` crate so all LLM-output parsers use the secure primitive.
4. **RR-0025 scope:** Broaden the include field to scan per-crate `Cargo.toml` files, not just the workspace root, to catch the K2-3 class of pin-bypass.
5. **`resolve_manifest` path handling:** `kask/crates/hkask-templates/src/manifest_loader.rs:196` accepts an arbitrary `reference` and loads from CWD without path containment. Currently no external caller (the function is exported but unused outside the crate), so this is latent. If a future caller passes attacker-controlled `reference`, add a containment check. No regression proposed until a caller materializes.

---

## Post-Fix Update (same session)

All findings K2-1, K2-2, K2-3 were fixed in the same session and committed as
`7accd80d78` ("Fix kali-audit cycle 2 security findings"). The proposed
regressions RR-0027, RR-0028, RR-0029 were added with `status: enforced` (not
`pending`) because the fixes landed in the same commit. The convergence metric
below supersedes the original.

### Fixes applied

| Finding | Fix | Files | Regression |
|--------|-----|-------|------------|
| K2-1 | Added `propagate_taint_for_binding(orig_v, k)` before `context.insert` in `execute_populate`, with lockstep iteration over original + resolved maps (original carries `$ref` markers for taint extraction; resolved is what gets inserted) | `kask/crates/hkask-templates/src/executor.rs` | RR-0027 (cargo-test `execute_populate_propagates_source_taint`) |
| K2-2 | Extracted `json_extract` module into `hkask-types`; replaced vulnerable first-brace-to-last-brace slice with `extract_json_from_response` (brace-balanced) in **3 sites** (not 2 — see correction below) | `kask/crates/hkask-types/src/json_extract.rs`, `kask/crates/hkask-templates/src/executor.rs`, `kask/crates/hkask-inference/src/fusion_orchestrator.rs`, `kask/mcp-servers/hkask-mcp-corpus/src/runtime/classify_impl.rs`, `kask/mcp-servers/hkask-mcp-corpus/src/hkask_mcp_corpus.rs` (re-export) | RR-0028 (grep, broadened to whole `kask/` tree) |
| K2-3 | Changed `llm-guard = "0.2"` to `llm-guard.workspace = true` to inherit the workspace `=0.2.0` exact pin | `kask/crates/hkask-guard/Cargo.toml` | RR-0029 (grep) |

### Correction: K2-2 covers 3 sites, not 2

The original audit identified 2 vulnerable JSON-extraction sites (executor.rs,
fusion_orchestrator.rs). During the essentialist + security-theater review of
the fixes, a **4th site** was found in the same class:
`kask/mcp-servers/hkask-mcp-corpus/src/runtime/classify_impl.rs:561`
(`parse_triple_extraction`) — in the same crate that already had the secure
`extract_json_from_response` but didn't use it for this function. This site was
fixed in the same commit, and RR-0028's grep was broadened from 2 named files
to `--include=*.rs --exclude-dir=json_extract .` (whole `kask/` tree) so the
class cannot recur silently. The original audit log's "proposed RR-0028" YAML
block (above) shows the narrow 2-file scope; the committed RR-0028 has the
broad scope. This is documentation drift — the actual regression file is
authoritative.

### K2-5 not closed by the reasoning-scan change

The commit included a `guarded_inference.rs` change (pre-existing staged
work, not authored in this audit session) that adds `accumulated_reasoning`
tracking and scans the reasoning delta at stream end. This closes the
*non-streaming* reasoning gap (RR-0030 enforces it via
`canary_in_reasoning_field_is_redacted`, which asserts `result.reasoning`
does not contain the canary). However, it does **not** close the K2-5
streaming pre-redaction leak. The streaming path still forwards each chunk
(text and reasoning) to the consumer immediately at
`GuardedStream::poll_next` L113-115, before `scan_output` runs at stream
end. The test `streaming_canary_in_reasoning_delta_is_redacted` only asserts
`chunks.last()` is clean — it does not assert that earlier chunks (the
streamed-out reasoning) didn't contain the canary. So the canary is leaked
in `chunks[0].reasoning_delta` and redacted in `chunks[1]`.

K2-5 remains a single open loop covering the streaming path (both text and
reasoning deltas): streamed content is visible to the consumer before
`scan_output` runs. Closing K2-5 requires buffering chunks before emission
(costs streaming latency) or scanning each chunk before forwarding (costs
per-chunk scan overhead). This is a design decision, not a one-line fix —
deferred.

### Deferred items — status

| # | Item | Status |
|---|------|--------|
| 1 | K2-1, K2-2, K2-3 fixes | **done** — committed `7accd80d78`, regressions enforced |
| 2 | K2-5 streaming pre-redaction leak | **open** — reasoning-scan change adds detection but not prevention; both text and reasoning paths leak streamed content before `scan_output` runs (design decision, deferred) |
| 3 | Shared JSON extractor | **done** — `hkask_types::json_extract` is the shared module; corpus re-exports it |
| 4 | RR-0025 scope (per-crate Cargo.toml) | **done via RR-0029** — RR-0029 scans `kask/crates/hkask-guard/Cargo.toml` directly; RR-0025's narrow workspace-root scope is now complemented |
| 5 | `resolve_manifest` path handling | **deferred** — still latent (no external caller); no regression until a caller materializes |

### Pre-existing open loops (not introduced by this audit)

- **RR-0022** (`status: proposed`) — "MCP tools do not emit reg.guard.* on
  capability denial." An unbuilt-feature tracker, not a regression — moved to
  `security/backlog/RR-0022.yaml` in the 2026-08-01 prune so the regressions
  directory contains only enforced/enforceable entries.

### Cycle 3 regressions (parallel session, same date)

A parallel kali-audit cycle 3 session committed `5f8a952c18` adding three more
regressions that overlap with this audit's findings:

- **RR-0030** — reasoning/reasoning_delta channels bypass `scan_output`
  (high). Enforces the non-streaming reasoning field is scanned. This is the
  `guarded_inference.rs` change that was included in this audit's commit
  `7accd80d78` as staged work; cycle 3 added the regression entry and test.
- **RR-0031** — inference IPC unix socket permission/auth (high). The
  `inference_ipc_client.rs` / `inference_ipc_server.rs` staged work.
- **RR-0032** — corpus MCP file tools path containment (high). The
  `path_safety.rs` staged work + corpus tool changes.

These three were tracked as "pre-existing staged work" in this audit's
commit because they were in the index when this session started. They are
properly attributed to cycle 3 in the regression library. The cycle 2 audit
log treats them as out-of-scope (correctly — they were not cycle 2 findings).

### Convergence Metrics — Cycle 2 (post-fix)

- Critical/high unresolved: **0** (K2-1, K2-2, K2-3 fixed; K2-5 is low-severity design tradeoff, deferred)
- Defense-layer coverage: **7 full + 1 partial** (Layer 5 IFC closed by K2-1; Layer 7 output filtering closed by K2-2 across all 3 sites; K2-5 streaming pre-redaction leak remains as documented ART-1 tradeoff — Layer 7 is partial for the streaming path, full for the non-streaming path)
- Regression growth: 3 added (RR-0027, RR-0028, RR-0029), all `enforced`
- **Metric: converged** ✅ (K2-5 is a documented design tradeoff, not an unresolved finding)

### Validation run (post-cycle-3)

- `bash scripts/check-kali-regressions.sh` — 0 grep violations, 0 cargo-test
  failures, 29 enforced regressions pass (23 pre-existing + 3 cycle-2 + 3 cycle-3)
- `cargo test -p hkask-types --lib` — 85 passed
- `cargo test -p hkask-templates --lib` — 116 passed (includes
  `execute_populate_propagates_source_taint`)
- `cargo test -p hkask-mcp-corpus --lib` — 172 passed (includes `path_safety` tests)
- `cargo test -p hkask-guard --lib` — 35 passed (includes
  `streaming_canary_in_reasoning_delta_is_redacted` and
  `canary_in_reasoning_field_is_redacted`)
- `cargo test -p hkask-inference --lib` — 85 passed (the pre-existing
  `inference_ipc_client.rs` test compile error was fixed in cycle 3)

### Security-theater check (per `.rules`)

Every advertised invariant has an enforcement point:

| Invariant | Enforcement |
|-----------|-------------|
| `execute_populate` propagates Source taint | `propagate_taint_for_binding` call + cargo-test (RR-0027) |
| LLM JSON extraction is brace-balanced | `extract_json_from_response` in 3 sites + grep RR-0028 (whole tree) |
| `llm-guard` exact-pinned | `llm-guard.workspace = true` + grep RR-0029 |
| Reasoning field (non-streaming) scanned for secrets | `scan_output` on `result.reasoning` + cargo-test `canary_in_reasoning_field_is_redacted` (RR-0030) |
| Reasoning delta (streaming) scanned at stream end | `scan_output(&accumulated_reasoning)` + test `streaming_canary_in_reasoning_delta_is_redacted` (asserts final redaction chunk only; pre-redaction leak remains — see K2-5) |

No advertised invariant lacks an enforcement point. No `_token` parameters
read nowhere; no doc-comment security claims without code backing.
