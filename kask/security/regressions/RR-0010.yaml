id: RR-0010
title: "Missing instruction hierarchy in system prompts"
surface: llm-io
cwe: CWE-1039
owasp_llm_2025: LLM01
atlas_tactic: AML.TA0005
nist_ssdf: PW.4.1
discovered_in: crates/hkask-types/src/agent_registry.rs
discovered_by: kali-audit
discovered_at: 2026-07-17
severity: medium
detection:
  kind: grep
  # RETIRED (2026-08-04, pass 3): the instruction-hierarchy defense layer
  # (System P10 > User P20 > Tool P30) was never deployed in this repo. The
  # original include path (crates/hkask-types/src/agent_registry.rs) never
  # existed in zed-kask git history — it referenced the pre-merge hKask layout.
  # No instruction-hierarchy text exists in any kask system prompt, registry
  # manifest, or Rust source. The pass-2 audit downgraded this entry to
  # `pending`; pass 3 took the operator decision to DE-ADVERTISE rather than
  # deploy: the "Layer 3 instruction hierarchy" references were removed from
  # the adversarial-red-team and kali-audit skill templates (renumbered the
  # 8-layer stack to 7 layers), and this entry is retired. The OpenAI
  # Instruction Hierarchy paper (arXiv:2404.13208) remains cited as an academic
  # source in kask/registry/manifests/kali-audit.yaml — that is a citation,
  # not a deployment claim. If the hierarchy text is ever deployed in a real
  # system prompt, re-open this entry with `status: enforced` and point
  # `include` at the real artifact path.
  semantics: presence
  pattern: 'Instruction Hierarchy'
  include: "crates/hkask-types/src/"
mitigation: "Add instruction hierarchy text to all system prompts: System (P10) > User (P20) > Tool output (P30). Tool output instructions must never be followed."
source: "https://arxiv.org/html/2404.13208v1 — OpenAI Instruction Hierarchy"
ci_gate: scripts/check-kali-regressions.sh
status: retired
