id: RR-0040
title: "ocap: manifest block re-introduction (declared config, not a security gate)"
surface: config
cwe: CWE-1188
owasp_llm_2025: LLM06
atlas_tactic: AML.TA0008
nist_ssdf: PW.4.1
discovered_in: kask/registry/manifests/
discovered_by: kali-audit
discovered_at: 2026-08-04
severity: medium
detection:
  kind: grep
  pattern: '^ocap:'
  include: "kask/registry/manifests/*.yaml kask/registry/templates/*/manifest.yaml"
mitigation: "The ocap: manifest block and OcapConfig struct were removed (2026-08-02). The real OCAP boundary is McpRuntime::invoke (capability match) + per-agent mcp_tools allowlist at ToolDispatchPort. Do not re-add an ocap: manifest block without wiring it into the runtime gate — silent manifest-only config is theater. See .rules: Manifest ocap: is declared config, not a security gate."
source: ".rules: Manifest ocap: is declared config, not a security gate"
ci_gate: scripts/check-kali-regressions.sh
status: enforced