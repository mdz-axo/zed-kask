[package]
name = "hkask-git-cas"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "gix-backed Git content-addressed storage adapter — the backup/gitcas component for registries and artifacts. The only crate that depends on gix."

[dependencies]
hkask-types = { path = "../hkask-types" }
gix = "0.81"
tokio = { workspace = true, features = ["rt-multi-thread"] }
async-trait.workspace = true

[dev-dependencies]
tempfile.workspace = true