[package]
name = "hkask-bridge-ontology"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Ontology bridge — Dublin Core + BIBO + CiTO + PKO core vocabulary and domain supplements (FIBO, ESO, GOLEM, OMC, ML-Schema). Single source of truth for ontology URIs and the dual-axis domain-selection logic."

[lib]
path = "src/hkask_bridge_ontology.rs"

[dependencies]
serde = { workspace = true, features = ["derive"] }
schemars = { workspace = true }

[lints]
workspace = true
