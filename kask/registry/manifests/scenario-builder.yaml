# Scenario Planning Process Manifest
# ℏKask v0.31.0 — Schwartz scenario planning as convergent PDCA

manifest:
  id: scenario-builder
  category: skill
  name: Scenario Builder
  description: >
    Convergent scenario planning process. Refines focal questions, maps forces,
    constructs scenario narratives, runs independent quality gate, extracts
    implications, computes convergence, and iterates until scenario set and
    indicators are decision-ready.
  functional_role: flowdef
  version: 0.31.0
  editor: curator-or-human-admin
  visibility: Public

convergence:
  threshold: 0.15
  improvement_gate: threshold_only
  max_iterations: 3
  min_iterations: 1
  convergence_field: step_7_result.convergence_metric
  on_not_reached: escalate

gas:
  cap: 100000
  cost_per_iteration: 100
  alert_threshold: 0.8
  hard_limit: true

rjoule:
  cap: 3
  alert_threshold: 0.8
  hard_limit: true

inputs:
  - name: focal_question
    type: string
    required: true
  - name: planning_horizon
    type: string
    required: false
  - name: domain
    type: string
    required: false
  - name: prior_convergence_metric
    type: number
    required: false
    description: Prior iteration's convergence metric for stall detection

fusion:
  judge: deepseek-v4-pro
  panel:
    - Kimi2.7
    - Qwen3.7 Max
    - GLM5.2
    - Minimax3
  mode: synthesis
  skills:
    - essentialist
  max_rounds: 3

steps:
  - ordinal: 1
    action: select
    description: Refine and bound focal question.
    renderer: minijinja
    template_ref: scenario-builder/focal-question
    gas_cap: 3000
    timeout_seconds: 30
    input_mapping:
      focal_question: "{{ focal_question }}"
      planning_horizon: "{{ planning_horizon | default('5 years') }}"
      domain: "{{ domain | default('general') }}"

  - ordinal: 2
    action: select
    description: Enumerate key microenvironment forces.
    renderer: minijinja
    template_ref: scenario-builder/key-forces
    gas_cap: 5000
    timeout_seconds: 60
    input_mapping:
      refined_question: "{{ step_1_result.refined_question | default(focal_question) }}"
      planning_horizon: "{{ planning_horizon | default('5 years') }}"
      domain: "{{ domain | default('general') }}"

  - ordinal: 3
    action: select
    description: Map macro driving forces and identify critical uncertainties.
    renderer: minijinja
    template_ref: scenario-builder/driving-forces
    gas_cap: 5000
    timeout_seconds: 60
    input_mapping:
      refined_question: "{{ step_1_result.refined_question | default(focal_question) }}"
      key_forces: "{{ step_2_result.key_forces | default([]) }}"

  - ordinal: 4
    action: select
    description: Generate 2x2 axes and scenario narratives.
    renderer: minijinja
    template_ref: scenario-builder/axes-and-narratives
    gas_cap: 8000
    timeout_seconds: 120
    input_mapping:
      refined_question: "{{ step_1_result.refined_question | default(focal_question) }}"
      critical_uncertainties: "{{ step_3_result.critical_uncertainties | default([]) }}"
      planning_horizon: "{{ planning_horizon | default('5 years') }}"

  - ordinal: 5
    action: select
    description: Independent quality gate — evaluates divergence, consistency, and coverage.
    renderer: minijinja
    template_ref: scenario-builder/scenario-quality-gate
    gas_cap: 4000
    timeout_seconds: 60
    input_mapping:
      refined_question: "{{ step_1_result.refined_question | default(focal_question) }}"
      scenarios: "{{ step_4_result.scenarios | default([]) }}"
    fusion: false

  - ordinal: 6
    action: select
    description: Extract implications, robust strategies, and indicators.
    renderer: minijinja
    template_ref: scenario-builder/implications-indicators
    gas_cap: 5000
    timeout_seconds: 60
    condition: "step_5_result.gate_pass"
    input_mapping:
      refined_question: "{{ step_1_result.refined_question | default(focal_question) }}"
      scenarios: "{{ step_4_result.scenarios | default([]) }}"

  - ordinal: 7
    action: select
    description: Compute normalized convergence metric for scenario cycle.
    renderer: minijinja
    template_ref: scenario-builder/scenario-convergence-check
    gas_cap: 3000
    timeout_seconds: 30
    input_mapping:
      quality_gate_result: "{{ step_5_result }}"
      narratives_result: "{{ step_4_result }}"
      implications_result: "{{ step_6_result }}"
      prior_convergence_metric: "{{ prior_convergence_metric | default(null) }}"
    fusion: false

  - ordinal: 8
    action: loop
    description: >
      Re-enter scenario cycle if convergence is not met. Restarts at narrative
      generation (step 4) so the refined focal question (step 1), key forces
      (step 2), and driving-forces/axes (step 3) are preserved across
      iterations. The quality gate diagnoses narrative divergence,
      consistency, and coverage — all fixed by regenerating scenarios, not by
      re-rolling the already-bounded focal question.
    input_mapping:
      loop_target: 4
      prior_convergence_metric: "{{ step_7_result.convergence_metric }}"

error_handling:
  on_gas_exceeded: abort
  on_timeout: retry
  max_retries: 1
  retry_backoff_seconds: 1
  on_validation_failure: abort

ocap:
  required_capabilities:
    - resource: template
      action: render
      template_id: scenario-builder/focal-question
      gas_budget: 3000
    - resource: template
      action: render
      template_id: scenario-builder/key-forces
      gas_budget: 5000
    - resource: template
      action: render
      template_id: scenario-builder/driving-forces
      gas_budget: 5000
    - resource: template
      action: render
      template_id: scenario-builder/axes-and-narratives
      gas_budget: 8000
    - resource: template
      action: render
      template_id: scenario-builder/scenario-quality-gate
      gas_budget: 4000
    - resource: template
      action: render
      template_id: scenario-builder/implications-indicators
      gas_budget: 5000
    - resource: template
      action: render
      template_id: scenario-builder/scenario-convergence-check
      gas_budget: 3000
    - resource: manifest
      action: execute
      template_id: scenario-builder
      gas_budget: 1000
  delegation_chain_required: true
  signature_algorithm: ed25519
  capability_expiry_seconds: 3600
  template_scoped: true