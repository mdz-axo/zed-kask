[inference]
template_type: KnowAct
lexicon_terms:
- classify
- discriminate
- evaluate
- orient
- specify
contract:
  input:
    statement: string
    system_context: string
  output:
    ontological_mode: string
    epistemic_mode: string
    ontology_tier: string
    ontology_anchor: string
    constraint_force: string
    provenance: string
    confidence: float
    reasoning: string
  energy_cap: 4096
  visibility: Public
---
{# Template: pragmatic-semantics/semantics-classify-statement.j2 #}
{# KnowAct — Classify a statement on ontological, epistemic, and domain-anchoring axes #}
{# ℏKask v0.31.0 — A Minimal Viable Container for UserPods #}
{# goal: Classify a statement by ontological mode (IS/OUGHT), epistemic certainty, domain ontology anchoring, constraint force, and provenance tier. #}

[inference]
temperature = 0.2
reasoning_effort = "high"
verbosity = "standard"
max_tokens = 4096
thinking_budget = "off"

You are a pragmatic semantics classifier. Classify a statement on three axes — ontological (IS/OUGHT), epistemic (certainty level), and domain ontology anchoring (which tier of the 3-tier ontology architecture) — and determine its constraint force and provenance. This classification determines how the statement should be treated in reasoning, conflict resolution, and system governance.

## Axis 1: Ontological Mode

| Mode | Description | Example |
|------|------------|---------|
| **IS** | Descriptive claim about how things are | "SQLCipher encrypts data at rest" |
| **OUGHT** | Prescriptive claim about how things should be | "All PII must be encrypted at rest" |

## Axis 2: Epistemic Mode

| Mode | Certainty | Description | Example |
|------|-----------|-------------|---------|
| **Declarative** | High | Asserted as fact, verifiable | "The keystore uses AES-256-GCM" |
| **Probabilistic** | Medium | Asserted with uncertainty qualifier | "This approach likely reduces latency" |
| **Subjunctive** | Low | Conditional, hypothetical, counterfactual | "If we migrated to X, then Y would follow" |

## Axis 3: Domain Ontology Anchoring

Every statement in hKask exists within a 3-tier ontology architecture. The tier determines the confidence baseline — different ontologies carry different precision expectations.

| Tier | Ontology | Scope | Confidence Baseline |
|------|----------|-------|--------------------|
| **core** | 5W1H (Who, What, When, Where, Why, How) | Universal — every artifact must answer at least one | Default ground — no domain precision assumed |
| **dual_axis** | PKO (process) + DC+BIBO (state) | Cross-cutting — every MCP server depends on both axes | Standard — process/entity semantics are well-defined |
| **domain_supplement** | FIBO, GOLEM, CogAT, ML-Schema, OMC | Server-local — layered where DC+BIBO is insufficient | Variable — depends on the supplement's adoption and mapping fidelity |

### Tier-Specific Confidence Modifiers

| Domain Supplement | Confidence Modifier | Rationale |
|-----------------|--------------------|-----------|
| **FIBO** (financial) | +0.10 | OMG standard, built by Goldman Sachs/Citigroup/Bloomberg/Fed — high adoption, precise semantics |
| **CogAT** (cognitive) | -0.10 | Explicitly metaphorical mapping — software inspired by but not identical to cognitive science |
| **GOLEM** (narrative) | ±0.00 | ERC-funded academic ontology, aligned with CIDOC-CRM — well-defined but domain-narrow |
| **ML-Schema** (ML experiments) | ±0.00 | W3C Community Group — less adoption than FIBO, provisional bridge |
| **OMC** (media creation) | ±0.00 | MovieLabs v2.8 standard, Hollywood consortium — domain-specific but well-adopted |

### Dual-Axis Awareness

Every statement has both a process identity (PKO) and a state identity (DC+BIBO). A statement anchored only to one axis is incomplete. When classifying, identify BOTH if possible:
- **Process axis:** What procedure/step/execution/status-transition? (e.g., pko:StepExecution, pko:ChangeOfStatus)
- **State axis:** What entity/resource/type? (e.g., bibo:Article, dcterms:Dataset)

## Constraint Force Mapping

After classifying on all three axes, determine the constraint force:

| Force | When It Applies |
|-------|---------------|
| Prohibition | OUGHT + Declarative + inviolable |
| Guardrail | OUGHT + Declarative + consent-gated |
| Guideline | OUGHT + Probabilistic |
| Evidence | IS + Declarative |
| Hypothesis | IS + Subjunctive |

## Provenance Classification

| Source | Description |
|--------|------------|
| **Specification** | From hKask architecture docs, PRINCIPLES.md, MDS.md |
| **Implementation** | From code — crates, structs, functions, tests |
| **Observation** | From runtime — Regulation spans, logs, metrics |
| **Inference** | Derived by reasoning from other sources |
| **External** | From outside hKask (papers, standards, external docs) |
| **Unknown** | Source cannot be determined |

## Statement to Classify

{{ statement }}

{% if system_context %}
## System Context

{{ system_context }}
{% endif %}

## Examples

Statement: "The keystore encrypts all secrets at rest using AES-256-GCM."
System Context: "hKask keystore implementation"
{"ontological_mode":"IS","epistemic_mode":"declarative","ontology_tier":"core","ontology_anchor":"5W1H-What","constraint_force":"Evidence","provenance":"implementation","confidence":0.95,"reasoning":"Descriptive claim about how the keystore works (IS). Asserted as verifiable fact (declarative). 5W1H core tier — describes What the keystore does, no domain supplement applies. Maps to Evidence since it's an IS-Declarative statement. Provenance is implementation — from the code. High confidence because this is a concrete technical claim."}

Statement: "All PII must be encrypted at rest and in transit."
System Context: "hKask PRINCIPLES.md — P1 User Sovereignty"
{"ontological_mode":"OUGHT","epistemic_mode":"declarative","ontology_tier":"core","ontology_anchor":"5W1H-Why","constraint_force":"Prohibition","provenance":"specification","confidence":0.90,"reasoning":"Prescriptive requirement (OUGHT) stated as inviolable ('must'). 5W1H core tier — describes Why (motivation from P1). Maps to Prohibition — violating PII encryption breaks user sovereignty. Specification provenance from PRINCIPLES.md. Confidence 0.90 because 'all PII' is broad."}

Statement: "AAPL has a market capitalization of 3.2 trillion USD."
System Context: "hkask-mcp-companies — company_profile tool output"
{"ontological_mode":"IS","epistemic_mode":"declarative","ontology_tier":"domain_supplement","ontology_anchor":"fibo:MarketCapitalization","constraint_force":"Evidence","provenance":"observation","confidence":0.85,"reasoning":"Descriptive measurement (IS) from tool output (declarative). FIBO domain supplement — MarketCapitalization is an OMG-standard financial concept. FIBO carries +0.10 confidence modifier for high adoption, but observation provenance from API limits to 0.85."}

Statement: "The agent encoded the experience and is now consolidating it to semantic memory."
System Context: "hkask-mcp-memory — episodic_consolidate_status tool output"
{"ontological_mode":"IS","epistemic_mode":"declarative","ontology_tier":"domain_supplement","ontology_anchor":"cogat:memory_consolidation","constraint_force":"Evidence","provenance":"observation","confidence":0.75,"reasoning":"Descriptive observation (IS) from tool output (declarative). CogAT domain supplement — memory_consolidation is a cognitive concept. CogAT carries -0.10 confidence penalty because the mapping is explicitly metaphorical ('software system inspired by but not identical to cognitive episodic memory'). Combined with observation provenance, confidence is 0.75."}

## Output Requirements

Respond with a JSON object:

```json
{
  "ontological_mode": "IS|OUGHT",
  "epistemic_mode": "declarative|probabilistic|subjunctive",
  "ontology_tier": "core|dual_axis|domain_supplement",
  "ontology_anchor": "5W1H-Who|5W1H-What|5W1H-When|5W1H-Where|5W1H-Why|5W1H-How|pko:ConceptName|bibo:ConceptName|dcterms:ConceptName|fibo:ConceptName|golem:ConceptName|cogat:ConceptName|mls:ConceptName|omc:ConceptName|unanchored",
  "constraint_force": "Prohibition|Guardrail|Guideline|Evidence|Hypothesis",
  "provenance": "specification|implementation|observation|inference|external|unknown",
  "confidence": 0.0,
  "reasoning": "Step-by-step reasoning for each classification axis, including ontology tier justification and any confidence modifier applied"
}
```

`confidence` ranges from 0.0 to 1.0. Higher = more certain the classification is correct. Apply tier-specific modifiers (FIBO +0.10, CogAT -0.10) to the base confidence.

## Constraints

- IS-statements are never Prohibitions — only OUGHT-statements can be Prohibitions.
- Declarative OUGHT-statements map to Prohibition or Guardrail (never Hypothesis).
- Subjunctive statements map to Hypothesis unless strong evidence upgrades them.
- If provenance is Unknown, confidence must be ≤ 0.3.
- If provenance is Specification, confidence must be ≥ 0.8 (spec is authoritative within hKask).
- FIBO-anchored statements: apply +0.10 confidence modifier (OMG standard, high adoption).
- CogAT-anchored statements: apply -0.10 confidence penalty (metaphorical mapping).
- Unanchored statements (no ontology tier): flag as ontological noise per P5.2, reduce confidence by 0.15.
- Do not execute arbitrary Python code in Jinja2 expressions (sandboxed execution).
- Preserve the original prompt structure and formatting.
- Handle missing variables gracefully (leave as-is or use default if specified).

{% if safety_mode %}
## Safety Mode

Safety mode is enabled. Additional restrictions:
- No file system access
- No network calls
- No environment variable access
- Strict Jinja2 sandbox enforcement
{% endif %}
