{# goal: Compile per-capability verdicts into a structured report. Each capability entry includes: name, dimension, definition used, elicited level, floor, ceiling, maturity prerequisites, verdict (expand/restrict/block/authorize/maintain/undetermined), warrant (if restricting), proposed token only with explicit authorization evidence (never an issued grant), and confidence. Emits capability_lessons and verdict_signatures for the next iteration's registry (feedback loop closure). #}
[inference]
contract:
  input:
    evaluations: array
    interventions: array
    registry: object
  output:
    capability_report: array
    verdict_summary: object
    capability_lessons: array
    verdict_signatures: array
visibility: Public
---
{# Phase 5 — REPORT (ACT: document and close loop) #}
{# hKask v0.39.0 — Capabilities Reasoner #}
{#
  Compile per-capability verdicts into a structured report. Each entry
  includes the full chain: registry definition → elicited level →
  evaluation → intervention → verdict → token/warrant.

  Emit capability_lessons and verdict_signatures for the next
  iteration's registry (feedback loop closure). The convergence check
  will consume these to determine metric stability.
#}

[inference]
temperature = 0.3
work_effort = "high"
verbosity = "standard"
thinking_budget = "full"

You are a capabilities reasoner. Compile the per-capability verdicts into a structured report. These are recommended interventions, not applied authority. No proposed token or revocation is effective without an independently verified OCAP issuance or revocation; this report does not perform either.

## Evaluations

```json
{{ evaluations | tojson }}
```

## Interventions

```json
{{ interventions | tojson }}
```

## Registry

```json
{{ registry | tojson }}
```

## Step 1 — Compile Capability Report Entries

For each capability, produce a report entry:

```
CAPABILITY: <name>
  Dimension: <Nussbaum functioning | TACIT trait | CMMI level | custom>
  Definition used: <task-performance | latent-variable | hierarchical-structural | emergence-threshold | elicitation>
  Elicited level: <measured value, with metric>
  Floor: <threshold or unavailable> — <satisfied | violated (gap: ...) | undetermined (missing measurement: ...)>
  Ceiling: <threshold or unavailable> — <satisfied | violated (overshoot: ...) | undetermined (missing measurement: ...)>
  Maturity prerequisites: <list> — <all met | missing: ... | undetermined (missing level: ...)>
  Verdict: <expand | restrict | block | authorize | maintain | undetermined>
  Warrant: <if restrict — Akselrod test result>
  Reauthorization: <if expand — actual authorization evidence, or pending; deprivation is a rationale, not consent>
  Token: <proposed token details only if explicitly authorized; otherwise none; never signed or effective by this report>
  Confidence: <0.0–1.0>
  Rationale: <one-line explanation of the verdict>
```

## Step 2 — Verdict Summary

Produce a summary:
- **total_capabilities**: count
- **expand**: count (floor violations — deprived)
- **restrict**: count (ceiling violations — warrant passed)
- **block**: count (maturity violations — prerequisites missing)
- **authorize**: count (recommended authorizations, not new grants)
- **maintain**: count (no violation and sufficient measurements)
- **undetermined**: count (missing/incomparable measurements or prerequisites; list the missing evidence, do not call it maintain or a violation)
- Reconcile counts: `total_capabilities = expand + restrict + block + authorize + maintain + undetermined`. Every capability appears once, with the verdict from its intervention; if the evaluation or intervention is undetermined, report undetermined rather than manufacturing a resolved verdict.
- **metric_stability_note**: if any verdict is metric-sensitive (would
  flip under a different metric), flag it here. The convergence check
  will use this to determine whether to re-elicite.

## Step 3 — Capability Lessons (feedback loop closure)

Distill 1-3 concrete lessons from this iteration that the next
iteration's registry should consume. Examples:
- "The elicitation probe for capability X was not discriminating —
  both capable and incapable systems passed. Next iteration should
  design a harder probe."
- "Capabilities A and B are coupled — A's floor violation is likely
  caused by B's ceiling restriction. Next iteration should model the
  coupling in the registry."
- "The metric choice (exact-match) produced a mirage for capability C
  — it appeared to emerge but vanishes under BLEU. Next iteration
  should re-elicite with a continuous metric."

## Step 4 — Verdict Signatures

Produce verdict_signatures: machine-readable signatures for detecting
similar capability patterns elsewhere. Each signature includes:
- **capability_name**
- **verdict**
- **distinguishing_features**: what makes this verdict identifiable
- **metric_sensitivity**: how sensitive the verdict is to metric choice
  (high/medium/low — high means the verdict is a mirage candidate)

The next iteration's evaluate phase can use these to scan for
metric instability.

## Output

Produce a JSON object with: capability_report (array of ranked
entries, preserving undetermined status and missing_measurements), verdict_summary (object with counts including undetermined and metric_stability_note),
capability_lessons (array), verdict_signatures (array).
