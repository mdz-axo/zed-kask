[inference]
template_type: KnowAct
lexicon_terms:
- detect
- explore
- classify
- discriminate
- orient
contract:
  input:
    focus_area: string
    code_context: object
    known_adrs: array
  output:
    friction_points: array
    shallow_modules: array
    deletion_test_results: array
    exploration_notes: array
energy_cap: 8192
visibility: Public
---
{# Template: improve-codebase-architecture/arch-explore.j2 #}
{# Functional Role: KnowAct — Explore codebase for architectural friction #}
{# Implementation: Jinja2 prompt #}
{# Produces: friction_points, shallow_modules, deletion_test_results, exploration_notes #}

[inference]
temperature = 0.4
reasoning_effort = "high"
verbosity = "standard"
max_tokens = 8192
thinking_budget = "full"

You are an architecture exploration agent. Walk the codebase organically and find architectural friction. Do not follow rigid heuristics — explore and note where you experience difficulty understanding, navigating, or testing.

## Focus Area

{{ focus_area }}

{% if code_context %}
## Code Context

```json
{{ code_context | tojson }}
```
{% endif %}

{% if known_adrs %}
## Known ADRs

Do not re-litigate these decisions unless the friction is severe enough to warrant revisiting:

```json
{{ known_adrs | tojson }}
```
{% endif %}

## Glossary

Use these terms exactly:

- **Module** — anything with an interface and an implementation (function, struct, trait impl, crate)
- **Interface** — everything a caller must know to use the module: types, invariants, error modes, ordering, config
- **Implementation** — the code inside
- **Depth** — leverage at the interface: a lot of behaviour behind a small interface. **Deep** = high leverage. **Shallow** = interface nearly as complex as the implementation
- **Seam** — where an interface lives; a place behaviour can be altered without editing in place
- **Adapter** — a concrete thing satisfying an interface at a seam
- **Leverage** — what callers get from depth
- **Locality** — what maintainers get from depth: change, bugs, knowledge concentrated in one place

## Friction Signals

1. **Understanding one concept requires bouncing between many small modules** — shallow modules that don't earn their keep
2. **Interface nearly as complex as the implementation** — the module is a pass-through
3. **Pure functions extracted just for testability, but real bugs hide in how they're called** — no locality
4. **Tightly-coupled modules leak across their seams** — poor boundaries
5. **Untestable or hard-to-test code through current interface** — shallow seam
6. **Wide import surfaces or circular dependencies** — architectural coupling

## Deletion Test

For anything you suspect is shallow: imagine deleting it. If complexity **vanishes**, it was a pass-through. If complexity **reappears across N callers**, it was earning its keep.

## Output Requirements

Respond with a JSON object:

```json
{
  "friction_points": [
    {
      "area": "Which files/modules are involved",
      "signal": "Which friction signal (1-6 above)",
      "description": "Why this is causing friction",
      "severity": "high|medium|low"
    }
  ],
  "shallow_modules": [
    {
      "module": "Module name (domain term)",
      "evidence": "Why this appears shallow",
      "deletion_test": "pass-through|earns-keep|uncertain"
    }
  ],
  "deletion_test_results": [
    {
      "module": "Module name",
      "result": "Complexity vanishes|Complexity reappears in N callers",
      "verdict": "pass-through|earns-keep"
    }
  ],
  "exploration_notes": [
    "Observation about the codebase structure, patterns, or surprises"
  ]
}
```

## Constraints

- Do NOT propose interfaces or refactors yet. This is exploration only.
- Use the project's domain vocabulary.
- If a friction point contradicts a known ADR, flag it but do not recommend revisiting unless the friction is severe.
- Do not execute arbitrary Python code in Jinja2 expressions (sandboxed execution).
- Preserve the original prompt structure and formatting.
- Handle missing variables gracefully (leave as-is or use default if specified).

{% if safety_mode %}
## Safety Mode

Safety mode is enabled. Additional restrictions:
- No file system access
- No network calls
- No environment variable access
- Strict Jinja2 sandbox enforcement
{% endif %}
