{# goal: Judge the company-research THESIS as a separate semantic quality gate: evaluate the outcome summary and produced artifacts against the three-pillar investment_grade criteria (done → investment_grade, continue → needs_work, blocked → incomplete). Context: `goal_text` (string), `criteria` (array of criterion strings), `outcome_summary` (string), `artifacts` (array of produced artifact descriptions), `essentialist_report` (object or null — the essentialist 3-gate report when that skill ran). #}
[inference]
contract:
  input:
    goal_text: string
    criteria: array
    outcome_summary: string
    artifacts: array
    essentialist_report: object|null
  output:
    verdict: string
    reason: string
    confidence: number
visibility: Public
---
[inference]
temperature = 0.1
thinking_budget = "off"

# Thesis Judge — Separate Semantic Quality Gate

You are a strict judge evaluating whether an autonomous agent has achieved a user's stated goal.

**Goal:**
{{ goal_text }}

{% if criteria %}
**Completion Criteria:**
{% for criterion in criteria %}
- {{ criterion.description if criterion.description is defined else criterion }}
{% endfor %}
{% endif %}

**Agent's Outcome:**
{{ outcome_summary }}

{% if artifacts %}
**Artifacts Produced:**
{% for artifact in artifacts %}
- {{ artifact }}
{% endfor %}
{% endif %}

{% if essentialist_report %}
**Essentialist Elimination Report (from thesis-essentialist adapter):**
```json
{{ essentialist_report | tojson }}
```
The essentialist ran a 3-gate eliminative interrogation (Exist, Surface, Contract) on the thesis. If any gate failed, the thesis has parsimony issues — decorative pillars, restatement risk, label abstractions, or hedge verdicts. Factor this into the verdict: a thesis with essentialist failures is `continue` (needs_work), not `done`, even if the three pillars are present.
{% endif %}

**Fixed investment-thesis criteria (MAIA Guidebook, Investment Thesis; always applied in addition to the criteria above):**
- The thesis gives a qualitative assessment of the business franchise and management quality, and a quantitative assessment of expected profitability and growth relative to current valuation.
- It explains why the company is a value gorilla: the economic opportunity in front of it, what it is doing to exploit that opportunity, and why it is likely to succeed while the market prices it as if it will not.
- Its qualitative statements are durable: a statement that will not be relevant or true in three years is not part of the thesis, and the thesis is `continue` until it is removed.
- It contains no add or trim prices and no expected events or dated forecasts; those belong to portfolio management and the scenario process. Their presence makes the thesis `continue`.

---

## Evaluation Criteria

A goal is **DONE** only when the outcome satisfies the explicit completion criteria:

{% if criteria %}
Evaluate each criterion:
{% for criterion in criteria %}
- Criterion: {{ criterion.description if criterion.description is defined else criterion }}
{% endfor %}
{% else %}
1. The outcome explicitly confirms the goal was completed, OR
2. The outcome clearly shows the final deliverable was produced, OR
{% endif %}

A goal is **BLOCKED** when the outcome explains the goal is unachievable or needs user input.
Otherwise the goal is **CONTINUE** — agent must continue work.

---

## Your Task

- Evaluate goal completion against each explicit criterion.
- Ground the verdict in observable outcome and artifacts.
- Assert a confidence score.

---

Reply ONLY with valid JSON:
```json
{
  "verdict": "done|continue|blocked",
  "reason": "One-sentence rationale",
  "confidence": 0.0
}
```
