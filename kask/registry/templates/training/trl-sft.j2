# Auto-generated by hKask Training Server (harness: trl, trainer: sft)
# Rendered from registry/templates/training/trl-sft.j2
# Evolvable via gpa-evolution / skill-maintenance — no Rust recompile needed.
#
# TRL SFTTrainer script — parallel to axolotl-lora.j2 (YAML).
# The lora-training skill's G6 gate recommends harness=trl when the operator
# needs TRL-specific capabilities (assistant_only_loss, packing strategies,
# VLMs) or when preference optimization is planned (Phase 2).
#
# TRL version: pinned in the pod template (see runpod-lora-training-guide.md
# Lesson 12). Version mismatches between training and inference cause garbage
# output (Lesson 6 precedent: PiSSA portability).
#
# LoRA initialization is rendered from the typed TrainingParams boundary.
# EVA is the canonical data-driven initializer. PiSSA is not used because its
# mutated-base save contract is not portable across verified environments.

import os
import sys

# ── TRL version assertion ──────────────────────────────────────────────────
# Fail fast if the installed TRL version is incompatible. The pod template
# pins TRL to a specific version; this assertion catches drift.
try:
    import trl
    _trl_version = getattr(trl, "__version__", "unknown")
except ImportError as e:
    print(f"FATAL: trl not installed: {e}", file=sys.stderr)
    sys.exit(1)

from trl import SFTConfig, SFTTrainer
from datasets import load_dataset
from peft import LoraConfig
import torch

# ── Base model ──────────────────────────────────────────────────────────────
base_model = "{{ base_model }}"

# ── PEFT / LoRA configuration ───────────────────────────────────────────────
# Rendered from the typed LoraParams boundary — same fields as axolotl YAML.
peft_config = LoraConfig(
    r={{ lora_r }},
    lora_alpha={{ lora_alpha }},
    lora_dropout={{ lora_dropout | default(0) }},
    target_modules=[
{% for module in lora_target_modules %}
        "{{ module }}",
{% endfor %}
    ],
{% if peft_init_lora_weights %}
    init_lora_weights="{{ peft_init_lora_weights }}",
{% endif %}
{% if use_rslora == "true" %}
    use_rslora=True,
{% endif %}
{% if use_dora == "true" %}
    use_dora=True,
{% endif %}
)

# ── Dataset ────────────────────────────────────────────────────────────────
# TRL SFTTrainer accepts conversational ChatML datasets directly — same format
# as axolotl's `chat_template` dataset type. No preprocessing needed.
{% if data_files %}
dataset = load_dataset("{{ dataset_path }}", data_files="{{ data_files }}", split="train")
{% else %}
dataset = load_dataset("{{ dataset_path }}", split="train")
{% endif %}

# ── SFT configuration ──────────────────────────────────────────────────────
# Rendered from the typed TrainingParams boundary. Defaults mirror axolotl's
# canonical config (see corpus/lora/axolotl-lora.yaml) where TRL has a direct
# equivalent.
config = SFTConfig(
    output_dir="{{ output_dir }}",
    num_train_epochs={{ num_epochs | default(2) }},
    per_device_train_batch_size={{ micro_batch_size | default(1) }},
    eval_batch_size=1,
    gradient_accumulation_steps={{ gradient_accumulation_steps | default(16) }},
    learning_rate={{ learning_rate | default(0.0001) }},
    bf16=True,
    gradient_checkpointing=True,
{% if lr_scheduler %}
    lr_scheduler_type="{{ lr_scheduler }}",
{% endif %}
{% if warmup_steps %}
    warmup_steps={{ warmup_steps }},
{% endif %}
{% if weight_decay %}
    weight_decay={{ weight_decay }},
{% endif %}
{% if max_grad_norm %}
    max_grad_norm={{ max_grad_norm }},
{% endif %}
{% if optim %}
    optim="{{ optim }}",
{% endif %}
{% if sequence_len %}
    max_length={{ sequence_len }},
{% endif %}
{% if packing %}
    packing=True,
{% endif %}
    # Eval — preserve an explicit operator-selected split; use 0.05 only when
    # the accepted TrainingParams leaves the value unspecified.
    eval_strategy="steps",
    eval_steps=200,
    save_strategy="steps",
    save_steps=200,
    save_total_limit=5,
    load_best_model_at_end=True,
    metric_for_best_model="loss",
    report_to="none",
    logging_steps=10,
    trust_remote_code=True,
)

# ── Trainer ────────────────────────────────────────────────────────────────
trainer = SFTTrainer(
    model=base_model,
    args=config,
    train_dataset=dataset,
    peft_config=peft_config,
)

# ── Train ──────────────────────────────────────────────────────────────────
trainer.train()

# ── Save adapter ───────────────────────────────────────────────────────────
# TRL + PEFT saves adapter_model.safetensors + adapter_config.json to output_dir.
# The RunPod host's completion_marker checks for adapter_model.safetensors.
trainer.save_model("{{ output_dir }}")

# ── Completion manifest ────────────────────────────────────────────────────
# Written by the pod entrypoint (bash heredoc, no Python) after this script
# exits successfully. The entrypoint reads HKASK_COMPLETION_MANIFEST_PATH.
print(f"TRL SFTTrainer training complete. Adapter saved to {{ output_dir }}")
