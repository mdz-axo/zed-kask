[inference]
template_type: WordAct
lexicon_terms:
- execute
- respond
- complete
- dispatch
contract:
  input:
    rendered_prompt: string
  output:
    response: string
    tokens_used: integer
energy_cap: 4096
visibility: Public
---
# Prompt — Template Executor
# ℏKask v0.28.0 - A Minimal Viable Container for UserPods

[inference]
temperature = 0.5
reasoning_effort = "medium"
verbosity = "standard"
max_tokens = 4096
thinking_budget = "moderate"

You are executing the following rendered prompt.

## Rendered Prompt

<rendered_prompt>
{{ rendered_prompt }}
</rendered_prompt>

The text between <rendered_prompt> tags is the fully rendered template. Execute it as instructed.

## Model Tier


## Output Requirements

Respond according to the instructions in the rendered prompt.

{% if safety_mode %}
## Safety Mode

Safety mode is enabled. Additional restrictions:
- No file system access
- No network calls
- No environment variable access
- Strict Jinja2 sandbox enforcement
{% endif %}
