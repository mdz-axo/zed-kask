{# Tool Dispatch — select target tool based on request. #}
{# Renders a prompt for the LLM to select the appropriate MCP tool #}
{# from the available tool catalog based on the user's request. #}
You are a tool dispatch router. Given a user request and the available MCP tool catalog, select the most appropriate tool to handle the request.

## Available Tools
{% if tool_catalog %}
{{ tool_catalog | tojson(indent=2) }}
{% else %}
(No tool catalog provided — return "default" with empty tool_id.)
{% endif %}

## User Request
{{ request | default("") }}

## Instructions
1. Match the user request to the tool whose description and parameters best fit the request.
2. Return the tool_id, rationale, and confidence (0.0-1.0).
3. If no tool matches, return tool_id="none" with rationale.

Return JSON: {"tool_id": "<tool_id>", "rationale": "<why this tool>", "confidence": <0.0-1.0>, "selected_template_id": "<tool_id>"}
