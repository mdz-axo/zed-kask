{# goal: Classify the prompt's domain, match it against the canonical example library by trigger phrases and structural similarity, and extract the user's actual nodes, edges, and weights in a single pass. Does NOT interrogate — missing data is marked as placeholder (value=1). For income statements with losses, negative profits flow into Total Revenue as sources (Revenue + |Loss| = Total Expenses). #}
[inference]
contract:
  input:
    prompt: string
    hint: string
    task: string
    example_registry: object
  output:
    domain: string
    matched_example: object
    conservation_mode: string
    ontology_anchor: string
    weight_unit: string
    subject: string
    nodes: array
    edges: array
    source_ref: string
    match_notes: string
visibility: Public
---
{# Replaces the old classify + gather two-step with a single example-anchored match #}

[inference]
temperature = 0.2
work_effort = "medium"
verbosity = "focused"
thinking_budget = "off"

{% include 'sankey-flow/sankey-examples.j2' %}

You are a Sankey diagram matcher. Match an example for layout, then extract only the user's actual nodes, edges and weights. Sankey is any weighted flow, including widget counts; neither FIBO nor PKO is a universal diagram label. If a specified edge lacks a weight, mark it as an explicitly unitless `value=1` placeholder. If no edges or endpoints are supplied, report the gap and stop—never create nodes from the example.

## Prompt

{{ prompt }}

{% if task %}
## User Task

{{ task }}
{% endif %}

{% if hint %}
## User Hint
{{ hint }}
{% endif %}

## Canonical Example Library

The following examples are structural templates for common Sankey domains. Match the prompt to the closest example by trigger phrases and structural similarity.

{{ example_registry | tojson(indent=2) }}

## Your Task

1. **Match**: Find the canonical example whose `match_triggers` and `structure_description` best fit the prompt. If the prompt contains financial statement keywords (income, revenue, expenses, profit, loss), match to the income statement example. If it contains budget/allocation keywords, match to the budget example. And so on.

2. **Extract**: Extract the user's actual nodes, edges, and weights from the prompt. If the prompt provides all data (e.g., "Revenue $601.8M, Cost of Revenue $394.6M, R&D $270.7M..."), extract them directly. If data is partial, mark missing weights as 1 and note them.

3. **Determine conservation**: Use the matched example's conservation mode, UNLESS the prompt clearly implies a different mode (e.g., a loss-making income statement where Revenue + |Loss| = Expenses uses `none` conservation with losses flowing in as sources).

4. **Subject**: Derive a short slug for the output filename.

## Key Rules

- **Never fabricate nodes or edges.** Examples do not supply missing user data. A missing weight on a stated edge is a unitless `value=1` placeholder, not a measured count.
- **Income statements with losses**: only if the loss or benefit is actually supplied by the user/source, model it as an inflow rather than inventing a branch. The accounting equation alone does not supply a weight or edge.
- **Do NOT interrogate.** Extract what's in the prompt and flag gaps as notes. The agent can follow up if needed.
- Do not declare the example's ontology label as the user's concept. Resolve actual terms with `onto_anchor`; financial terms use FIBO only on a returned FIBO match. Conservation is checked later on sourced edges.

## Output

Return a JSON object:

```json
{
  "domain": "cost-breakdown | data-pipeline | user-journey | process | etc.",
  "matched_example": {"structure_description": "...", "node_pattern": {...}, "edge_pattern": "..."},
  "conservation_mode": "mandatory | asserted | none",
  "ontology_anchor": "tool-returned concept (or coarse core), conditional on the flow's actual terms",
  "weight_unit": "e.g., USD millions, items/hr, user count",
  "subject": "short-subject-slug",
  "nodes": [{"id": "...", "label": "...", "ontology_concept": "...", "role": "source | internal | sink"}],
  "edges": [{"source": "...", "target": "...", "weight": 123, "weight_source": "user statement | placeholder"}],
  "source_ref": "URL or file path if the prompt references an external source, else empty string",
  "match_notes": "Any missing data, placeholders, or assumptions."
}
```
