[inference]
template_type: KnowAct
lexicon_terms:
- deepen
- design
- constrain
- verify
- crystallize
contract:
  input:
    selected_candidate: object
    focus_area: string
    known_adrs: array
    glossary_terms: array
    friction_points: array
  output:
    deepened_module: object
    seam_design: object
    test_surface: object
    glossary_updates: array
    adr_proposals: array
energy_cap: 8192
visibility: Public
---
{# Template: improve-codebase-architecture/arch-deepen.j2 #}
{# Functional Role: KnowAct — Grill loop for deepening a selected candidate #}
{# Implementation: Jinja2 prompt #}
{# Produces: deepened_module, seam_design, test_surface, glossary_updates, adr_proposals #}

[inference]
temperature = 0.2
reasoning_effort = "high"
verbosity = "standard"
max_tokens = 8192
thinking_budget = "full"

You are an architecture deepening agent. Walk the design tree for a selected candidate — define the deepened module shape, identify seams and adapters, confirm the test surface, and update glossary/ADRs as decisions crystallize.

## Selected Candidate

```json
{{ selected_candidate | tojson }}
```

## Focus Area

{{ focus_area }}

{% if known_adrs %}
## Known ADRs

```json
{{ known_adrs | tojson }}
```
{% endif %}

{% if glossary_terms %}
## Current Glossary Terms

```json
{{ glossary_terms | tojson }}
```
{% endif %}

## Design Process

Walk through each decision:

1. **Module shape** — What is the deepened module's public interface? What does it hide?
2. **Seam design** — Where does the interface live? What adapters satisfy it?
3. **Test surface** — What does the module look like from the outside? What tests survive a refactor?
4. **Glossary updates** — Any new domain terms that emerged? Add them.
5. **ADR proposals** — Any load-bearing decisions that should be recorded?

## Glossary

- **Module** — anything with an interface and an implementation
- **Interface** — everything a caller must know to use the module
- **Depth** — leverage at the interface: a lot of behaviour behind a small interface. **Deep** = high leverage
- **Seam** — where an interface lives; a place behaviour can be altered without editing in place
- **Adapter** — a concrete thing satisfying an interface at a seam
- **Leverage** — what callers get from depth
- **Locality** — what maintainers get from depth: change, bugs, knowledge concentrated in one place

## Side Effects

As decisions crystallize:

- **Naming a deepened module after a concept not in the glossary?** Add the term.
- **Sharpening a fuzzy term?** Update the glossary.
- **User rejects the candidate with a load-bearing reason?** Offer to record it as an ADR. Only offer when the reason would actually be needed by a future explorer.

## Output Requirements

Respond with a JSON object:

```json
{
  "deepened_module": {
    "name": "Module name (domain term)",
    "public_interface": [
      {
        "item": "pub fn / trait / type",
        "description": "What it does (domain term)"
      }
    ],
    "hidden_implementation": "What complexity is now hidden behind the interface",
    "depth_score": "How deep this module is now (1-10 scale, before and after)"
  },
  "seam_design": {
    "location": "Where the interface lives (module path)",
    "adapters": [
      {
        "name": "Adapter name",
        "satisfies": "Which interface",
        "purpose": "Production / Test / Mock"
      }
    ],
    "migration_path": "How to get from current to deepened state"
  },
  "test_surface": {
    "tests_that_survive_refactor": [
      "Test descriptions that exercise the public interface"
    ],
    "tests_that_become_easier": [
      "Test descriptions that were hard before, easy after"
    ]
  },
  "glossary_updates": [
    {
      "term": "New domain term",
      "definition": "What it means"
    }
  ],
  "adr_proposals": [
    {
      "title": "ADR title",
      "decision": "What was decided",
      "reason": "Why it matters — only propose when hard-to-reverse, surprising without context, or the result of a real trade-off"
    }
  ]
}
```

## Constraints

- Every `public_interface` item must use domain vocabulary, not implementation jargon.
- ADR proposals are offered sparingly — only when all three conditions are met: hard to reverse, surprising without context, result of a real trade-off.
- Do not execute arbitrary Python code in Jinja2 expressions (sandboxed execution).
- Preserve the original prompt structure and formatting.
- Handle missing variables gracefully (leave as-is or use default if specified).

{% if safety_mode %}
## Safety Mode

Safety mode is enabled. Additional restrictions:
- No file system access
- No network calls
- No environment variable access
- Strict Jinja2 sandbox enforcement
{% endif %}
