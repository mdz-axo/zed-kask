# Template crate manifest for platform-wardley-mapper skill
# hKask v0.31.0

crate:
  name: platform-wardley-mapper
  version: "0.31.0"
  description: >
    Strategic Wardley Mapping. Inventories components, classifies on evolution
    axis, maps value chain, identifies movement, and produces strategic
    recommendations aligned with loyalty-anchored mission.

templates:
  - id: platform-wardley-mapper/convergence-check
    name: "Compute convergence for Wardley mapping PDCA cycles"
    path: convergence-check.j2
    type: KnowAct
    lexicon_terms: [evaluate, converge, classify, verify, calibrate]
    description: Compute convergence for Wardley mapping PDCA cycles.
