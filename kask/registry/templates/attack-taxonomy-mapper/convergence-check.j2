{# goal: Compute normalized convergence metric [0,1] from evidence counts only: unmapped findings (0.45), OSC&R tactic coverage (0.30), regression taxonomy_mapping field growth (0.15), residual unmapped risk (0.10). Emit reg.taxonomy.convergence span (P9) — namespace registered in CANONICAL_NAMESPACES (event.rs). UserPod_host mandatory (P12). Minimal: evidence-based computation, no synthetic metrics (P5, P8). #}
{# Computes normalized metric [0, 1] where 0 = fully converged #}
{# Emits reg.taxonomy.convergence Regulation span (P9) — namespace registered #}

INPUT: unmapped_findings = {{ unmapped_findings }} (from previous cycle or current)
INPUT: oscr_tactics_covered = {{ oscr_tactics_covered }} (from taxonomize phase)
INPUT: oscr_tactics_missing = {{ oscr_tactics_missing }}
INPUT: oscr_techniques_covered = {{ oscr_techniques_covered }}
INPUT: existing_taxonomy_mappings = {{ existing_taxonomy_mappings }} (count of regression entries with taxonomy_mapping field)
INPUT: proposed_taxonomy_mappings = {{ proposed_taxonomy_mappings }} (count from current cycle)
INPUT: previous_metric = {{ previous_metric | default(1.0) }}
INPUT: userpod_host = {{ userpod_host }} (P12)

COMPUTE METRIC (weighted — evidence-based only, P8, P5):
- Unmapped findings (critical/high) weight: 0.45
  Score = 0.00 if zero critical/high unmapped; +0.45 if any critical/high remains unmapped.
  Partial = (count_critical * 1.0 + count_high * 0.5) / max(1, previous_critical + previous_high) — only if evidence-backed.
- OSC&R tactic coverage weight: 0.30
  OSC&R has 12 tactics (Initial Access, Collection, Resource Development,
  Lateral Movement, Execution, Exfiltration, Credential Access, Impact,
  Reconnaissance, Defense Evasion, Persistence, Privilege Escalation).
  All 12 tactics covered = +0.00
  10-11 covered = +0.05; 7-9 = +0.10; 4-6 = +0.15; 2-3 = +0.22; 0-1 = +0.30
  Each tactic only counts as covered when at least one finding maps to a
  technique within it (P8 — concrete evidence only, no aspirational
  coverage). Verified against `github.com/pbom-dev/OSCAR` `matrix.json`.
- Regression taxonomy_mapping field growth weight: 0.15
  New `taxonomy_mapping` field proposed this cycle = +0.00
  No new proposal despite evidence = +0.15 (stagnation)
  Existing `taxonomy_mapping` fields remain (no field removal in metric).
- Residual unmapped risk weight: 0.10
  Zero unmapped findings = +0.00
  Any remaining unmapped finding = +0.10

SUM = sum of contributions, clamp [0, 1].

CONVERGENCE CHECK (P9 — homeostatic regulation):
- Metric ≤ 0.10: near converged.
- Metric ≤ 0.10 AND relative improvement ≥ 5% from previous_metric: converged.
- If metric > 0.10 OR improvement < 5%: not converged — identify blockers.
- Blockers = specific OSC&R tactic gaps (from `oscr_tactics_missing`),
  unmapped critical/high findings (with finding reference), stagnation
  (no new taxonomy_mapping proposed despite finding evidence).

Regulation SPAN (P9):
- `reg.taxonomy.convergence` is registered in `CANONICAL_NAMESPACES`
  (`crates/hkask-types/src/event.rs`). Emit span unconditionally:
  (`target: "reg.taxonomy.convergence"`, message: `"Regulation"`,
  operation: `"convergence_check"`, metric, previous_metric, improvement_pct,
  blockers: [...], userpod_host, latency_ms).

OUTPUT FORMAT (JSON):
`{convergence_metric, previous_metric, improvement_pct, converged: bool,
blockers: [...], oscr_tactics_covered, oscr_tactics_missing,
oscr_techniques_covered,
existing_taxonomy_mappings_count, proposed_taxonomy_mappings_count,
unmapped_critical, unmapped_high, taxonomy_coverage, residual_unmapped_risk,
userpod_host, reg_span_emitted: true}`.

5W1H GATE (P5 — minimal): Who = userpod host; What = convergence metric;
Where = audit cycle / workspace; When = scan cycle; Why = P9 observable
self-correction / P7 evolutionary progress; How = compute metric + check
improvement + emit Regulation span + identify blockers.
All 6 present.

MINIMAL: One template (`KnowAct`); no extra computation; uses evidence from
previous phases only; no synthetic metrics.
EVIDENCE-BACKED ONLY (P8): Metric dimensions use concrete counts from
`mappings`, `oscr_tactics_covered` (verified by finding evidence),
`existing_taxonomy_mappings` (actual YAML file count),
`proposed_taxonomy_mappings` (actual proposal count from current cycle).
No fabricated numbers.
EVOLUTIONARY (P7): Blockers propose concrete next steps (e.g., "map
finding RR-0007 to OSC&R Initial Access / Typosquatting") based on real
mapping evidence — not speculative.
OCAP BOUNDARY (P4): Metric computed from workspace finding evidence only;
no external taxonomy download; no external package download.
Regulation REGULATION (P9): Span emitted unconditionally — namespace registered
in `CANONICAL_NAMESPACES` (`crates/hkask-types/src/event.rs`).
USERPOD HOST (P12): `userpod_host` mandatory.
VISIBILITY (P11): Metric output public; no hidden taxonomy coverage.
SAFETY FLOOR (P3.1): Taxonomy convergence protects Generative Space —
unconverged metric indicates unstructured attack pattern awareness.
SEMANTIC GROUNDING (P8): Every blocker references concrete evidence
(finding reference, OSC&R tactic gap, regression file or missing proposal
reason).
SOURCE REFERENCES (P8 — concrete):
- OSC&R framework (`github.com/pbom-dev/OSCAR` — `matrix.json`, verified
  2026-07-18) — supply chain attack taxonomy reference. OSC&R uses
  tactic + technique names, NOT numeric IDs.
- MITRE CWE definitions (mitre.org/data/definitions/)
- `security/regressions/README.md` (regression format and ratchet lifecycle)
- `security/regressions/` (actual regression library — read as input)
- `supply-chain-sentinel` SKILL.md (§convergence-check — convergence metric
  computation pattern adopted)
- `bug-hunt` SKILL.md (§bug-hunt-convergence-check — convergence metric
  computation pattern adopted)
- `principles.md` (§P5 Essentialism, §P7 Evolutionary, §P8 Semantic Grounding,
  §P9 Homeostatic Self-Regulation, §P12 Authenticated Host Mandate)

REGISTRY AUTHORITATIVE (P5.1): Registry manifest + `.j2` templates are
canonical. SKILL.md derived. If conflict, registry wins.
PUBLIC VISIBILITY: `visibility: public` — no hidden taxonomy coverage.
NO FABRICATION: Metric computed from evidence counts only. Blockers derived
from actual `unmapped_findings`, `oscr_tactics_missing`,
`proposed_taxonomy_mappings`. No synthetic finding references. No synthetic
mapping counts.
CONVERGENCE INDEPENDENT: Separate `.j2` template; does not duplicate
`taxonomize` phase logic. Uses `mappings` from `map-taxonomy` (or from
previous audit cycle if persistence is enabled by user). If no previous
cycle, assumes `previous_metric = 1.0` (initial state) — explicit
assumption, not hidden.
Regulation SPAN EMITTED: `reg.taxonomy.convergence` is registered, so the span is
always emitted. Output states `reg_span_emitted: true`.
MINIMAL ARTIFACT: This template produces exactly the metric, verdict,
blockers, and span emission — nothing more.
OSC&R VERIFICATION GATE: The OSC&R tactic and technique names referenced
are VERIFIED against the live OSC&R framework (`github.com/pbom-dev/OSCAR`
`matrix.json`, verified 2026-07-18). OSC&R uses tactic + technique names,
NOT numeric IDs.
