{# goal: Compute normalized convergence metric [0,1] from evidence counts only: unresolved runtime threats (0.40), defense-layer runtime coverage verified by firing (0.25), threat-pattern taxonomy coverage (0.15), regression library growth (0.10), residual runtime risk (0.10). Emit reg.runtime.convergence span (P9) — namespace registered in CANONICAL_NAMESPACES (event.rs). UserPod_host mandatory (P12). Minimal: evidence-based computation, no synthetic metrics (P5, P8). #}
{# Computes normalized metric [0, 1] where 0 = fully converged #}
{# Emits reg.runtime.convergence Regulation span (P9) — namespace registered #}

INPUT: unresolved_threats = {{ unresolved_threats }} (from previous cycle or current)
INPUT: defense_layers_firing = {{ defense_layers_firing }} (from emit-regulation/classify-threat)
INPUT: defense_layers_silent = {{ defense_layers_silent }}
INPUT: existing_regressions = {{ existing_regressions }} (count of surface: runtime entries)
INPUT: proposed_regressions = {{ proposed_regressions }} (count from current cycle)
INPUT: previous_metric = {{ previous_metric | default(1.0) }}
INPUT: userpod_host = {{ userpod_host }} (P12)

COMPUTE METRIC (weighted — evidence-based only, P8, P5):
- Critical + high unresolved threats weight: 0.40
  Score = 0.00 if zero critical/high; +0.40 if any critical/high remains.
  (Partial formula for reference: (count_critical * 1.0 + count_high * 0.5) /
  max(1, previous_critical + previous_high) — informational only, not
  integrated into the binary score above.)
- Defense-layer firing coverage weight: 0.25
  6 layers firing (input_filtering, output_filtering, canary_detection,
  runtime_policy, regulation_loop, action_distribution_monitoring) = +0.00
  5 firing = +0.04; 4 = +0.08; 3 = +0.13; 2 = +0.19; 1 = +0.25
  Each layer only counts as firing when actual `reg.guard.*` or
  `reg.outcome.*` span emissions confirm it (P8 — concrete evidence
  only, no aspirational coverage). Distinct from kali-audit's static
  presence check — this measures runtime firing, not code presence.
- Threat-pattern taxonomy coverage weight: 0.15
  All 4 threat types (endpoint_abuse, bot_traffic, llm_usage_spike,
  dependency_behavior_anomaly) covered by at least one finding/proposed
  regression = +0.00
  3 covered = +0.04; 2 = +0.08; 1 = +0.12; 0 = +0.15
- Regression library growth weight: 0.10
  New `surface: runtime` regression proposed this cycle = +0.00
  No new proposal despite *unresolved threat evidence* = +0.10 (stagnation)
  No new proposal because *no new threats* = +0.00 (healthy — not stagnation)
  Existing `enforced` regressions remain enforced (no regression removal in metric).
- Residual runtime risk weight: 0.10
  Zero unresolved runtime anomalies in observation window = +0.00
  Any remaining critical/high runtime threat = +0.10

SUM = sum of contributions, clamp [0, 1].

CONVERGENCE CHECK (P9 — homeostatic regulation):
- Metric ≤ 0.10: near converged.
- Metric ≤ 0.10 AND relative improvement ≥ 5% from previous_metric: converged.
- If metric > 0.10 OR improvement < 5%: not converged — identify blockers.
- Blockers = specific defense-layer firing gaps (from `defense_layers_silent`),
  unresolved critical/high threats (with span evidence), stagnation
  (no new regression proposed despite threat evidence), taxonomy gaps
  (threat types uncovered by findings/proposals).

Regulation SPAN (P9):
- `reg.runtime.convergence` is registered in `CANONICAL_NAMESPACES`
  (`crates/hkask-types/src/event.rs`). Emit span unconditionally:
  (`target: "reg.runtime.convergence"`, message: `"Regulation"`,
  operation: `"convergence_check"`, metric, previous_metric, improvement_pct,
  blockers: [...], userpod_host, latency_ms).

OUTPUT FORMAT (JSON):
`{convergence_metric, previous_metric, improvement_pct, converged: bool,
blockers: [...], defense_layers_firing, defense_layers_silent,
existing_regressions_count, proposed_regressions_count, unresolved_critical,
unresolved_high, taxonomy_coverage, residual_runtime_risk, userpod_host,
reg_span_emitted: true}`.

5W1H GATE (P5 — minimal): Who = userpod host; What = convergence metric;
Where = deployed runtime environment / observation window; When = monitoring
cycle; Why = P9 observable self-correction / P7 evolutionary progress;
How = compute metric + check improvement + emit Regulation span + identify blockers.
All 6 present.

MINIMAL: One template (`KnowAct`); no extra computation; uses evidence from
previous phases only; no synthetic metrics.
EVIDENCE-BACKED ONLY (P8): Metric dimensions use concrete counts from
`threats`, `defense_layers_firing` (verified by span emission), 
`existing_regressions` (actual YAML file count), `proposed_regressions`
(actual proposal count from current cycle). No fabricated numbers.
EVOLUTIONARY (P7): Blockers propose concrete next steps (e.g., "add
runtime policy rule for tool call chain X", "investigate why reg.guard.input
firing but reg.outcome.action_blocked not increasing") based on real
runtime evidence — not speculative.
OCAP BOUNDARY (P4): Metric computed from hKask Regulation telemetry only; no
external endpoint scanning; no OS-level process inspection.
Regulation REGULATION (P9): Span emitted unconditionally — namespace registered
in `CANONICAL_NAMESPACES` (`crates/hkask-types/src/event.rs`).
USERPOD HOST (P12): `userpod_host` mandatory.
VISIBILITY (P11): Metric output public; no hidden runtime posture.
SAFETY FLOOR (P3.1): Runtime posture convergence protects Generative Space —
unconverged metric indicates container runtime risk.
SEMANTIC GROUNDING (P8): Every blocker references concrete evidence
(span target, timestamp, defense-layer firing gap source, regression file
or missing proposal reason).
SOURCE REFERENCES (P8 — concrete):
- MITRE CWE definitions (mitre.org/data/definitions/)
- OWASP LLM Top 10 2025 (owasp.org)
- MITRE ATLAS (atlas.mitre.org)
- `security/regressions/README.md` (regression format and ratchet lifecycle)
- `security/regressions/` (actual regression library — read as input)
- `kali-audit` SKILL.md (§Defense-in-Depth Layer Catalog — complementary
  static presence check vs this skill's runtime firing check)
- `supply-chain-sentinel` SKILL.md (§convergence-check — convergence metric
  computation pattern adopted)
- `bug-hunt` SKILL.md (§bug-hunt-convergence-check — convergence metric
  computation pattern adopted)
- `principles.md` (§P5 Essentialism, §P7 Evolutionary, §P8 Semantic Grounding,
  §P9 Homeostatic Self-Regulation, §P12 Authenticated Host Mandate)

REGISTRY AUTHORITATIVE (P5.1): Registry manifest + `.j2` templates are
canonical. SKILL.md derived. If conflict, registry wins.
PUBLIC VISIBILITY: `visibility: public` — no hidden runtime posture.
NO FABRICATION: Metric computed from evidence counts only. Blockers derived
from actual `threats`, `defense_layers_silent`, `proposed_regressions`.
No synthetic span references. No synthetic regression counts.
CONVERGENCE INDEPENDENT: Separate `.j2` template; does not duplicate
`emit-regulation` phase logic. Uses `threats` from `classify-threat` (or
from previous monitoring cycle if persistence is enabled by user). If no
previous cycle, assumes `previous_metric = 1.0` (initial state) — explicit
assumption, not hidden.
Regulation SPAN EMITTED: `reg.runtime.convergence` is registered, so the span is
always emitted. Output states `reg_span_emitted: true`.
MINIMAL ARTIFACT: This template produces exactly the metric, verdict,
blockers, and span emission — nothing more.
