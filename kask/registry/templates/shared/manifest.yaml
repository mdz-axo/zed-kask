# Shared template crate manifest
# hKask v0.32.0
#
# The shared convergence-check template has been removed. All skills now use
# the deterministic `kata.convergence_check` compute primitive instead of an
# LLM self-grade convergence-check template. This file is kept for the crate
# structure; if no shared templates remain, the crate can be deleted.

crate:
  name: shared
  version: "0.32.0"
  description: >
    Shared templates used across multiple skill PDCA flows. The old
    convergence-check template has been removed — all skills now use the
    deterministic `kata.convergence_check` compute primitive.

templates: []
