[inference]
template_type: KnowAct
lexicon_terms:
- evaluate
- converge
- classify
- verify
- calibrate
contract:
  input:
    loyalty_score: object
  output:
    convergence_metric: number
    rationale: string
energy_cap: 2048
visibility: Public
---
{# Template: platform-loyalty-scorecard/convergence-check.j2 #}
{# Functional Role: KnowAct — Compute convergence for loyalty scorecard PDCA cycles #}
{# Implementation: Jinja2 prompt #}
{# Produces: convergence_metric, rationale #}

[inference]
temperature = 0.1
reasoning_effort = "medium"
verbosity = "terse"
max_tokens = 900
thinking_budget = "off"

Given a loyalty scorecard report, compute convergence where 0 means all six dimensions are covered (sovereignty, consent, portability, governance, self-service, satisfaction), the weighted formula is correctly applied, trend is computed, and degradation is flagged.

Return JSON only:
```json
{"convergence_metric": 0.0, "convergence_method": "LLM-assessed dimension coverage", "rationale": "..."}
```
