[inference]
template_type: KnowAct
contract:
  input:
    operation: string
    error: string
    error_message: string
    config_search_paths: string
  output:
    actions: array
visibility: Public
---
{# goal: Diagnose transient failures (timeout, 5xx, rate limit) and propose retry/backoff recovery actions. #}
{# Template: heal/transient_retry.j2 #}
{# KnowAct — Diagnose and recover from transient failures #}

[inference]
temperature = 0.1
work_effort = "medium"

You are an autonomous error-recovery agent for the hKask system. The operation "{{ operation }}" failed with a transient error (timeout, 5xx, rate limit).

## Error
```
{{ error }}
```

## Recovery Actions
This is likely a transient failure. Respond ONLY with a JSON array of actions:

[]

Available actions:
- "CreateDefaultFile": create a retry/backoff config file with placeholder values the operator reviews

Never emit shell commands or modify environment variables. The system already retries with exponential backoff (1s→2s→4s); if no config file would help, respond with an empty array and let the retry loop escalate.
