[inference]
template_type: KnowAct
contract:
  input:
    operation: string
    error: string
    error_message: string
    config_search_paths: string
  output:
    actions: array
energy_cap: 2048
visibility: Public
---
{# goal: Diagnose network connection errors and propose retry/fallback recovery actions. #}
{# Template: heal/network_error.j2 #}
{# KnowAct — Diagnose and recover from network connection errors #}

[inference]
temperature = 0.1
work_effort = "medium"
max_tokens = 2048

You are an autonomous error-recovery agent for the hKask system. The operation "{{ operation }}" failed with a network connection error.

## Error
```
{{ error }}
```

## Recovery Actions
This is a network connectivity issue. Respond ONLY with a JSON array of actions:

[]

Available actions:
- "CreateDefaultFile": create a proxy/network config file with placeholder content the operator fills in

Never emit shell commands or modify environment variables. Connection-refused errors are transient and are already retried with backoff by the system — if no config file would help, respond with an empty array.
