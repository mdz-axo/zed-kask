# Template crate manifest for goal-analysis skill
# hKask v0.35.0

crate:
  name: goal-analysis
  version: "0.35.0"
  description: >
    Goal specification and verification. Extracts structured goals from user
    intent, judges completion via semantic evaluation or command execution,
    and produces calibrated verdicts with confidence scoring.

templates:
  - id: goal-analysis/create
    name: "Extract a structured goal from raw user intent"
    path: create.j2
    type: WordAct
    description: >
      Extract a structured goal from raw user intent. Produces goal text,
      completion criteria, visibility setting, and priority level.

  - id: goal-analysis/judge
    name: "Verify goal completion via semantic evaluation of outcome summary and…"
    path: judge.j2
    type: KnowAct
    description: >
      Verify goal completion via semantic evaluation of outcome summary
      and produced artifacts against the original goal criteria.

  - id: goal-analysis/judge_command
    name: "Verify goal completion via executed command results against…"
    path: judge_command.j2
    type: KnowAct
    description: >
      Verify goal completion via executed command results against acceptance
      criteria. Produces a done/continue/blocked verdict with reasoning.

  - id: goal-analysis/judge_simple
    name: "Fallback goal verification with minimal evaluation"
    path: judge_simple.j2
    type: KnowAct
    description: >
      Fallback goal verification with minimal evaluation. Produces a
      continue verdict and default confidence for lightweight judgment.

  - id: goal-analysis/goal-activate
    name: "Activate a goal for tracking"
    path: goal-activate.j2
    type: KnowAct
    description: >
      Activate a goal for tracking. Registers the goal with the goal
      management system and returns an activation confirmation.

  - id: goal-analysis/goal-resolve
    name: "Resolve a goal as completed or blocked"
    path: goal-resolve.j2
    type: KnowAct
    description: >
      Resolve a goal as completed or blocked. Produces a final
      resolution record with the verdict, confidence, and reason.

