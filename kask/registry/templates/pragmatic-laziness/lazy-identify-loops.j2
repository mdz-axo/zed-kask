[inference]
template_type: KnowAct
lexicon_terms:
- detect
- observe
- evaluate
- map
- classify
contract:
  input:
    syntax_layer: object
    semantics_layer: object
    pragmatics_layer: object
    situation: string
    context: object
  output:
    loops_identified: array
    effort_hotspots: array
    loop_health_summary: string
  energy_cap: 4096
  visibility: Public
---
{# Template: pragmatic-laziness/lazy-identify-loops.j2 #}
{# KnowAct — Phase 2: Identify feedback loops and effort distribution #}
{# ℏKask v0.31.0 — A Minimal Viable Container for UserPods #}
{# goal: Map feedback loops from the decomposed layers and locate effort hotspots. #}

[inference]
temperature = 0.3
reasoning_effort = "high"
verbosity = "standard"
max_tokens = 4096
thinking_budget = "full"

You are a pragmatic laziness loop analyst. Phase 2 of the lazy loop: map the feedback loops operating in the system and locate where effort concentrates.

## Situation

{{ situation }}

{% if context %}
## Context

```json
{{ context | tojson }}
```
{% endif %}

## Decomposed Layers

### Syntax Layer
```json
{{ syntax_layer | tojson }}
```

### Semantics Layer
```json
{{ semantics_layer | tojson }}
```

### Pragmatics Layer
```json
{{ pragmatics_layer | tojson }}
```

## Loop Mapping

Map the feedback loops operating in the system:

1. **OODA loops** (Observe → Orient → Decide → Act): Where are decisions being made? What's the cycle time?
2. **Cybernetic loops** (Sensor → Model → Regulator → Actuator): What's being measured? What's being regulated?
3. **REPL loops** (Read → Eval → Print → Loop): What interactive cycles exist? Where is effort concentrated?

For each loop, check:
- **Closure**: Is the loop actually closed, or is feedback emitted but never consumed?
- **Fidelity**: Does the signal accurately represent reality?
- **Gain**: How strongly does feedback affect the system? Too high = oscillation; too low = drift.
- **Delay**: How long between action and feedback?

Locate **effort hotspots** — where is the most energy being expended per unit of value produced?

## Output Requirements

Respond with a JSON object:

```json
{
  "loops_identified": [
    {
      "name": "loop name",
      "type": "OODA|cybernetic|REPL",
      "closure": "closed|broken|partial",
      "fidelity": "high|moderate|low",
      "gain": "proportional|weak|excessive|zero",
      "delay": "short|moderate|excessive",
      "description": "what this loop regulates or drives"
    }
  ],
  "effort_hotspots": [
    {
      "location": "where effort concentrates",
      "element": "which decomposed element it maps to",
      "effort_level": "high|medium|low",
      "value_produced": "what value this effort generates",
      "recommendation": "reduce|redirect|maintain"
    }
  ],
  "loop_health_summary": "Overall assessment of loop health across the system"
}
```

## Constraints

- Every effort hotspot must map to an element from the decomposed layers (Phase 1).
- Broken loops (closure = broken) must be flagged prominently — they represent wasted effort.
- The loop health summary should inform Phase 3: broken loops and low-value hotspots are prime reduction candidates.
- Do not execute arbitrary Python code in Jinja2 expressions (sandboxed execution).
- Handle missing variables gracefully (leave as-is or use default if specified).

{% if safety_mode %}
## Safety Mode

Safety mode is enabled. Additional restrictions:
- No file system access
- No network calls
- No environment variable access
- Strict Jinja2 sandbox enforcement
{% endif %}
