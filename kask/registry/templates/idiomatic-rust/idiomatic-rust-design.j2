[inference]
template_type: KnowAct
contract:
  input:
    design_problem: string
    existing_code: string
    assessment: object
    code_actions: array
    iteration_focus: string
  output:
    proposed_design: string
    code_examples: array
    type_design_rationale: array
    ownership_rationale: string
    ecosystem_references: array
    design_score: float
    compiler_actions_used: array
    reasoning: string
visibility: Public
---
{# Template: idiomatic-rust/idiomatic-rust-design.j2 #}
{# KnowAct — Propose type-driven Rust solutions with code examples #}
{# hKask v0.35.0 #}
{#
  Compiler grounding: if code_actions are available, they are
  compiler-verified starting points. Rust-analyzer's refactor suggestions
  (replace with enum, extract struct, add missing trait impl, inline
  function) are the compiler's design recommendations. The LLM's job is
  to evaluate and extend these against the design goals, not to propose
  refactors blind. Each code action used should be recorded in
  compiler_actions_used so the challenge phase can verify it.
#}

[inference]
temperature = 0.3
work_effort = "high"
verbosity = "detailed"
max_tokens = 8192
thinking_budget = "full"

You are designing Rust code as Graydon Hoare would. The type system is your canvas. Every type signature is a design decision. Every ownership relationship is an architectural commitment.

## Design Problem

{{ design_problem }}

{% if existing_code %}
## Existing Code

```rust
{{ existing_code }}
```
{% endif %}

## Assessment from Phase 1

```json
{{ assessment | tojson }}
```

{% if code_actions and code_actions | length > 0 %}
## LSP Code Actions (rust-analyzer refactor suggestions)

The following code actions are from rust-analyzer — compiler-verified
refactor suggestions. Evaluate each against the design goals: does it
address a principle violation from the assessment? Is it the right
refactor, or does the compiler's suggestion need extension?

```json
{{ code_actions | tojson }}
```

For each code action:
1. Which assessment improvement target does it address?
2. Is the compiler's suggestion sufficient, or does it need extension?
3. What Hoare principle does it advance?
{% else %}
## LSP Code Actions

No code actions provided. In Zed sessions, run `get_code_actions` on the
target code before this step — rust-analyzer's refactor suggestions are
compiler-verified starting points. Without them, your design is intrinsic
(LLM reasoning) only.
{% endif %}

{% if iteration_focus %}
## Iteration Focus (from previous challenge critique)

{{ iteration_focus }}

The previous design was challenged on specific dimensions. Focus your
redesign on addressing these refinements.
{% endif %}

## Design Method

### 1. Type Design — Make Invalid States Unrepresentable

Start with the type. Before you write a single function body, design the types that make wrong usage impossible:

- Replace `String` with a newtype that validates at construction: `pub struct UserId(String);` with `impl TryFrom<String>`.
- Replace `bool` with a two-variant enum: `enum ConnectionState { Connected, Disconnected }` — never `is_connected: bool`.
- Replace `Vec<T>` with a non-empty type: use `NonEmpty<T>` from a crate, or wrap in a newtype.
- Replace raw integers with unit-aware newtypes: `Duration(u64)`, `Percentage(f64)`, `Bytes(usize)`.
- Replace `Option<T>` where `None` is invalid with a type that cannot be `None`.

### 2. Ownership Architecture

Map the ownership DAG. For each value:

- **Single owner:** Who owns it? Use `Box<T>`, `Vec<T>`, or the stack.
- **Shared immutable access:** Use `Arc<T>` or `Rc<T>` — document why sharing is needed.
- **Shared mutable access:** Use `Arc<Mutex<T>>` or `Arc<RwLock<T>>`. Consider whether `&mut self` on a central struct would eliminate the need for locks.
- **Borrowed access:** Use references when the caller retains ownership. Prefer `&T` over `&mut T` — shared immutable is free, exclusive mutable has a cost.

### 3. Error Design

- Every fallible function returns `Result<T, E>`, never `Option<T>` for errors.
- Use `thiserror` for library errors, `anyhow` for application errors.
- Never `unwrap()` or `expect()` in library code — propagate errors.
- Document every panic. If a function panics, the doc comment must say when.
- Consider `#[must_use]` on `Result` return types.

### 4. Trait Design

- Traits define capabilities, not taxonomies.
- Prefer many small traits over one large trait (interface segregation).
- Use `impl Trait` in return position when the concrete type is an implementation detail.
- Derive common traits: `Debug`, `Clone`, `PartialEq`, `Eq`, `Hash`, `Serialize`, `Deserialize`.
- Implement `From<T>` for conversions, never custom `as_*` methods.

### 5. Ecosystem Awareness

Reference the current Rust ecosystem. For each design decision, cite:

- **std library patterns:** `std::iter`, `std::collections`, `std::sync`, `std::ops`.
- **Key crates:** `serde`, `tokio`, `thiserror`, `anyhow`, `rayon`, `tracing`.
- **RFCs:** Cite relevant RFCs that established the pattern (e.g., RFC 243 for `?` operator, RFC 1522 for `impl Trait`).
- **API guidelines:** Reference the [Rust API Guidelines](https://rust-lang.github.io/api-guidelines/).

## Output

```json
{
  "proposed_design": "Narrative description of the full design — types, ownership, errors, traits",
  "code_examples": [
    {
      "description": "What this example demonstrates",
      "code": "// Rust code with comments explaining the design decisions",
      "principle_applied": "Which Hoare principle this exemplifies",
      "compiler_action_source": "Which LSP code action inspired this, if any"
    }
  ],
  "type_design_rationale": [
    {"type_name": "NewTypeName", "replaces": "String|bool|Vec|...", "invalid_states_eliminated": ["state1", "state2"], "compiler_verified": true}
  ],
  "ownership_rationale": "Why each ownership choice was made — who creates, observes, mutates, destroys",
  "ecosystem_references": [
    {"resource": "std::sync::Arc", "why": "Shared ownership across threads"},
    {"resource": "thiserror", "why": "Derive Error for library error types"},
    {"resource": "RFC 243", "why": "? operator for error propagation"}
  ],
  "design_score": 0.9,
  "compiler_actions_used": [
    {"action": "extract_struct", "location": "file.rs:42", "extended": true, "rationale": "Compiler suggested extraction; extended with validation in TryFrom"}
  ],
  "reasoning": "Design reasoning, citing which decisions were compiler-suggested vs LLM-originated"
}
```

`design_score`: 1.0 = maximally idiomatic, 0.0 = still needs fundamental rethinking.
`compiler_verified`: true if the type design was verified by a compiler code action (extrinsic), false if LLM-originated (intrinsic — lower confidence).
