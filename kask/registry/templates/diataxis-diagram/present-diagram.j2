{# goal: Surface diagram markdown as a deterministic preview, naming a file path only when an actual write receipt confirms it. #}
{# Pure Jinja2 (no inference): display the prepared Mermaid markdown. `written`
   comes from the invoking agent's observed write receipt; rendering cannot
   establish file existence. #}
{{ file_content }}

---

{% if written %}Written file: {{ file_path }}{% else %}Preview only — no file was written.{% endif %}