{# goal: Rendering template — surfaces the finalized diagram markdown (containing the fenced ```mermaid block) as the process's final output string. Without this step, extract_final_step_result returns the write step's JSON object {file_path, file_content, description_paragraph} and the model must discover and extract the file_content field. This render step flattens it to a raw markdown string so the model sees the mermaid block directly. Deterministic (no LLM call). #}
{# Surface the finalized diataxis-diagram markdown as the process's final output.
   This is a pure Jinja2 template (no [inference] header, no LLM call) rendered by
   the `render_template` tool from the SKILL.md body's final step.

   The write step produces {file_path, file_content, description_paragraph} where
   file_content contains the fenced ```mermaid block. Without this render step, that
   JSON object is the step output and the model must discover and extract the
   file_content field. This render step flattens it to a raw markdown string so the
   model sees the mermaid block directly and reproduces it in its assistant message,
   which acp_thread renders. #}
{{ file_content }}

---

File path: {{ file_path | default("(not specified)") }}